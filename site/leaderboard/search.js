window.LB_SEARCH = JSON.parse('[{"u":"","t":"Knowledge Base","c":"Knowledge Base","d":"How the HttpArena benchmarks work: what each test profile measures, how the load generators drive traffic, and how results are scored.","x":"Learn how HttpArena benchmarks work, what each test profile measures, and how the load generators drive traffic. Load Generators How gcannon (HTTP/1.1) and h2load (HTTP/2) drive traffic for the benchmark suite. Hardware & Topology Reference machine: Threadripper PRO 3995WX. NUMA, cache, SMT, and how the harness pins resources. Scoring How the composite score is computed: normalization, arithmetic mean, and participation penalties. Running Locally Set up and run the full benchmark suite on your own machine with Docker and gcannon. Add a Framework Step-by-step guide to adding your framework: endpoints, Dockerfile, metadata, and PR."},{"u":"add-framework","t":"Add a Framework","c":"Knowledge Base","d":"Submit a framework to HttpArena: create a Dockerfile, declare metadata, implement the required endpoints, and open a pull request.","x":"Adding a framework to HttpArena takes a few steps: create a Dockerfile, add metadata, implement the required endpoints, and open a PR. Directory Structure How to organize your framework\'s files - Dockerfile, meta.json, and source code. Test Profiles All HTTP endpoints your framework must implement, organized by test profile. meta.json Framework metadata - display name, language, type, and which tests to participate in. Implementation Rules Rules for keeping benchmarks realistic - use framework APIs, production settings only, standard libraries. README Badge Publish your entry\'s rank in your own README - one live badge per protocol family."},{"u":"add-framework/badges","t":"README Badge","c":"Knowledge Base \\u203a Add a Framework","d":"Add a live HttpArena rank badge to your project\'s README: one URL per protocol family, updated every time the leaderboard is republished.","x":"Once your framework has results on the board, it gets a badge for every protocol family it scored in. The badge reports where the entry ranks, and it re-renders itself each time the leaderboard is republished. You paste it once. HTTP Arena H/1.1 \\u00b7 #6 of 71 Paste it [![HTTP Arena](https://img.shields.io/endpoint?url=https://www.http-arena.com/badge/actix/h1.json)](https://www.http-arena.com/#type=emerging,flagship&tuned=0) Replace actix with your entry and h1 with the family you want. The exact line for your framework, already assembled and deep-linked to the right view of the board, is in /badge/index.json under scopes.<family>.default.markdown . Families Family <family> What the composite covers HTTP/1.1 h1 Connection, Workload, Database and Multi-endpoint profiles HTTP/2 h2 the HTTP/2 profiles HTTP/3 h3 the HTTP/3 profiles Gateway gw reverse-proxy and production-stack profiles gRPC grpc unary and streaming, plaintext and TLS WebSocket ws the echo profiles <framework> is your entry\'s board name, lowercased, with anything outside A-Z a-z 0-9 . _ - replaced by - . It is the same name as your file under site/data/results/ , so aspnet-minimal + nginx becomes aspnet-minimal-nginx . Ranked among your own language Optional, and a second badge rather than a replacement. Append -<language> to the family and the badge reads #1 of 6 (Rust) : [![HTTP Arena](https://img.shields.io/endpoint?url=https://www.http-arena.com/badge/actix/h1-rust.json)](https://www.http-arena.com/#type=emerging,flagship&tuned=0&lang=Rust) The language segment is the board\'s own language name, lowercased, with # spelled out as sharp and ++ as pp : C# \\u2192 csharp , C++ \\u2192 cpp , TS \\u2192 ts , JS \\u2192 js . Your ready-made line is in /badge/index.json under scopes.<family>.byLanguage.markdown . Counting tuned entries in Also optional. The default field is standard configurations only , so the number compares like for like: a stock server is never ranked against someone else\'s hand-tuned build. Append -with-tuned to count tuned entries as well: [![HTTP Arena](https://img.shields.io/endpoint?url=https://www.http-arena.com/badge/actix/h1-with-tuned.json)](https://www.http-arena.com/#type=emerging,flagship&tuned=1) The suffixes combine, so the full set for one family is: File Field h1.json standard entries h1-with-tuned.json standard + tuned h1-rust.json standard, one language h1-rust-with-tuned.json standard + tuned, one language Each is in /badge/index.json under scopes.<family> , keyed default , withTuned , byLanguage and byLanguageWithTuned . If your entry is itself tuned , it has no place in the standard-only field, so its default URL serves the tuned-inclusive ranking instead, and h1.json and h1-with-tuned.json give it the same number. Nothing to change, and no URL stops working. This is a filter, not a rescore . Scores and order are identical to the overall badge; only the field is narrowed, so the two can never disagree about who is ahead of whom. The link opens the board filtered to that language with #lang=Rust , an exact match unlike the search box, so the field on the page is the field the badge claims. What the colours mean The two halves are read separately. The right half is placement: gold for first, then shading down by how far into the field the entry sits. The left half is the tier it competed in, in the same four colours the board uses for the type swatch beside every framework name: Tier Ranked against \\ud83d\\udfe9 flagship flagship + emerging, as one field \\ud83d\\udfe6 emerging flagship + emerging, as one field \\ud83d\\udfe7 experimental other experimental entries \\ud83d\\udfe5 engine other engines, on the profiles engines are scored on The tier is also in /badge/index.json as type , next to each entry\'s ranks. What the number means The rank comes from the composite score for that family: the same sum of normalized per-profile scores the board shows, computed the same way, at the board\'s default settings. Following the badge lands you on the field it was measured against, meaning the whole league for that family rather than your entry on its own, so #6 of 83 has the other 82 next to it. You are ranked inside your own tier. flagship and emerging entries rank together as one field, because that is the board\'s default view. engine entries rank among engines, over the smaller profile subset engines are scored on. Keeping the tiers apart is what stops a framework\'s 100 from being set by an engine\'s result, or the reverse. A badge only exists where you scored. No result in a family means no badge for that family. A family where your tier holds a single entry publishes nothing, because a rank is only worth showing once there was somebody to beat. The field is every row the board lists. #1 of 31 is the number you get counting rows in that league on the linked page. That includes entries sitting at 0 because nothing they ran counts for their tier. An engine whose only HTTP/1.1 results are json (not scored for engines) and pipelined (reference-only) still holds a place in the field. Those entries are counted, but get no badge of their own: there is no placing to claim. Freshness The endpoints are regenerated whenever the site deploys, including every time a benchmark result is saved, since results live under site/ . Two caches sit in front of that: Holds a stale rank for shields.io 5 minutes ( cacheSeconds , at its floor) GitHub Camo a few hours So a new rank is visible almost immediately wherever the badge is embedded directly, and takes a few hours to turn over inside a GitHub README. Nothing to re-paste either way. To see the current value straight away, add any parameter to the shields URL, such as &style=flat-square , which makes a fresh cache key. Styling The endpoints are plain shields.io documents, so shields\' own options work. Append &style=flat-square , &logo=rust or &labelColor=\\u2026 to the img.shields.io URL."},{"u":"add-framework/directory-structure","t":"Directory Structure","c":"Knowledge Base \\u203a Add a Framework","d":"The files every HttpArena entry provides under frameworks/, and what each one is used for during build, validation and benchmarking.","x":"Create a directory under frameworks/ with your framework\'s name: frameworks/ your-framework/ Dockerfile meta.json ... (source files) Dockerfile The Dockerfile should build and run your server. Containers are started with --network host , so bind to: Port 8080 - HTTP/1.1 Port 8443 - HTTPS with HTTP/2 and HTTP/3 (if supported) Example (Go): FROM golang:1.22-alpine AS build WORKDIR /app COPY go.mod go.sum ./ RUN go mod download COPY . . RUN CGO_ENABLED=0 go build -o server . FROM alpine:3.19 COPY --from=build /app/server /server CMD [\\"/server\\"] Mounted volumes The benchmark runner mounts these paths into your container (read-only): Path Purpose /data/dataset.json 50-item dataset for /json endpoint /data/static/ 20 static assets (CSS, JS, HTML, fonts, images) - 15 ship with .gz and .br sibling files for precompression-aware frameworks /certs/server.crt TLS certificate for HTTPS/H2/H3 /certs/server.key TLS private key for HTTPS/H2/H3 Postgres (profiles async-db , crud , api-4 , api-16 , and the compose-orchestrated gateway + production-stack) is provided by a separate sidecar container, reachable via the DATABASE_URL environment variable - not a mount. Redis (profile crud ) is similarly reachable via REDIS_URL . See Configuration for the full env var list."},{"u":"add-framework/implementation-rules","t":"Implementation Rules","c":"Knowledge Base \\u203a Add a Framework","d":"Every entry declares a type in meta.json. Learn which rules apply to framework, engine, infrastructure and experimental entries, and how each is ranked.","x":"Every entry declares a type in meta.json - what it is and how it is ranked. The split is about what the entry is , not how featureful it is: a server applications are written against is a framework entry however thin, and the framework tiers then grade how production-proven it is. Framework entries (Flagship / Emerging / Experimental) additionally declare a mode (Standard or Tuned). Frameworks Servers you write application code against, tiered by how production-proven they are. Run in Standard or Tuned mode. Engine HTTP implementations applications are not written against (raw sockets, custom parsers, WSGI/ASGI hosts). Ranked separately. Infrastructure Reverse proxies and static-file servers (nginx, Caddy, h2o) without an app framework layer."},{"u":"add-framework/implementation-rules/engine","t":"Engine","c":"Knowledge Base \\u203a Add a Framework \\u203a Implementation Rules","d":"Rules for engine entries: HTTP implementations applications are not written against - raw sockets, low-level I/O and protocol hosts - ranked separately from frameworks.","x":"Engine entries ( type: engine ) are HTTP implementations that applications are not written against - raw sockets, custom parsers, low-level I/O, built to show what a transport or a technique can do rather than to host application code. They are not frameworks and are ranked separately. (Reverse proxies and static-file servers like nginx and h2o are classified as Infrastructure , not Engine.) What qualifies as an engine Raw TCP socket servers with custom HTTP parsing Direct io_uring or epoll implementations Application-server hosts, where your code is written against a protocol interface rather than the server\'s own API - a WSGI or ASGI server, a Rack handler What does not A server whose own API is the surface applications are written and deployed on is a framework entry, however thin it is. A runtime\'s built-in HTTP server - node:http , Deno.serve , Bun.serve , Go\'s net/http - is a framework entry, and so is a low-level server library that people ship services on directly. Having no router, no middleware stack and no ecosystem does not make something an engine - see Frameworks . Rules Must implement the endpoint spec correctly Must pass the validation suite No restrictions on implementation approach, with one exception: on the static profiles, file contents may be served from memory only through the engine\'s own static file handling, not a cache assembled in the entry, and that cache must follow the disk - replacing a file must change the next response Ranked separately from framework entries (flagship and emerging) Only participates in connection-level tests (baseline, pipelined, limited-conn) and protocol tests (H2, H3, gRPC, WebSocket) by default"},{"u":"add-framework/implementation-rules/frameworks","t":"Frameworks","c":"Knowledge Base \\u203a Add a Framework \\u203a Implementation Rules","d":"How framework entries are grouped into the Flagship, Emerging and Experimental tiers, and what each tier requires of a submission.","x":"Framework entries are servers you write application code against. There is exactly one requirement: The entry must implement HTTP itself - parse the request line, headers and body, frame the response, and handle keep-alive, chunked bodies and fragmented reads correctly. It must pass the validation suite on its own, not by leaning on something in front of it. That is the whole bar. Routing, middleware, a plugin ecosystem and a template engine are not required. A runtime\'s own HTTP server qualifies, and so does a minimal one, as long as applications are genuinely written and deployed on it. How much the server does for you is not ignored - it is scored rather than gated. Routing, middleware, whether the request arrives already made - path parameters, query parameters, a body buffered or streamed - and whether the response is built for you are the four completeness axes, and each one the entry leaves to you takes 2.5% off its composite. A thin server and a batteries-included framework compete in the same tier; the difference between them shows up in that factor and in the throughput they buy with it. The three tiers grade one thing: how proven the entry is in production. Not ecosystem size, not feature count, not how many profiles it covers. Pick your best fit and it may be adjusted on review. Set it with meta.json.type . Flagship - proven in production. Real applications are built on it and operated on it at scale, and it is actively maintained. Whether it hands you a router and a middleware stack or a bare request callback makes no difference here. Emerging - a genuine, working server that is not yet production-proven: young, niche, or without real deployments to point to. Experimental - very new work that has not proved itself yet. Ranked alongside frameworks, but hidden by default on the leaderboard (opt-in via the type filter). All three are scored in the same framework normalization pool and can be combined on the leaderboard. Completeness Framework entries also declare completeness in meta.json : which of routing, middleware, the request it hands you and the response it builds the server does for you. Each one it leaves to you costs 2.5% of the whole composite, so a framework that does all four keeps its full score and one that does none keeps 90% of it. It is the counterweight to the open door above: a server can enter the framework tiers with nothing but an HTTP parser, but it competes there carrying the score of one. What counts as done on each axis, how to declare it, and how the multiplier is computed are on the Completeness page. Engine and infrastructure entries are not graded on it. Mode Every framework entry also declares a mode in meta.json.mode - how strictly it follows the implementation rules. The same framework can be submitted in either mode; tuned entries are marked with a ring on the leaderboard and ranked alongside standard ones. Standard Default, production-style usage: documented framework APIs, production settings, and standard libraries. Tuned Non-default configs, experimental flags, and custom optimizations allowed."},{"u":"add-framework/implementation-rules/frameworks/standard","t":"Standard","c":"Knowledge Base \\u203a Add a Framework \\u203a Implementation Rules \\u203a Frameworks","d":"Standard mode measures a framework in its default configuration, with no experimental flags, reflecting what developers get out of the box.","x":"Standard is the default mode for a framework entry ( mode: standard ). It measures what the framework gives you out of the box, the way developers actually use it in real applications - documented APIs, production-grade settings, and standard libraries. Use framework-level APIs If a framework provides a documented, high-level way to accomplish a task, the benchmark implementation must use it. Bypassing the framework to hand-roll a faster solution is not permitted. Good Bad # Use the framework\'s built-in parameter binding @app.get(\\"/baseline\\") def baseline(a: int, b: int): return str(a + b) # Manually parse query string for speed @app.get(\\"/baseline\\") def baseline(request): qs = request.url.query.encode() a = fast_parse_int(qs, b\\"a=\\") b = fast_parse_int(qs, b\\"b=\\") return custom_serialize(a + b) Settings must be production-documented Non-default configuration is allowed only if the framework\'s official production deployment guide recommends it . If there is no official documentation recommending a setting for production use, it does not belong in the benchmark. Allowed: GC settings recommended in production deployment guides Worker/thread counts matching available CPU cores Connection pool sizes for the environment Not allowed: Undocumented flags found by reading framework source code Experimental or unstable options that trade safety for speed Settings that disable buffering, validation, or error handling Use standard libraries and drivers If the ecosystem has a well-established, production-grade library for a task (database driver, JSON serializer), use it. Experimental or hand-rolled alternatives solely for benchmark performance are not permitted. Exception: If the framework itself bundles or officially recommends a specific library, that library is acceptable. Static files: the cache is the framework\'s, not the entry\'s For the static file tests an entry may serve file contents out of memory. Caching is not the thing being excluded - the operating system caches these files anyway, and after the first read the bytes are in RAM whatever the entry does. Two things are required instead. The cache must be the framework\'s own. A documented static file handler, with whatever caching and invalidation it comes with, is fine - if it passes validation it counts, and what it does internally is its business. What is not allowed is a cache assembled in the entry: reading the directory into a map at startup, holding pre-loaded buffers, mapping the files by hand. Moving that code behind a function name does not change what a user of the framework actually gets, and what a user gets is what these numbers are meant to report. The cache must follow the disk. Replace a file and the next response must carry the new bytes. A cache that is filled once and never revalidated is serving something the filesystem no longer contains, which is not serving files - it is replaying them. Memory-mapping sits on the same footing. It is fine when the framework\'s static handler does it and the mapping tracks the file; it is not fine when the entry maps the directory at startup and serves whatever it captured, because a mapping keeps pointing at the inode it was opened on and files are normally changed by being replaced. This applies to Standard, Tuned and Engine entries alike. Infrastructure entries (reverse proxies and static-file servers) are exempt from the first requirement as well: configuring a cache is the job that tier is measuring. Static file compression Compression of static files is optional but recommended for better results. All static file requests include Accept-Encoding: br;q=1, gzip;q=0.8 - frameworks that compress will naturally benefit from reduced I/O. Standard rule: compression must use the framework\'s standard middleware or built-in static file handler (e.g., Nginx gzip on / gzip_static on , ASP.NET response compression middleware, Express compression() middleware). No handmade compression code. Pre-compressed files ( .gz , .br ) are available on disk alongside the originals, and serving them is allowed for every entry type . Use the framework\'s own feature where it has one (e.g. Nginx gzip_static / brotli_static , ASP.NET MapStaticAssets , Hono serveStatic({ precompressed: true }) ); where it has none, select the variant in the entry off Accept-Encoding . Those bytes already exist on disk, so choosing one is a file read, not compression - which is why it is allowed here while compressing by hand is not. The response must carry the original file\'s Content-Type and the matching Content-Encoding . This is a deliberate change. The rule used to allow pre-compressed files only behind a documented API, which made the profile turn on whether a framework happened to ship that one feature: an entry sending full-size bodies against entries sending brotli is not doing the same work, and the resulting gap swamped everything else the profile measures. What is still out is a static handler written for this benchmark. The cache must be the framework\'s own and must follow the disk, and nothing may be compressed at runtime by code in the entry. The line is what the framework gives you, not what can be written against it: whatever a framework\'s own static handler does internally is its business - if it passes validation, it counts. Tuned entries are free to hand-roll the same thing; the distinction only binds Standard, where the point is to show what the framework does out of the box. Deployment-environment tuning Adapting to the benchmark hardware is permitted: Setting worker count to match CPU cores Configuring connection pool sizes Adjusting memory limits for the container The boundary is: adapt to the environment, do not exploit it."},{"u":"add-framework/implementation-rules/frameworks/tuned","t":"Tuned","c":"Knowledge Base \\u203a Add a Framework \\u203a Implementation Rules \\u203a Frameworks","d":"Tuned mode allows non-default configuration, experimental flags and optimizations beyond what a framework ships with by default.","x":"Tuned mode ( mode: tuned ) gives a framework entry more freedom. It can use non-default configurations, experimental flags, and optimizations that go beyond standard framework usage. Tuned entries are marked with a ring on the leaderboard and shown alongside standard ones. What is allowed Alternative JSON serializers (simd-json, sonic-json, etc.) Custom buffer sizes and TCP socket options Experimental or unstable framework flags Custom thread pools and worker configurations Non-default GC settings without documentation requirement Framework-specific performance flags not recommended for production Any compression approach for static files - custom compression, pre-compressed file serving, alternative compression libraries What is NOT allowed Pre-computed response bodies - serializing a fixed response at startup and returning the same bytes per request (e.g. caching a JSON blob and writing it back unchanged). The serialization + compression work is the workload; bypassing it defeats the measurement. Response caching - memoizing the full HTTP response body keyed by URL/params and replaying it. This is distinct from upstream data caching (DB query results, JWT verification, etc.), which remains allowed where the profile calls for it (e.g. the CRUD profile\'s read cache). Hand-rolled static file caching - reading the directory into a map at startup, holding pre-loaded buffers, or memory-mapping the files in the entry. Serving static files from memory is allowed, but the cache has to be the framework\'s own static file handler rather than one assembled here, and it has to follow the disk: replace a file and the next response must carry the new bytes. Pre-rendered response headers and pre-compressed .gz / .br variants on disk are still fine. What is still required Must use the framework\'s HTTP server (not a raw socket replacement) Must implement all endpoint specs correctly Must pass the validation suite The framework dependency must be a real, published framework When to choose Tuned If your submission uses any setting or optimization that a typical production team would not use, set mode to tuned. If in doubt, start with standard and switch to tuned only once you add non-standard optimizations."},{"u":"add-framework/implementation-rules/infrastructure","t":"Infrastructure","c":"Knowledge Base \\u203a Add a Framework \\u203a Implementation Rules","d":"Rules for infrastructure entries: reverse proxies and static-file servers such as nginx, Caddy and h2o, run without an application framework layer and ranked separately.","x":"Infrastructure entries ( type: infrastructure ) are reverse proxies and static-file servers - nginx, Caddy, h2o and the like - run without an application framework layer. Like engines, they are not frameworks and are ranked separately. What qualifies Reverse proxies terminating TLS and forwarding upstream (nginx, Caddy, h2o) Standalone static-file servers Edge servers used purely as a proxy in front of an application Something that embeds one of these servers but ships application code on top of it is a framework entry, not an infrastructure one - openresty , ngx-php and frankenphp are ranked with the frameworks. Rules Must implement the endpoint spec correctly and pass the validation suite No restrictions on configuration Ranked separately from framework entries: its own composite, its own normalization pool A handler module written specifically for the benchmark is allowed - that is how a server without an application layer answers /baseline11 and /json at all - but it must be a module loaded by the server, not a replacement for it Scored profiles Infrastructure is scored on the eleven profiles a server can answer without an application framework behind it: Category Profiles Connection Baseline, Pipelined, Short-lived Workload JSON, JSON TLS, Static, Static TLS HTTP/2 Baseline, Static HTTP/3 Baseline, Static Everything else - upload, the database and CRUD profiles, the API mixes, gRPC, WebSocket and the gateway stacks - is displayed as reference data where an entry has it, but does not count toward the infrastructure composite. Pipelined is the one profile scored here and nowhere else. It stopped counting for frameworks in #1058 because it measures batching behaviour more than framework throughput; for a proxy that behaviour is the thing being compared, so it stays in."},{"u":"add-framework/meta-json","t":"meta.json","c":"Knowledge Base \\u203a Add a Framework","d":"Every field in a framework\'s meta.json: display name, language, type, mode, engine, maintainers, and the tests the entry subscribes to.","x":"Create a meta.json file in your framework directory: { \\"display_name\\": \\"your-framework\\", \\"language\\": \\"Go\\", \\"engine\\": \\"net/http\\", \\"type\\": \\"flagship\\", \\"mode\\": \\"standard\\", \\"completeness\\": { \\"routing\\": true, \\"middleware\\": true, \\"request\\": true, \\"response\\": true }, \\"description\\": \\"Short description of the framework and its key features.\\", \\"repo\\": \\"https://github.com/org/repo\\", \\"enabled\\": true, \\"tests\\": [\\"baseline\\", \\"pipelined\\", \\"limited-conn\\", \\"json\\", \\"json-comp\\", \\"upload\\", \\"api-4\\", \\"api-16\\", \\"baseline-h2\\", \\"static-h2\\"], \\"maintainers\\": [\\"your-github-username\\"] } Fields Field Description display_name Name shown on the leaderboard language Programming language (e.g., Go , Rust , C# , Java ) engine HTTP server engine (e.g., Kestrel , Tomcat , hyper ) type flagship , emerging or experimental for frameworks - servers you write application code against, tiered by how production-proven they are; engine for implementations applications are not written against (raw sockets, WSGI/ASGI hosts); infrastructure for reverse proxies / static-file servers. See Implementation Rules mode Frameworks only: standard (default - idiomatic, production-style usage) or tuned (non-default config / optimizations) completeness Frameworks only: which of routing , middleware , request and response the framework does for you. Each one set to false takes 2.5% off the composite; an axis you leave out reads as done, and omitting the field scores a full \\u00d71.00. Omit it entirely on an entry that runs only WebSocket or gRPC profiles - the factor never applies on those boards. See Completeness description Shown in the framework detail popup on the leaderboard repo Link to the framework\'s source repository enabled Set to false to skip this framework during benchmark runs tests Array of test profiles this framework participates in maintainers Array of GitHub usernames to notify when a PR modifies this framework Available test profiles Profile Protocol Required endpoints baseline HTTP/1.1 /baseline11 pipelined HTTP/1.1 /pipeline limited-conn HTTP/1.1 /baseline11 json HTTP/1.1 /json/{count}?m=N json-comp HTTP/1.1 /json/{count}?m=N (must honor Accept-Encoding: gzip, br ) json-tls HTTP/1.1 + TLS /json/{count}?m=N (port 8081, ALPN http/1.1 ) upload HTTP/1.1 /upload api-4 HTTP/1.1 /baseline11 , /json/{count} , /async-db (4 CPU, 16 GB) api-16 HTTP/1.1 /baseline11 , /json/{count} , /async-db (16 CPU, 32 GB) static HTTP/1.1 /static/* (port 8080) async-db HTTP/1.1 /async-db?min=X&max=Y&limit=N (requires DATABASE_URL ) crud HTTP/1.1 /api/items , /api/items/{id} (GET/POST/PUT; requires DATABASE_URL , optional REDIS_URL ) baseline-h2 HTTP/2 /baseline2 (TLS, port 8443) static-h2 HTTP/2 /static/* (TLS, port 8443) baseline-h2c HTTP/2 cleartext /baseline2 (port 8082, prior-knowledge) json-h2c HTTP/2 cleartext /json/{count}?m=N (port 8082, prior-knowledge) baseline-h3 HTTP/3 /baseline2 (QUIC, port 8443) static-h3 HTTP/3 /static/* (QUIC, port 8443) gateway-64 HTTP/2 Compose stack serving /static/* , /json , /async-db , /baseline2 (TLS, port 8443) gateway-h3 HTTP/3 Compose stack serving /static/* , /json , /async-db , /baseline2 (QUIC, port 8443) production-stack HTTP/2 Compose stack: edge + JWT auth sidecar + Redis + server (TLS, port 8443) unary-grpc gRPC BenchmarkService/GetSum (h2c, port 8080) unary-grpc-tls gRPC BenchmarkService/GetSum (TLS, port 8443) echo-ws WebSocket /ws echo (port 8080) Only include profiles your framework supports. Frameworks missing a profile simply don\'t appear in that profile\'s leaderboard. Per-profile endpoint contracts, request/response shapes, and validation rules live under the Test Profiles section - link to the specific profile\'s Implementation page from your PR description when adding a new framework."},{"u":"hardware","t":"Hardware & Topology","c":"Knowledge Base","d":"The reference machine behind every HttpArena result: an AMD Threadripper PRO 3995WX workstation, its CPU topology, and how cores are allocated.","x":"The reference hardware for HttpArena results is a single-socket AMD Threadripper PRO 3995WX workstation. Every benchmark run uses the same machine, so numbers are directly comparable framework-to-framework. This page explains the CPU topology - NUMA, cache hierarchy, SMT - and how the benchmark harness pins resources against it. Processor Spec Value Model AMD Ryzen Threadripper PRO 3995WX Architecture Zen 2 Physical cores 64 Logical threads 128 (SMT2) Sockets 1 Base / boost clock 2.7 GHz / 4.2 GHz TDP 280 W Memory channels 8 \\u00d7 DDR4-3200 Aggregate DRAM bandwidth ~205 GB/s Total L3 256 MB (16 \\u00d7 16 MB) Total L2 32 MB (64 \\u00d7 512 KB) Total L1d / L1i 2 MB each (64 \\u00d7 32 KB) NUMA layout Confirmed with numactl --hardware : available: 1 nodes (0) node 0 cpus: 0-127 node 0 size: 257 GB node distances: node 0 0: 10 One NUMA node , all 128 logical threads, all DRAM channels. This is NPS1 mode (Nodes Per Socket = 1), configured in BIOS. All memory is symmetrically accessible to every core with identical reported latency, so the kernel scheduler has full placement freedom and software doesn\'t need NUMA policies. Could NPS4 help? No, and it could hurt. NPS4 would split the chip into 4 NUMA nodes (16 cores + 2 DRAM channels each), surfacing physical proximity asymmetries to the kernel. That\'s useful for shared-nothing workloads - multiple independent DBs, per-node JVMs, etc. HttpArena\'s server workloads are the opposite: heavily shared mutable state (thread-pool queues, IMemoryCache , Npgsql multiplexer state, Postgres shared buffers). Under NPS4 that shared state gets a \\"home node\\" and cross-node access becomes measurably slower. Also, at our throughput (~1 KB memory traffic per request \\u00d7 350K rps \\u2248 350 MB/s) we use 0.2% of aggregate memory bandwidth - the resource NPS partitions isn\'t one we\'re constrained on. Cache hierarchy Level Size Shared by L1i + L1d 32 KB each One physical core (plus its SMT sibling) L2 512 KB One physical core (plus its SMT sibling) L3 16 MB One CCX - 4 physical cores / 8 threads Zen 2 arranges cores into CCXs (core complexes) of 4 physical cores. Each CCX has its own exclusive L3 slice. Physical chiplets on the 3995WX contain 2 CCXs each, and there are 8 chiplets \\u2192 16 CCXs total , 16 MB L3 per CCX. Within a CCX, L3 access is ~40 cycles. Crossing CCXs (Infinity Fabric) is ~110 cycles. So CCX boundaries are the real locality boundaries on this chip, more so than NUMA. CCX-to-CPU mapping Verified from /sys/devices/system/cpu/cpuN/cache/index3/shared_cpu_list : CCX Physical cores (and SMT siblings) 0 0\\u20133, 64\\u201367 1 4\\u20137, 68\\u201371 2 8\\u201311, 72\\u201375 3 12\\u201315, 76\\u201379 4 16\\u201319, 80\\u201383 5 20\\u201323, 84\\u201387 6 24\\u201327, 88\\u201391 7 28\\u201331, 92\\u201395 8 32\\u201335, 96\\u201399 9 36\\u201339, 100\\u2013103 10 40\\u201343, 104\\u2013107 11 44\\u201347, 108\\u2013111 12 48\\u201351, 112\\u2013115 13 52\\u201355, 116\\u2013119 14 56\\u201359, 120\\u2013123 15 60\\u201363, 124\\u2013127 SMT (Simultaneous Multithreading) Each physical core has two hardware threads. The sibling of CPU N is CPU N+64 : cpu0 \\u2192 0,64 cpu1 \\u2192 1,65 ... cpu63 \\u2192 63,127 Verified from /sys/devices/system/cpu/cpuN/topology/thread_siblings_list . Why this matters for pinning: SMT siblings share L1, L2, execution units, and decode bandwidth. When you cpuset-pin a consumer, always include both threads of a pair - splitting an SMT pair across two different consumers creates pathological cache thrashing. When the harness assigns, say, 0,64 to Redis and 1-31,65-95 to the server, both sides get coherent pairs. SMT2 roughly yields +30% throughput on our workload over single-thread-per-core - useful for latency-bound async handlers where one logical thread is usually idle waiting on I/O while its sibling can execute. How HttpArena pins against this topology Most profiles use a 64-thread server cpuset: 0-31,64-95 . That\'s 32 physical cores spanning CCX 0\\u20137 (8 CCXs = 128 MB L3 budget). The load generator (gcannon) gets 32-63,96-127 - symmetric split, CCX 8\\u201315. One half of the chip drives the test, the other half serves it. Same NUMA node either way. For profiles that need a sidecar (Postgres, Redis), the harness reshuffles: crud profile (uses Redis): consumer phys threads cpuset L3 reach Redis 1 2 0,64 CCX 0 (shared with 3 server cores) Server 31 62 1-31,65-95 CCX 0\\u20137 (112 MB L3 exclusive + 16 MB shared with Redis) Gcannon 32 64 32-63,96-127 CCX 8\\u201315 (128 MB L3) Postgres unpinned - - kernel-scheduled, typically lands on server CCXs for request-path L3 locality Redis sharing a CCX with a few server cores is beneficial - data the server just wrote to Redis (on cache miss) stays in the shared L3 when it reads back (on hit). Moving Redis to a non-server CCX would introduce a ~70-cycle inter-CCX coherence hop per read. production-stack profile (explicit multi-service pinning): Service cpuset edge (nginx) 4-15,68-79 (12 phys) authsvc (JWT verifier) 16-19,80-83 (4 phys) Redis (cache) 15,79 (1 phys) Postgres (unpinned) - server (framework) 0-3,20-31,64-67,84-95 (16 phys) gcannon 32-63,96-127 (32 phys) This split is empirically tuned - see the CHANGELOG entry for 2026-04-16 for the calibration history and why an edge-heavy allocation works at the given rps. Kernel tuning applied per run scripts/lib/system.sh runs before each benchmark: CPU governor \\u2192 performance (no DVFS ramp delays) net.core.somaxconn \\u2192 65535 (accept queue) net.ipv4.tcp_max_syn_backlog \\u2192 65535 net.core.netdev_max_backlog \\u2192 65535 net.ipv4.ip_local_port_range \\u2192 1024 65535 (avoid ephemeral port exhaustion under -r reconnect storms) net.ipv4.ip_local_reserved_ports \\u2192 5432,6379,8080,8081,8082,8443,9090 , every port a server or sidecar listens on. The range above is wide enough to include them, so without this the kernel can hand one to an outbound load-generator socket and the next profile to bind it fails. That is not theoretical: production-stack died at the end of a full run with bind 0.0.0.0:9090: Address in use while passing when run first. Reserving keeps them bindable and out of ephemeral allocation. net.ipv4.tcp_tw_reuse \\u2192 1 (outbound reuse only; it does not help a listening bind) net.ipv4.tcp_max_tw_buckets \\u2192 131072 net.core.rmem_max / wmem_max \\u2192 7.5 MB (UDP buffer for QUIC) Loopback MTU \\u2192 1500 (realistic Ethernet; the default 65536 hides kernel segmentation cost) Docker daemon restart to pick up the new limits Post-run system_restore reverts governor and loopback MTU to defaults. Practical takeaways CCXs are the locality unit, not NUMA nodes. Pin consumers to contiguous 4-core groups when you can; avoid splitting a CCX across two consumers unless you explicitly want them to share L3 (like Redis + server). Keep SMT pairs together. Every cpuset in the harness respects (N, N+64) pairing - preserved automatically if you specify cpusets like a-b,(a+64)-(b+64) . NUMA is a non-issue on this chip. Don\'t waste time with numactl --membind or NPS subdivision. Memory bandwidth is 99.8% idle at our rps. Memory-side optimizations only help workloads with much higher per-request data movement (analytics, streaming) than REST-API-style request handling."},{"u":"load-generators","t":"Load Generators","c":"Knowledge Base","d":"HttpArena drives each transport with a purpose-built load generator. An overview of gcannon, wrk, zrk, h2load and h2load-h3, and when each is used.","x":"HttpArena uses a different load generator for each transport / workload. HTTP/1.1 gcannon (io_uring) for most tests, wrk for static file serving, zrk for the fixed-rate Latency-1M profile. HTTP/2 h2load - nghttp2\'s load generator with TLS and stream multiplexing. HTTP/3 h2load-h3 - nghttp2 + ngtcp2 for QUIC-based HTTP/3 benchmarks. WebSocket gcannon --ws - io_uring WebSocket echo driver reusing the HTTP/1.1 engine with a frame-aware send/recv loop."},{"u":"load-generators/h1","t":"HTTP/1.1","c":"Knowledge Base \\u203a Load Generators","d":"The load generators behind the HTTP/1.1 profiles: gcannon for most workloads, wrk for URI-rotating static file tests, and zrk for the fixed-rate Latency-1M profile.","x":"gcannon Custom io_uring-based load generator for baseline, JSON, upload, and other tests. wrk Multi-threaded HTTP benchmark tool used for the static file serving test with Lua rotation. zrk Constant-throughput generator that holds a fixed offered rate, with coordinated-omission correction. Drives Latency-1M."},{"u":"load-generators/h1/gcannon","t":"gcannon","c":"Knowledge Base \\u203a Load Generators \\u203a HTTP/1.1","d":"gcannon is HttpArena\'s purpose-built HTTP/1.1 load generator, using Linux io_uring for zero-copy networking at very high request rates.","x":"gcannon is a custom HTTP load generator built specifically for HttpArena. It uses Linux\'s io_uring interface for high-performance, zero-copy networking, ensuring the benchmarking tool itself never becomes the bottleneck. Why a custom load generator? Traditional HTTP benchmarking tools like wrk or ab use epoll or kqueue for I/O multiplexing. While effective, these tools can become the bottleneck when testing extremely fast servers (millions of requests per second). gcannon uses io_uring to push the client-side ceiling higher. Architecture gcannon spawns N worker threads , each managing its own: io_uring ring with IORING_SETUP_SINGLE_ISSUER and IORING_SETUP_DEFER_TASKRUN for minimal kernel overhead Provided buffer ring for zero-copy multishot receives -- the kernel writes directly into pre-registered buffers Connection pool -- each thread manages connections / threads TCP connections I/O flow Connect -- async io_uring_prep_connect for non-blocking TCP setup Send -- pipelined requests are pre-built into a single buffer and sent with io_uring_prep_send Receive -- multishot io_uring_prep_recv_multishot with provided buffers; a single SQE produces multiple CQEs as data arrives Parse -- a streaming HTTP response parser counts completed responses and extracts headers Refill -- as responses complete, new requests are fired to maintain pipeline depth CQE batching gcannon processes completions in batches using io_uring_peek_batch_cqe . With DEFER_TASKRUN , SEND and RECV completions can arrive in the same batch, which requires careful handling to avoid pipeline stalls. Request templates gcannon supports two modes: URL mode When given a plain URL (e.g., http://host:8080/pipeline ), gcannon generates a standard HTTP/1.1 GET request and replicates it N times for pipelining. Raw template mode ( --raw ) When given raw request files (e.g., --raw get.raw,post_cl.raw,post_chunked.raw ), gcannon sends the exact bytes from each file. This enables: Mixed GET/POST workloads Requests with specific headers, body encodings, or query parameters Bit-perfect request reproduction Templates are assigned round-robin to connections, so each connection sends one request type consistently. Dynamic placeholders Raw templates support per-request value substitution via two placeholder types: {RAND:min:max} - replaced with a random number between min and max (inclusive) on every request. Uses a per-connection xorshift64 PRNG with no cross-thread contention. Ideal for distributing reads and writes across a large ID space. GET /items/{RAND:1:100000} HTTP/1.1 Host: localhost:8080 {SEQ:start} - replaced with a globally incrementing counter starting at start . Uses a shared atomic counter across all threads, so every request gets a unique value. Ideal for INSERT operations where each row needs a distinct ID. POST /items HTTP/1.1 Host: localhost:8080 Content-Type: application/json Content-Length: 72 {\\"id\\":{SEQ:100001},\\"name\\":\\"Bench\\",\\"category\\":\\"test\\",\\"price\\":100,\\"qty\\":50} Values are zero-padded to the digit width of the max value, so the substituted buffer is always the same length as the original placeholder. This means Content-Length stays correct for POST/PUT bodies without recalculation. One placeholder per template (the first {RAND: or {SEQ: found). Each template gets its own independent counter/RNG state. A per-connection scratch buffer is used for substitution - the shared template buffer is never modified. Pipelining With -p N , gcannon sends N requests in a single write operation. As responses arrive, it refills the pipeline proportionally -- if 5 responses are received, 5 new requests are queued. This maintains steady pressure without overwhelming the server\'s receive buffer. Latency measurement Latency is measured per-request from the moment the send SQE is prepared (not when the kernel completes the send) to when the corresponding response is fully parsed. This captures the true round-trip time as seen by the application. For pipelined requests, each request in the batch gets its own send timestamp, and latencies are matched FIFO to response completions. Command-line reference Usage: gcannon <url> -c <conns> -t <threads> -d <duration> [-p <pipeline>] [-r <req/conn>] [-R|--raw file1,file2,...] Flag Description Default -c Total connections required -t Worker threads required -d Test duration (e.g., 5s , 30s ) required -p Pipeline depth 1 -r Requests per connection (0 = unlimited) 0 --raw Comma-separated raw request template files --"},{"u":"load-generators/h1/wrk","t":"wrk","c":"Knowledge Base \\u203a Load Generators \\u203a HTTP/1.1","d":"HttpArena uses wrk with a Lua script to rotate across 20 URIs for the static file profile, measuring realistic multi-asset page loads.","x":"wrk is a multi-threaded HTTP benchmarking tool used for the static file serving test profile. Why wrk for static files? The static file test serves 20 files with response sizes ranging from 3 KB to 67 KB (brotli-compressed). At these sizes, gcannon\'s io_uring provided buffer ring path introduces per-completion overhead that limits throughput to ~14 GB/s on loopback. wrk\'s simpler epoll + read() approach achieves ~19 GB/s on the same workload, ensuring the load generator is never the bottleneck. For small-response tests (baseline, JSON, pipelined), gcannon\'s io_uring batched submission is faster than wrk. The tools complement each other. Lua rotation script wrk uses a Lua script ( requests/static-rotate.lua ) to cycle through all 20 static file paths. Each request includes Accept-Encoding: br;q=1, gzip;q=0.8 to test compressed file serving. request = function() counter = counter + 1 local path = paths[((counter - 1) % 20) + 1] return wrk.format(\\"GET\\", path, {[\\"Accept-Encoding\\"] = ae_header}) end Parameters Parameter Value Test profiles static Threads 64 Connections 1,024, 4,096, 6,800 Duration 5s Runs 3 (best taken)"},{"u":"load-generators/h1/zrk","t":"zrk","c":"Knowledge Base \\u203a Load Generators \\u203a HTTP/1.1","d":"HttpArena uses zrk to hold a fixed offered rate for the Latency-1M profile, with coordinated-omission correction and a machine-readable summary.","x":"zrk is a constant-throughput HTTP load generator, used for the Latency-1M profile. Why a third generator gcannon and wrk are both closed-loop: each connection sends its next request as soon as the previous response lands, so the rate finds its own ceiling. That answers \\"how fast can this go\\", which is what almost every profile here asks. Latency-1M asks the opposite question. The rate is pinned at one million requests per second and the measurement is what the server spent to serve it, so the generator has to hold a rate rather than discover one. gcannon has no rate limiter at all, by design. Why zrk and not wrk2 wrk2 is the original constant-throughput generator and the tool zrk is a rewrite of. Three things decided it: Maintenance. wrk2\'s last commit is from September 2019, with over a hundred open issues. zrk is actively developed. Pacing resolution. wrk2 schedules sends on a millisecond timer wheel, which rounds every wait up and adds roughly half a millisecond of the tool\'s own noise to every sample. zrk computes a closed-form nanosecond offset instead. Machine-readable output. --format json emits a single summary object, so the harness parses a document rather than scraping a report. That is what makes rate_ratio available at all, and rate_ratio is the validity gate for the whole profile. Coordinated omission Latency is measured from the time a request should have been sent according to the schedule, not from when it actually went out. A server stall therefore lands in the tail instead of being smoothed away by the client simply sending fewer requests while it recovers. This is why a run that misses the rate reports latencies in the seconds: the client falls behind its schedule and every queued request carries that lag. It is a correct reading of a server that could not keep up, not an artifact. The validity gate rate_ratio is achieved rate over offered rate, and it is published on every row. A run meaningfully below 1.0 did not measure the profile, because the load was not the load, so its CPU figure describes a lighter workload than every other entry\'s. Those runs are flagged rather than ranked as though they were cheap. Version pinning The image pins an exact version and verifies its checksum, unlike gcannon which tracks its default branch. The premise of Latency-1M is that the offered load is identical across entries and across rounds , so a generator drifting underneath it would invalidate comparisons against numbers already published. Bumping the version is a deliberate act that re-baselines the profile. Parameters Parameter Value Test profiles latency-1m Offered rate 1,000,000 req/s, paced Threads 64 Connections 1,024 Duration 20s Runs 3 (best by score)"},{"u":"load-generators/h2","t":"HTTP/2","c":"Knowledge Base \\u203a Load Generators","d":"How HttpArena generates multiplexed HTTP/2 load for the TLS and cleartext (h2c) profiles.","x":"h2load HTTP/2 load generator from nghttp2 with TLS and stream multiplexing support."},{"u":"load-generators/h2/h2load","t":"h2load","c":"Knowledge Base \\u203a Load Generators \\u203a HTTP/2","d":"h2load from the nghttp2 project drives every HTTP/2 profile in HttpArena, with configurable concurrent streams per connection.","x":"h2load is part of the nghttp2 project and is used for the baseline-h2 profile. It supports HTTP/2 multiplexing with configurable concurrent streams per connection. Installation sudo apt install nghttp2-client How it\'s used h2load https://localhost:8443/baseline2?a=1&b=1 -c 256 -m 100 -t 128 -D 5s Flag Description Value -c Number of connections 256 or 1,024 -m Max concurrent streams per connection 100 -t Threads 128 -D Duration 5s Why h2load? gcannon is HTTP/1.1 only (by design - it uses raw request templates and io_uring for maximum throughput). h2load handles TLS negotiation, ALPN, HPACK header compression, and HTTP/2 stream multiplexing natively."},{"u":"load-generators/h3","t":"HTTP/3","c":"Knowledge Base \\u203a Load Generators","d":"How HttpArena generates HTTP/3 traffic over QUIC, and why a custom ngtcp2 build replaced the previous tool.","x":"h2load-h3 nghttp2\'s h2load built with ngtcp2 + nghttp3 + quictls for HTTP/3 (QUIC) load testing."},{"u":"load-generators/h3/h2load-h3","t":"h2load-h3","c":"Knowledge Base \\u203a Load Generators \\u203a HTTP/3","d":"A custom h2load build linked against ngtcp2 that drives HttpArena\'s HTTP/3 profiles over QUIC with ALPN negotiation.","x":"h2load is part of the nghttp2 project. HttpArena uses a custom build that links against ngtcp2 , nghttp3 , and quictls to enable HTTP/3 (QUIC) support. This build is invoked as h2load-h3 and drives both baseline-h3 and static-h3 profiles. Installation The h2load binary shipped by Linux distributions does not include HTTP/3. HttpArena builds its own image: docker build -f docker/h2load-h3.Dockerfile -t h2load-h3:local docker/ For native execution under benchmark.sh , the binary and runtime libraries are extracted from the image to /opt/h2load-h3/ and a wrapper is installed at /usr/local/bin/h2load-h3 that sets LD_LIBRARY_PATH and execs the binary. How it\'s used h2load-h3 --alpn-list=h3 https://localhost:8443/baseline2?a=1&b=1 \\\\ -c 64 -m 64 -t 64 -D 5s For multi-URI static file tests: h2load-h3 --alpn-list=h3 -i requests/static-h2-uris.txt \\\\ -H \\"Accept-Encoding: br;q=1, gzip;q=0.8\\" \\\\ -c 64 -m 64 -t 64 -D 5s Flag Description Value --alpn-list=h3 Negotiate HTTP/3 over QUIC - -c Number of QUIC connections 64 -m Max concurrent streams per connection 64 -t Worker threads 64 -D Duration 5s -i URI list file (multi-URL rotation) - Why h2load-h3 instead of oha? The previous HTTP/3 driver ( oha ) topped out at ~85k req/s and could not saturate any framework - its single-threaded quinn client became the bottleneck before the server. h2load-h3 uses ngtcp2\'s multi-threaded worker model with sendmmsg / recvmmsg syscall batching, reaching ~580k req/s on the same hardware against the same server (Kestrel + msquic). Both ends are CPU-saturated at that point, which makes the result a fair measurement of the framework\'s QUIC stack rather than the load generator\'s. Why HTTP/3 results are still lower than HTTP/2 HTTP/3 carries an inherent CPU cost vs HTTP/2 that loopback benchmarks make particularly visible: Per-packet AEAD encryption with no kernel TLS offload All packet processing in userspace via msquic / ngtcp2 More syscalls per request than TCP\'s buffered read/write paths Loopback at MTU 1500 - required for proper GSO/GRO behavior - gives no help from a real NIC Expect a 4-6\\u00d7 gap between h3 and h2 numbers for the same framework. This reflects the state of the QUIC ecosystem in 2026, not a configuration issue."},{"u":"load-generators/ws","t":"WebSocket","c":"Knowledge Base \\u203a Load Generators","d":"HttpArena drives its WebSocket profiles with gcannon in --ws mode, reusing the same io_uring engine as the HTTP/1.1 tests.","x":"HttpArena drives the echo-ws profile with gcannon in --ws mode . The same io_uring engine documented under HTTP/1.1 \\u2192 gcannon is reused here - worker threads, per-thread provided-buffer rings, multishot receives, per-connection state - with a frame-aware send/recv loop layered on top. Using one tool across transports keeps the client-side ceiling, threading model, and CPU-pinning behavior consistent so differences in the measurement land on the server, not the generator. Handshake Each worker opens TCP connections and issues an HTTP/1.1 upgrade request to the target URL (typically http://localhost:8080/ws ). The server must respond with HTTP/1.1 101 Switching Protocols and the correct Sec-WebSocket-Accept value derived from the client\'s Sec-WebSocket-Key . Connections that fail the handshake are reported as reconnects; the validator ( WebSocket validation ) checks the handshake path separately and catches framework-side bugs before benchmarks run. Echo loop Once upgraded, each connection runs the steady-state loop: Build a masked client-to-server text frame with a short payload Send the frame via io_uring_prep_send Wait for the server to echo it back (matched server-to-client frame) On receipt, increment the per-thread frame counter and immediately send the next frame Pipeline depth is 1 for the echo-ws profile - one message in flight per connection - so the measurement is effectively a back-to-back request/response loop rather than a batched burst. With thousands of concurrent connections each running this loop in parallel, the steady-state throughput reflects the server\'s ability to multiplex WebSocket frames across a large connection count without head-of-line blocking. Both text frames (opcode 0x1 ) and binary frames (opcode 0x2 ) are exercised against the server during validation; benchmark runs use the text shape for simplicity. Framing follows RFC 6455: masked from client to server, unmasked from server to client, FIN bit set on every frame (no fragmented messages in the benchmark path). Command-line usage gcannon http://localhost:8080/ws --ws \\\\ -c <connections> -t <threads> -d <duration> -p 1 Flag Description <url> The WebSocket endpoint served over HTTP/1.1 (uses http:// scheme; the upgrade is implicit) --ws Switches gcannon from HTTP request mode into WebSocket echo mode -c Total concurrent connections (distributed evenly across -t threads) -t Worker threads (each owns an io_uring and a slice of connections; defaults to $THREADS=64 ) -d Test duration - 5s for echo-ws -p Pipeline depth - fixed at 1 for echo-ws (one message in flight per connection) The profile dispatcher ( scripts/lib/tools/gcannon.sh:ws-echo ) wires all of this automatically when you invoke ./scripts/benchmark.sh <framework> echo-ws . Output shape gcannon reports WebSocket results with the same layout as HTTP requests, except the summary line reads \\"frames sent / frames received\\" instead of \\"requests / responses\\": 2400000 frames sent in 5.00s, 2400000 frames received Throughput: 480.00K frames/s WS frames: 2400000 The parser ( gcannon_parse ws-echo ) records frames received as the status_2xx equivalent and divides by the measured duration to produce the headline RPS number shown on the WebSocket leaderboard . One echo round-trip counts as one unit - the frames-received count from the client side, not frames-sent, because the metric is \\"how many echoes the framework completed,\\" not \\"how many messages the benchmarker pushed into the socket.\\" Why not a dedicated WebSocket tool The two common alternatives - wrk2 with a Lua WebSocket plugin, or artillery - either can\'t saturate the server at 64-core scale (GC + per-connection Lua overhead becomes the bottleneck) or produce non-deterministic per-thread CPU pinning that makes cross-framework comparison unreliable. Reusing gcannon means the generator\'s tuning story is the same one already vetted against the HTTP/1.1 profiles, and the operator-side flags ( $GCANNON_CPUS , cpuset pinning, provided buffer ring sizing) compose identically."},{"u":"running-locally","t":"Running Locally","c":"Knowledge Base","d":"Run the full HttpArena benchmark suite on your own machine, from prerequisites through to saving and comparing results.","x":"Run the full HttpArena benchmark suite on your own machine. Scripts Reference What each script in scripts/ does - run, validate, benchmark, compare, and archive. Prerequisites OS support, Docker, and the native vs Docker-image load-generator choices. Setup Clone the repository, build gcannon, and prepare TLS certificates. Manual load testing Start a server yourself and fire the dockerized load generators at it directly. Configuration Default benchmark parameters - threads, duration, runs, and ports. Archiving Rounds Snapshot current results as named rounds and manage archived benchmarks."},{"u":"running-locally/archiving-rounds","t":"Archiving Rounds","c":"Knowledge Base \\u203a Running Locally","d":"Snapshot the current results as a named round so previous benchmark generations stay browsable on the leaderboard.","x":"Snapshot current benchmark results as named rounds that users can browse on the leaderboard. Overview You can archive the current benchmark results as a named snapshot. On the leaderboard, users can switch between archived rounds and the current ongoing results. Creating a snapshot ./scripts/archive.sh create \\"Round 1 - March 2026\\" When you create a snapshot, it bundles all current result data from site/data/results/*.json into a single site/data/rounds/<id>.json file. After re-running scripts/gen_new_leaderboard_data.py , the round selector will appear on the leaderboard page letting users switch between \\"Current\\" and any archived rounds. Listing archived rounds ./scripts/archive.sh list Deleting an archived round ./scripts/archive.sh delete 1"},{"u":"running-locally/configuration","t":"Configuration","c":"Knowledge Base \\u203a Running Locally","d":"Every environment variable the benchmark driver accepts, plus the anatomy of a profile definition in profiles.sh.","x":"Everything the benchmark driver can be told to do via environment variables, plus the shape of a profile definition. For the full flag-by-flag walkthrough see benchmark.sh and benchmark-lite.sh . Run settings Defined in scripts/lib/common.sh . Override by exporting before you run the script, or inline: THREADS=8 ./scripts/benchmark.sh actix baseline . Variable Default Description DURATION 5s Load-test duration per run ( -d / -D passed through to the tool). RUNS 3 Measurement iterations per (profile, connection count). Best wins. THREADS 64 gcannon / wrk worker threads. H2THREADS 64 h2load worker threads (HTTP/2, h2c gRPC). H3THREADS 64 h2load-h3 worker threads (HTTP/3 over QUIC). SKIP_TUNE false When true , skips kernel/hardware tuning (CPU governor, socket limits) that requires sudo . Useful for local development. In benchmark-lite.sh , THREADS defaults to max(nproc / 2, 1) and H2THREADS / H3THREADS mirror $THREADS . Pass --load-threads N to override all three in one shot. Ports Variable Default Description PORT 8080 HTTP/1.1 plaintext (all h1* profiles + echo-ws ); also h2c for gRPC ( unary-grpc - prior-knowledge on the same socket). H2PORT 8443 HTTPS / HTTP/2 over TLS ( baseline-h2 , static-h2 , gateway + production-stack), HTTP/3 over QUIC ( baseline-h3 , static-h3 , gateway-h3 ), and gRPC-TLS ( unary-grpc-tls ). H1TLS_PORT 8081 HTTP/1.1 + TLS, used only by the json-tls profile (ALPN http/1.1 ). H2C_PORT 8082 HTTP/2 cleartext prior-knowledge for the baseline-h2c and json-h2c profiles. Must be a dedicated listener that refuses HTTP/1.1 - the validator checks this explicitly. Every framework Dockerfile reads the same defaults from its env, so you rarely need to change these. Load-generator mode Variable Default Description LOADGEN_DOCKER false When true , every load generator runs from its Docker image instead of the host binary. Builds missing images automatically from docker/*.Dockerfile . Forced true by benchmark-lite.sh . GCANNON_MODE native native or docker . Implied by LOADGEN_DOCKER . GCANNON_CPUS dynamic CPU list the load generators run on. Native mode wraps calls in taskset -c $GCANNON_CPUS ; docker mode passes it via --cpuset-cpus . Defaults to the upper half of available cores on smaller machines to avoid resource contention with the server. Tool binaries and images Each load generator has a pair of variables - native binary name and docker image tag: Native ( $TOOL ) Docker ( $TOOL_IMAGE ) Used for Source GCANNON=gcannon GCANNON_IMAGE=gcannon:latest h1, pipelined, limited-conn, json, json-comp, upload, api-4/16, async-db, echo-ws docker/gcannon.Dockerfile H2LOAD=h2load H2LOAD_IMAGE=h2load:latest baseline-h2, static-h2, unary-grpc, unary-grpc-tls, gateway-64 docker/h2load.Dockerfile (Ubuntu + glibc, not alpine) H2LOAD_H3=h2load-h3 H2LOAD_H3_IMAGE=h2load-h3:local baseline-h3, static-h3 docker/h2load-h3.Dockerfile (quictls + ngtcp2 + nghttp3) WRK=wrk WRK_IMAGE=wrk:local static, json-tls docker/wrk.Dockerfile Postgres sidecar Started automatically when the framework subscribes to async-db , api-4 , api-16 , or gateway-64 . Variable Default Description PG_CONTAINER httparena-postgres Container name. DATABASE_URL postgres://bench:bench@localhost:5432/benchmark Exported into the framework container so the app can connect. The sidecar uses postgres:18 (Debian, glibc) with -c max_connections=256 and is seeded from data/pgdb-seed.sql . Profile definitions Profiles live in scripts/lib/profiles.sh . Format: pipeline | req_per_conn | cpu_limit | connections | endpoint pipeline - gcannon -p value. 1 = sequential, 16 = pipelined. req_per_conn - gcannon -r value. 0 = keep-alive forever; a positive number forces reconnect every N requests (exercises the accept path). cpu_limit - cpuset written to the framework container\'s --cpuset-cpus . Blank = no pinning. connections - comma-separated list; each value becomes a separate iteration. endpoint - dispatch key. Tells endpoint_tool() which load generator to use and gcannon_build_args() (etc.) how to shape the request. benchmark-lite.sh overrides PROFILES and PROFILE_ORDER with a smaller subset and blanks the cpu_limit column; everything else parses identically. Profile \\u2192 tool dispatch From endpoint_tool() in scripts/lib/profiles.sh : Endpoint Tool static , json-tls wrk h2 , static-h2 , h2c , json-h2c , gateway-64 , grpc , grpc-tls , production-stack h2load h3 , static-h3 , gateway-h3 h2load-h3 everything else ( \\"\\" , pipeline , upload , api-4 , api-16 , async-db , crud , json , json-compressed , ws-echo ) gcannon Small-machine overrides The defaults in benchmark.sh assume the reference 64-core benchmark host. On a laptop, three variables usually get you to something reasonable: THREADS=8 H2THREADS=16 H3THREADS=4 ./scripts/benchmark.sh actix baseline If native gcannon / h2load / h2load-h3 aren\'t installed, flip the whole thing to docker mode instead of installing each tool: LOADGEN_DOCKER=true ./scripts/benchmark.sh actix --save Or just use benchmark-lite.sh , which is this combination pre-baked."},{"u":"running-locally/manual-load-testing","t":"Manual load testing","c":"Knowledge Base \\u203a Running Locally","d":"Start a framework container yourself, exercise individual endpoints, and run a load generator outside the benchmark harness.","x":"Sometimes you want to start a framework yourself, poke at individual endpoints, try different flags, or run a load generator outside the benchmark harness altogether. This page is the \\"bring your own server, fire a docker load-gen at it\\" workflow. When to use this Iterating on a framework implementation and you want fast feedback without the full tuning / build pipeline. Trying a different connection count, duration, or request template than any stock profile uses. Debugging a single endpoint in isolation. Running a load generator against a server that isn\'t in frameworks/ at all - anything on localhost works. If you want reproducible numbers that match the leaderboard, use benchmark.sh . This page is for the in-between. Step 1 - start the server Option A: use scripts/run.sh Starts a framework container interactively with the same mounts the benchmark uses. Ctrl+C to stop. ./scripts/run.sh actix This exposes 8080 (HTTP/1.1, h2c gRPC), 8443 (HTTPS / HTTP/2 / HTTP/3 / gRPC-TLS), and 8081 (h1 TLS for json-tls ). If you need postgres for async-db / api-* : docker run -d --name httparena-postgres --network host \\\\ -e POSTGRES_USER=bench -e POSTGRES_PASSWORD=bench -e POSTGRES_DB=benchmark \\\\ -v \\"$(pwd)/data/pgdb-seed.sql:/docker-entrypoint-initdb.d/seed.sql:ro\\" \\\\ postgres:18 -c max_connections=256 Option B: your own binary Any server on localhost:8080 (or wherever) works. The load generators don\'t care what\'s on the other end. Step 2 - build the load-generator images once If you\'ve run benchmark-lite.sh or LOADGEN_DOCKER=true ./scripts/benchmark.sh ... at least once, these already exist. Otherwise build them up front - it\'s a one-off cost: docker build -t gcannon:latest -f docker/gcannon.Dockerfile docker docker build -t h2load:latest -f docker/h2load.Dockerfile docker docker build -t h2load-h3:local -f docker/h2load-h3.Dockerfile docker docker build -t wrk:local -f docker/wrk.Dockerfile docker The slow one is h2load-h3:local (compiles quictls + nghttp3 + ngtcp2 + nghttp2 --enable-http3 from source - 5\\u201310 minutes). Everything else is under a minute. Step 3 - run a load generator Every image ships with the tool as its ENTRYPOINT , so docker run <image> <tool-args> works directly. Always use --network host so the container can reach localhost . A handy alias for brevity. The memlock and seccomp flags are required for gcannon - without them io_uring_queue_init returns Operation not permitted and gcannon exits silently with zero requests. DFLAGS=\'--rm --network host --ulimit nofile=1048576:1048576 --ulimit memlock=-1:-1 --security-opt seccomp=unconfined\' gcannon - HTTP/1.1, WebSocket, --raw templates # Baseline: 512 connections, 64 threads, 5s docker run $DFLAGS gcannon:latest \\\\ http://localhost:8080/baseline11?a=1\\\\&b=1 \\\\ -c 512 -t 64 -d 5s -p 1 # Pipelined (depth 16) docker run $DFLAGS gcannon:latest \\\\ http://localhost:8080/pipeline \\\\ -c 512 -t 64 -d 5s -p 16 # Multi-template raw request rotation (mount repo\'s requests/ dir read-only) docker run $DFLAGS \\\\ -v \\"$(pwd)/requests:/requests:ro\\" \\\\ gcannon:latest http://localhost:8080 \\\\ --raw /requests/get.raw,/requests/json-get.raw,/requests/async-db-get.raw \\\\ -c 1024 -t 64 -d 10s # WebSocket echo docker run $DFLAGS gcannon:latest \\\\ http://localhost:8080/ws --ws \\\\ -c 512 -t 64 -d 5s Useful flags: -r <N> force-reconnect every N requests ( 0 = keep-alive forever), -s <code> expected status, --recv-buf <bytes> receive buffer size, --json machine-readable output. Dynamic placeholders in raw templates Raw request files support {RAND:min:max} and {SEQ:start} placeholders that are substituted per-request at send time. Useful for CRUD benchmarks where each request should target a different database row: # Template with {RAND} - each request reads a random item printf \'GET /crud/{RAND:1:100000} HTTP/1.1\\\\r\\\\nHost: localhost:8080\\\\r\\\\n\\\\r\\\\n\' > /tmp/crud-read.raw # Template with {SEQ} - each request creates a unique item printf \'POST /crud HTTP/1.1\\\\r\\\\nHost: localhost:8080\\\\r\\\\nContent-Type: application/json\\\\r\\\\nContent-Length: 72\\\\r\\\\n\\\\r\\\\n{\\"id\\":{SEQ:100001},\\"name\\":\\"Bench\\",\\"category\\":\\"test\\",\\"price\\":100,\\"qty\\":50}\' > /tmp/crud-create.raw # Mix them - gcannon round-robins across templates docker run $DFLAGS \\\\ -v /tmp:/requests:ro \\\\ gcannon:latest http://localhost:8080 \\\\ --raw /requests/crud-read.raw,/requests/crud-create.raw \\\\ -c 512 -t 64 -d 5s Values are zero-padded to a fixed width so Content-Length stays correct. {RAND} uses per-connection RNG (no contention), {SEQ} uses a global atomic counter (unique across all threads). h2load - HTTP/2 and h2/h2c gRPC unary # HTTP/2 over TLS, 256 connections, 100 streams/conn docker run $DFLAGS h2load:latest \\\\ https://localhost:8443/baseline2?a=1\\\\&b=1 \\\\ -c 256 -m 100 -t 128 -D 5 # gRPC unary over h2c (need a prebuilt request body) docker run $DFLAGS \\\\ -v \\"$(pwd)/requests:/requests:ro\\" \\\\ h2load:latest \\\\ http://localhost:8080/benchmark.BenchmarkService/GetSum \\\\ -d /requests/grpc-sum.bin \\\\ -H \'content-type: application/grpc\' -H \'te: trailers\' \\\\ -c 256 -m 100 -t 128 -D 5 -D takes seconds (not 5s ), -m is the max concurrent streams per connection, -t is worker threads. h2load-h3 - HTTP/3 over QUIC Same binary family as h2load, same flags, plus --alpn-list=h3 : docker run $DFLAGS h2load-h3:local \\\\ --alpn-list=h3 \\\\ https://localhost:8443/baseline2?a=1\\\\&b=1 \\\\ -c 64 -m 64 -t 64 -D 5 If h3 errors out with connect error , bump the host\'s UDP buffer sizes - the benchmark driver sets these to 7.5 MB but a plain shell doesn\'t inherit them: sudo sysctl -w net.core.rmem_max=7500000 net.core.wmem_max=7500000 wrk - static file rotation, json-tls wrk needs the lua scripts from requests/ for multi-URL rotation: docker run $DFLAGS \\\\ -v \\"$(pwd)/requests:/requests:ro\\" \\\\ wrk:local \\\\ -t 64 -c 1024 -d 5s \\\\ -s /requests/static-rotate.lua \\\\ http://localhost:8080 A simple single-URL test (no script, no rotation): docker run $DFLAGS wrk:local \\\\ -t 8 -c 256 -d 5s \\\\ http://localhost:8080/baseline11?a=1\\\\&b=1 CPU pinning (optional) The benchmark script pins the load generator to one half of the CPU topology via --cpuset-cpus to avoid stealing cycles from the framework. For a manual run you can do the same: docker run $DFLAGS --cpuset-cpus=32-63,96-127 gcannon:latest ... On a laptop there\'s no point - the container will get more cores than the server has anyway. Just leave it off. Reusing the benchmark\'s request templates Everything under requests/ is fair game: File What it is requests/get.raw , post_cl.raw , post_chunked.raw Raw HTTP/1.1 requests for --raw mode. requests/json-{1,5,10,...}.raw Different JSON body sizes for the json profile. requests/async-db-{5,10,20,35,50}.raw Different limit=N query variants. requests/upload-{500k,2m,10m,20m}.raw Upload bodies for the upload profile. requests/static-rotate.lua / json-tls-rotate.lua wrk rotation scripts. requests/static-h2-uris.txt URI list for h2load -i (static-h2 / static-h3). requests/benchmark.proto gRPC service definition. requests/grpc-sum.bin Prebuilt GetSum gRPC body for h2load. Mount the directory read-only into any tool: -v \\"$(pwd)/requests:/requests:ro\\" . Parsing the output The benchmark driver\'s parsers live in scripts/lib/tools/*.sh - gcannon_parse , h2load_parse , h2load_h3_parse , wrk_parse . If you want to script on top of a manual run, source one of those modules: source scripts/lib/common.sh source scripts/lib/tools/gcannon.sh out=$(docker run --rm --network host \\\\ --ulimit memlock=-1:-1 --security-opt seccomp=unconfined \\\\ gcannon:latest \\\\ \\"http://localhost:8080/baseline11?a=1&b=1\\" \\\\ -c 512 -t 64 -d 5s -p 1 2>&1) gcannon_parse \\"\\" \\"$out\\" # rps=3401583 # avg_lat=123us # p99_lat=287us # ... When your numbers don\'t match the leaderboard A manual docker run is ~10\\u201320% slower than a native benchmark.sh run on the same hardware because the docker daemon restart, CPU governor flip, loopback MTU change, and kernel socket tuning are all skipped. For relative comparisons between frameworks on your own machine it\'s close enough; for absolute numbers use benchmark.sh ."},{"u":"running-locally/prerequisites","t":"Prerequisites","c":"Knowledge Base \\u203a Running Locally","d":"What to install before running HttpArena locally: Docker, the load generator toolchain, and the host settings the driver expects.","x":"What you need installed before running HttpArena benchmarks locally. Operating system OS Supported Notes Linux (kernel \\u2265 6.1) \\u2705 Yes The only fully-supported target. Required for io_uring (gcannon native), cpupower , the host sysctl knobs the tuner writes, and --network host behaviour Docker containers rely on. Tested primarily on Ubuntu 24.04 / 25.04. macOS \\u274c No Docker Desktop runs in a Linux VM; --network host , host-side tuning, and cgroup cpuset all behave differently. Windows \\u274c No The Windows driver scripts were removed. WSL2 may work for benchmark-lite.sh -style runs but is untested. benchmark.sh will refuse to do anything destructive without root (tuning is best-effort), but gcannon native mode, Docker --cpuset-cpus pinning, and the postgres sidecar all assume Linux semantics. Common requirements (both scripts) Docker Engine \\u2265 24 - framework containers, postgres sidecar, and every load-generator image run under Docker. bash \\u2265 4.4 - the driver uses associative arrays. python3 - for result JSON aggregation ( scripts/rebuild_site_data.py ) and meta.json parsing. curl - readiness probes. git - image builds clone gcannon / nghttp2 source from GitHub. Choosing a script benchmark.sh benchmark-lite.sh Who it\'s for Full runs on a tuned benchmark host Laptops, CI runners, quick local validation Load generators Native binaries by default, docker mode opt-in ( LOADGEN_DOCKER=true ) Always docker, forced CPU pinning Per-profile --cpuset-cpus None (container gets all cores) Threads 64 (h1) / 128 (h2) / 64 (h3) - fixed nproc / 2 , or --load-threads N Profiles All 21 Subset (no api-4/16, json-tls, gateway-64) Host tuning (CPU gov, sysctl, MTU, daemon restart) Yes Yes Root needed For tuning + docker For tuning + docker Pick benchmark-lite.sh if you just want to sanity-check a framework; pick benchmark.sh if you\'re trying to reproduce leaderboard numbers. benchmark-lite.sh requirements Just Docker, bash, python3. Every load-generator image is built from docker/*.Dockerfile on first run (first invocation takes a few minutes while quictls + ngtcp2 compile for h2load-h3 ; cached after that). You do not need gcannon , h2load , h2load-h3 , or wrk installed on the host. You do not need a specific kernel for gcannon - the container already carries liburing 2.9. benchmark.sh requirements (native load-generator mode) For the default (native) mode, install each tool the host binary. If anything is missing, set LOADGEN_DOCKER=true and the script falls back to the same docker images benchmark-lite.sh uses. gcannon - io_uring HTTP/1.1, WebSocket, and --raw multi-template generator. Requires Linux kernel \\u2265 6.1 with io_uring enabled. bash git clone --branch liburing-2.9 https://github.com/axboe/liburing.git cd liburing && ./configure --prefix=/usr && make -j\\"$(nproc)\\" -C src && sudo make install -C src cd .. git clone https://github.com/MDA2AV/gcannon.git cd gcannon && make && sudo cp gcannon /usr/local/bin/ h2load - HTTP/2 and gRPC (unary) generator from the nghttp2 project. bash sudo apt install nghttp2-client h2load-h3 - HTTP/3 over QUIC. No official distro package; the docker image ( docker/h2load-h3.Dockerfile ) builds quictls + nghttp3 + ngtcp2 + nghttp2 --enable-http3 from source. Easiest path: use LOADGEN_DOCKER=true for h3 profiles. wrk - static-file profile. Any recent build works. bash sudo apt install wrk Permission notes Docker commands assume your user is in the docker group, or you\'ll hit permission denied . System tuning ( cpupower frequency-set , sysctl -w , ip link set lo mtu , systemctl restart docker , drop_caches ) uses sudo . Without it the script warns and continues - you still get usable results, just with less-predictable numbers. --cpuset-cpus requires cgroup v2 (default on modern Linux)."},{"u":"running-locally/scripts","t":"Scripts Reference","c":"Knowledge Base \\u203a Running Locally","d":"Every script in scripts/, what it does, and the arguments it takes: benchmark, validate, compare, archive and run.","x":"All scripts live in the scripts/ directory and are meant to be run from the repository root. validate.sh Run the correctness validation suite against a framework\'s endpoints. run.sh Run a framework container interactively for manual testing and endpoint development. benchmark.sh Run benchmarks across test profiles with system tuning and result collection. benchmark-lite.sh Lightweight benchmarks for any machine with Docker - no CPU pinning, containerized load generators. compare.sh Compare benchmark results against the published leaderboard on main. archive.sh Snapshot current results as named rounds and manage archived benchmarks."},{"u":"running-locally/scripts/archive","t":"archive.sh","c":"Knowledge Base \\u203a Running Locally \\u203a Scripts Reference","d":"Snapshot current results as a named round, list archived rounds, or delete one, using scripts/archive.sh.","x":"Manage benchmark result archives (rounds). Snapshots current results as a named round, lists archived rounds, or deletes a round. ./scripts/archive.sh create <name> ./scripts/archive.sh list ./scripts/archive.sh delete <id> Commands create Archives all current benchmark results as a named round. ./scripts/archive.sh create \\"Round 1 - March 2026\\" What it does: Bundles every site/data/results/<framework>.json into a single round file at site/data/rounds/<id>.json , regrouped by <profile>-<conns> so archived rounds keep their existing shape Records hardware info (CPU, cores, RAM, governor), OS, kernel, Docker version, and git commit Reads system info from site/data/current.json if available (written by benchmark.sh --save ) Updates the round index at site/data/rounds/index.json Clears results/ and resets site data files to start a fresh round list Lists all archived rounds with their ID, name, date, and file size. ./scripts/archive.sh list Example output: # 1 Round 1 - March 2026 2026-03-15 (1248KB) # 2 Round 2 - Pre-optimization baseline 2026-03-20 (1305KB) delete Deletes an archived round by ID. ./scripts/archive.sh delete 1 Removes the round\'s data file and its entry from the index."},{"u":"running-locally/scripts/benchmark","t":"benchmark.sh","c":"Knowledge Base \\u203a Running Locally \\u203a Scripts Reference","d":"The full-fidelity driver: runs a framework across every profile it subscribes to, tunes the host, collects metrics and saves results.","x":"scripts/benchmark.sh is the full-fidelity benchmark driver. It runs one framework across every profile it subscribes to, tunes the host, collects metrics, and (with --save ) persists results to results/ and the site data JSONs. The driver itself is small (~320 lines of orchestration) - all of the real work lives in the composable library modules under scripts/lib/ . Synopsis ./scripts/benchmark.sh <framework> [profile] [--save] Argument Description <framework> Name of the framework directory under frameworks/ . Required. [profile] Optional - run only this profile (e.g. baseline , async-db , echo-ws ). If omitted, runs every profile the framework subscribes to. --save Persist results. Without it, you get a dry run - numbers printed, nothing written. --save can appear in any position. There is no other positional argument. What a run does, step by step Cleanup - stops and removes any leftover httparena-* containers. Load-generator images (docker mode only) - builds any missing gcannon , h2load , h2load-h3 , or wrk image from docker/*.Dockerfile . Runs before host tuning on purpose: tuning restarts the Docker daemon, and buildkit\'s DNS takes a few seconds to recover afterwards, long enough to break a git clone inside a build container. Framework build - frameworks/<fw>/build.sh if present, otherwise docker build frameworks/<fw> . Host tuning ( scripts/lib/system.sh ): CPU governor \\u2192 performance via cpupower (falls back to writing /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor ). net.core.somaxconn=65535 , tcp_max_syn_backlog=65535 , netdev_max_backlog=65535 , rmem_max=wmem_max=7500000 (QUIC). ip_local_port_range=1024 65535 , and ip_local_reserved_ports=5432,6379,8080,8081,8082,8443,9090 so the widened range cannot hand a service\'s own port to an outbound socket. See Kernel tuning . ip link set lo mtu 1500 - realistic Ethernet MTU, not the kernel\'s default 65536. systemctl restart docker - guarantees every subsequent container starts from a fresh daemon state. echo 3 > /proc/sys/vm/drop_caches . Postgres sidecar - started if the framework subscribes to any of async-db , api-4 , api-16 , crud , gateway-64 , gateway-h3 , production-stack . Uses postgres:18 (Debian, glibc), tmpfs-backed, seeded from data/pgdb-seed.sql , max_connections=256 , host network. Profile loop - for each subscribed profile \\u00d7 each connection count: Starts the framework container (or docker compose up for gateway profiles). Waits up to 30s for the right endpoint to respond. Builds the tool-specific argument vector. Runs $RUNS measurement iterations (default 3), keeps the best by rps. Each iteration: starts docker stats polling, runs the load generator ( timeout 45s ), stops polling, parses output. Raw load-generator output from every run is written to site/static/logs/<profile>/<conns>/<framework>.<tool>.run<N>.txt - useful when a parser misbehaves. Save ( --save only) - writes results/<profile>/<conns>/<framework>.json + framework docker logs to site/static/logs/<profile>/<conns>/<framework>.log . Restore - trap runs framework_stop , gateway_down , postgres_stop , then restores the original CPU governor and loopback MTU. Rebuild site data ( --save only) - re-runs scripts/rebuild_site_data.py to regenerate site/data/results/<framework>.json and site/data/frameworks.json . Flags Flag Description --save Persist result JSONs and rebuild site data. Default is dry run. Everything else is controlled through environment variables (see below). Environment variables Set via VAR=value ./scripts/benchmark.sh ... or export VAR=value . Run settings Variable Default Description DURATION 5s -d / -D value passed to each load generator. RUNS 3 Measurement iterations per (profile, conns). Best result wins. THREADS 64 Load-generator threads for gcannon, wrk, and the default path. H2THREADS 64 h2load worker threads (h2, h2c gRPC). H3THREADS 64 h2load-h3 worker threads (HTTP/3 over QUIC). Ports Variable Default Description PORT 8080 HTTP/1.1 plaintext (all h1* profiles + echo-ws ); also h2c for gRPC ( unary-grpc ). H2PORT 8443 HTTPS / HTTP/2 TLS ( baseline-h2 , static-h2 , gateway + production-stack), HTTP/3 QUIC ( baseline-h3 , static-h3 , gateway-h3 ), gRPC-TLS ( unary-grpc-tls ). H1TLS_PORT 8081 HTTP/1.1 + TLS - only used by the json-tls profile. H2C_PORT 8082 HTTP/2 cleartext prior-knowledge for baseline-h2c and json-h2c . Must refuse HTTP/1.1 - the validator checks this. Load generator selection Switch every load generator from native binary to the pre-built Docker image in one env var: LOADGEN_DOCKER=true ./scripts/benchmark.sh aspnet-minimal Variable Default Description LOADGEN_DOCKER false When true , every load generator runs from its Docker image instead of the host binary. Builds missing images automatically. GCANNON_MODE native native or docker . LOADGEN_DOCKER=true sets this to docker for you. GCANNON_CPUS 32-63,96-127 Cores the load generators are pinned to (via taskset in native mode or --cpuset-cpus in docker mode). Framework containers use the other half of the CPU topology. Tool binaries + images Variable Default Used for GCANNON gcannon Native binary - baseline, pipelined, limited-conn, json, json-comp, upload, api-4/16, async-db, crud, echo-ws. GCANNON_IMAGE gcannon:latest Docker image when LOADGEN_DOCKER=true . H2LOAD h2load Native binary - baseline-h2, static-h2, baseline-h2c, json-h2c, unary-grpc, unary-grpc-tls, gateway-64, production-stack. H2LOAD_IMAGE h2load:latest Docker image (Ubuntu 24.04 + glibc build; do not use the alpine/musl image - it\'s 20\\u201340% slower). H2LOAD_H3 h2load-h3 Native binary - baseline-h3, static-h3, gateway-h3. H2LOAD_H3_IMAGE h2load-h3:local Docker image with quictls + nghttp3 + ngtcp2 + nghttp2 --enable-http3 built from source. WRK wrk Native binary - static, json-tls. WRK_IMAGE wrk:local Docker image. Postgres sidecar Variable Default Description PG_CONTAINER httparena-postgres Name of the sidecar container. DATABASE_URL postgres://bench:bench@localhost:5432/benchmark Passed to framework containers for async-db , crud , api-4 , api-16 , gateway-64 , gateway-h3 , production-stack . Profiles Each profile is one line in scripts/lib/profiles.sh with the format: pipeline | req_per_conn | cpu_limit | connections | endpoint Profile Pipeline Req/conn CPU pinning Connections Tool Endpoint baseline 1 \\u221e 0-31,64-95 512, 4096 gcannon /baseline11 pipelined 16 \\u221e 0-31,64-95 512, 4096 gcannon /pipeline limited-conn 1 10 0-31,64-95 512, 4096 gcannon /baseline11 (reconnect every 10 req) json 1 \\u221e 0-31,64-95 4096 gcannon /json/{1..50} - 7 body sizes json-comp 1 \\u221e 0-31,64-95 512, 4096, 16384 gcannon /json/{count} + Accept-Encoding: gzip, br json-tls 1 \\u221e 0-31,64-95 4096 wrk /json/{count} over TLS on H1TLS_PORT upload 1 \\u221e 0-31,64-95 32, 256 gcannon /upload - 500K / 2M / 10M / 20M bodies, -r 5 api-4 1 5 0-3 256 gcannon 8-template mix (baseline / json / async-db) api-16 1 5 0-7,64-71 1024 gcannon 8-template mix static 1 200 0-31,64-95 1024, 4096, 6800 wrk 20 files via static-rotate.lua async-db 1 \\u221e 0-31,64-95 1024 gcannon 5 limit variants (5/10/20/35/50), -r 25 baseline-h2 1 \\u221e 0-31,64-95 256, 1024 h2load /baseline2 on H2PORT TLS static-h2 1 \\u221e 0-31,64-95 256, 1024 h2load /static/* on H2PORT TLS baseline-h3 1 \\u221e 0-31,64-95 64 h2load-h3 /baseline2 on H2PORT QUIC static-h3 1 \\u221e 0-31,64-95 64 h2load-h3 /static/* on H2PORT QUIC unary-grpc 1 \\u221e 0-31,64-95 256, 1024 h2load benchmark.BenchmarkService/GetSum h2c unary-grpc-tls 1 \\u221e 0-31,64-95 256, 1024 h2load same, TLS gateway-64 1 \\u221e 0-31,64-95 256, 1024 h2load 20-URI mix behind nginx via docker compose echo-ws 1 \\u221e 0-31,64-95 512, 4096, 16384 gcannon --ws /ws Subscription: a framework only runs profiles listed in its meta.json tests array. Profiles it\'s not subscribed to are silently skipped. Examples # Dry run - everything the framework subscribes to, nothing saved ./scripts/benchmark.sh actix # Save a full run ./scripts/benchmark.sh actix --save # One profile only ./scripts/benchmark.sh actix baseline --save # Run with every load generator in docker (useful if native gcannon / h2load-h3 aren\'t installed) LOADGEN_DOCKER=true ./scripts/benchmark.sh actix --save # Short runs for iterating on a framework DURATION=2s RUNS=1 ./scripts/benchmark.sh actix baseline # Override load-gen threading for a small machine THREADS=8 H2THREADS=16 H3THREADS=8 ./scripts/benchmark.sh actix baseline Output layout results/<profile>/<conns>/<framework>.json # metrics (RPS, p99, CPU, mem, status buckets) site/static/logs/<profile>/<conns>/<framework>.log # docker logs of the framework container site/static/logs/<profile>/<conns>/<framework>.<tool>.runN.txt # raw load-generator stdout per iteration site/data/results/<framework>.json # one file per framework, keyed by <profile>-<conns> Each framework owns exactly one results file, so two pull requests benchmarking different frameworks never write the same file and never conflict. The filename is the display_name lowercased with anything outside [A-Za-z0-9._-] replaced by - , so aspnet-minimal + nginx is stored as aspnet-minimal-nginx.json . site/data/frameworks.json # framework metadata site/data/current.json # host/OS/commit snapshot"},{"u":"running-locally/scripts/benchmark-lite","t":"benchmark-lite.sh","c":"Knowledge Base \\u203a Running Locally \\u203a Scripts Reference","d":"The laptop-friendly benchmark driver: Docker-only load generators, no CPU pinning, reduced thread counts and a smaller profile subset.","x":"scripts/benchmark-lite.sh is the local-dev variant of benchmark.sh . Same module structure, same metrics, same output layout - but with defaults tuned for \\"laptop with Docker and nothing else installed\\". What\'s different benchmark.sh benchmark-lite.sh Default load generators Native binaries Always docker (forced - no env override) CPU pinning Per-profile --cpuset-cpus None - all containers see every core THREADS default 64 nproc / 2 H2THREADS / H3THREADS default 64 / 64 Same as THREADS Profile set 24 profiles 15 - skips api-4 , api-16 , json-tls , crud , baseline-h2c , json-h2c , gateway-64 , gateway-h3 , production-stack Connection counts Varies (512, 1024, 4096, 16384, \\u2026) One per profile (mostly 512; upload 128; h3 64) Framework selection One framework, always Optional - runs every enabled framework if omitted Everything that isn\'t listed stays identical: --save behavior, host tuning, result layout, postgres sidecar for async-db , gcannon_parse version fallbacks, etc. Synopsis ./scripts/benchmark-lite.sh [framework] [profile] [--save] [--load-threads N] Argument Description [framework] Framework directory name. If omitted, every framework with \\"enabled\\": true in its meta.json is run in sequence. [profile] Optional - restrict to a single profile. --save Persist result JSONs + rebuild site data. --load-threads N Shortcut for THREADS=N H2THREADS=N H3THREADS=N for a specific run. Arguments can appear in any order; flags and positionals are separated during parsing. First run First time you invoke it (or after a docker rmi ), the script builds every load-generator image it needs from docker/*.Dockerfile : gcannon:latest - clones github.com/MDA2AV/gcannon main, pulls liburing-2.9 , compiles. h2load:latest - Ubuntu 24.04 + apt install nghttp2-client (glibc build, not musl). h2load-h3:local - Ubuntu 24.04 + builds quictls + nghttp3 + ngtcp2 + nghttp2 --enable-http3 from source. Takes 5\\u201310 minutes the first time. wrk:local - Ubuntu 24.04 + wrk source build. All images are built before the host tuning step, because system_tune() restarts the Docker daemon and buildkit DNS takes a few seconds to recover - long enough to break git clone inside a build container. To force a rebuild (e.g. to pick up a new gcannon commit): docker rmi gcannon:latest ./scripts/benchmark-lite.sh actix baseline Environment variables Everything in benchmark.sh \\u2192 Environment variables applies, with these lite-specific defaults / overrides: Variable Default in lite Notes LOADGEN_DOCKER true (forced, non-overridable) export at the top of the script. GCANNON_MODE docker (forced) Same. GCANNON_CPUS 0-$(nproc-1) Effectively \\"all cores\\" - the --cpuset-cpus value covers the whole CPU. THREADS $(( $(nproc) / 2 )) (min 1) Half the cores, leaving room for the framework container. H2THREADS Same as THREADS H3THREADS Same as THREADS Profile set Profile Pipeline Req/conn Connections Tool Endpoint baseline 1 \\u221e 512 gcannon /baseline11 pipelined 16 \\u221e 512 gcannon /pipeline limited-conn 1 10 512 gcannon /baseline11 json 1 \\u221e 512 gcannon /json/{count} json-comp 1 \\u221e 512 gcannon /json/{count} + compression upload 1 \\u221e 128 gcannon /upload static 1 10 512 wrk /static/* async-db 1 \\u221e 512 gcannon /async-db?limit=N baseline-h2 1 \\u221e 512 h2load /baseline2 (TLS) static-h2 1 \\u221e 512 h2load /static/* (TLS) baseline-h3 1 \\u221e 64 h2load-h3 /baseline2 (QUIC) static-h3 1 \\u221e 64 h2load-h3 /static/* (QUIC) unary-grpc 1 \\u221e 512 h2load GetSum h2c unary-grpc-tls 1 \\u221e 512 h2load GetSum TLS echo-ws 1 \\u221e 512 gcannon --ws /ws Requirements The only hard requirement is Docker Engine . Everything else - gcannon, h2load, h2load-h3, wrk - is built automatically inside containers. You don\'t need io_uring on the host kernel (the gcannon container carries its own liburing 2.9 ), you don\'t need nghttp2-client installed, and you don\'t need a Rust/Go toolchain. Host tuning (CPU governor, sysctl, docker daemon restart, MTU, page-cache drop) is still best-effort - it uses sudo where needed and warns + continues if you don\'t have it. Numbers without tuning are noisier but still usable for relative comparisons. Examples # Every enabled framework, every subset profile (dry run) ./scripts/benchmark-lite.sh # One framework, one profile ./scripts/benchmark-lite.sh actix baseline # Persist results and rebuild site data ./scripts/benchmark-lite.sh actix --save # 4-thread load generators for a low-core machine ./scripts/benchmark-lite.sh --load-threads 4 actix baseline # Shorter iterations while debugging a framework DURATION=2s RUNS=1 ./scripts/benchmark-lite.sh actix baseline Lite runs are great for CI-style smoke tests and for comparing frameworks relative to each other on your own hardware. Absolute numbers will not match the published leaderboard - that\'s produced on a dedicated 128-core host with CPU isolation and native load generators."},{"u":"running-locally/scripts/compare","t":"compare.sh","c":"Knowledge Base \\u203a Running Locally \\u203a Scripts Reference","d":"Diff a framework\'s local results against the published leaderboard data and emit a Markdown table of deltas for a pull request.","x":"Compare a framework\'s benchmark results against the published leaderboard data on the main branch. Outputs a Markdown table with deltas, suitable for PR comments. ./scripts/compare.sh <framework> [profile] [--compare <framework>] Options Parameter Description <framework> Name of the framework to compare [profile] Optional - compare only this test profile --compare <framework> Optional - use another framework\'s published results as the baseline instead of this framework\'s own What it does Reads new results from results/<profile>/<connections>/<framework>.json Reads published leaderboard data from site/data/results/<framework>.json Matches the framework by its display_name from meta.json Outputs a Markdown table for each profile with columns per connection count Comparing against a different framework By default the baseline is the framework\'s own published results, which answers \\"did my change help?\\" . When you are developing a tuned or successor entry, the more useful question is often \\"how does this compare to the entry it derives from?\\" - --compare swaps the baseline: ./scripts/compare.sh genhttp-11 --compare genhttp-11-kestrel Every delta is then measured against genhttp-11-kestrel \'s published results, and the output says so explicitly so the numbers cannot be mistaken for a regression against the framework\'s own history. Profiles the comparison framework does not run show n/a rather than NEW . The same flag is available from a pull request: /benchmark -f genhttp-11 --compare genhttp-11-kestrel Metrics compared For each profile and connection count, the table shows: Metric Direction Description RPS Higher is better Requests per second p99 Lower is better 99th percentile latency CPU Lower is better CPU utilization percentage Memory Lower is better Memory usage Each value includes a percentage delta against the main branch. New frameworks with no prior data show NEW instead of a delta. Example output ### baseline | Metric | 512c | | 4096c | | |--------|------|--|-------|--| | **RPS** | 1.30M | +2.1% | 1.37M | -0.5% | | **p99** | 2.00ms | ~0% | 33.80ms | +1.2% | | **CPU** | 6530% | ~0% | 6274% | -0.3% | | **Memory** | 408MB | ~0% | 922MB | +0.8% | CI usage The benchmark-pr.yml workflow calls this script automatically after benchmarking a PR branch, and posts the comparison table as a PR comment. Adding --compare <framework> to the /benchmark command forwards it to this script, and the comment header records which framework the deltas are against."},{"u":"running-locally/scripts/run","t":"run.sh","c":"Knowledge Base \\u203a Running Locally \\u203a Scripts Reference","d":"Build and start a single framework container with all data volumes and a Postgres sidecar attached, for hands-on testing.","x":"Run a framework\'s Docker container interactively for manual testing. Builds the image, starts a Postgres sidecar, mounts all data volumes, and streams container logs until you press Ctrl+C. ./scripts/run.sh <framework> What it does Builds the Docker image for the framework (or runs build.sh if one exists) Starts a Postgres sidecar container with the seeded benchmark database Mounts all data files unconditionally - datasets, static files, TLS certs Sets DATABASE_URL and DATABASE_MAX_CONN environment variables Runs the container attached so logs stream to your terminal Cleans up all containers on exit (Ctrl+C or script termination) Options Parameter Description <framework> Name of the framework directory under frameworks/ PORT env var Override HTTP port (default: 8080) H2PORT env var Override HTTPS/H2 port (default: 8443) Networking Uses --network host so the container binds directly to the host\'s network interfaces. No port mapping is needed - the framework listens on ports 8080 and 8443 directly. Example ./scripts/run.sh express # In another terminal: curl http://localhost:8080/baseline11?a=1&b=2 curl http://localhost:8080/json/5?m=3 curl http://localhost:8080/async-db?min=10&max=50&limit=20"},{"u":"running-locally/scripts/validate","t":"validate.sh","c":"Knowledge Base \\u203a Running Locally \\u203a Scripts Reference","d":"Run the correctness suite against a framework: builds the image, starts the container and sidecars, and checks every subscribed endpoint.","x":"Run the validation suite against a framework. Builds the Docker image, starts the container (and Postgres sidecar if needed), and runs correctness checks for every test profile listed in the framework\'s meta.json . ./scripts/validate.sh <framework> What it does Reads the framework\'s meta.json to determine which tests are subscribed Builds the Docker image Mounts only the data volumes required by the subscribed tests Starts a Postgres sidecar when async-db , api-4 , or api-16 tests are enabled Waits for the server to start (up to 30 seconds) Runs all validation checks for the subscribed tests Prints a pass/fail summary and exits with code 1 if any check failed Validation coverage Each subscribed test triggers its corresponding validation checks. Workload profiles ( api-4 , api-16 ) automatically trigger validation for all endpoints they use, even if those individual tests are not in meta.json . On failure, each check prints a link to the relevant validation documentation page for reference. Anti-cheat checks Several validations use randomized inputs to detect hardcoded responses: Baseline : random query parameters and POST bodies Upload : random binary payload with byte count verification Database / Async DB : empty price range that must return zero results Example output === Validating: express === [info] Subscribed tests: baseline json upload [build] Building Docker image... [ready] Server is up [test] baseline endpoints PASS [GET /baseline11?a=13&b=42] PASS [POST /baseline11?a=13&b=42 body=20] ... [test] json endpoint PASS [GET /json/{count}?m=X] (4 counts \\u00d7 multipliers + full item schema verified) PASS [GET /json Content-Type] (Content-Type: application/json) ... === Results: 15 passed, 0 failed ==="},{"u":"running-locally/setup","t":"Setup","c":"Knowledge Base \\u203a Running Locally","d":"Clone HttpArena and install the native load generators, or take the Docker-only path if you would rather not build them.","x":"Clone the repo and (optionally) install native load generators. If you\'d rather skip the native install, jump straight to the Docker-only path . Clone the repository git clone https://github.com/MDA2AV/HttpArena.git cd HttpArena Docker-only path The fast road: install nothing, let the scripts build everything. ./scripts/benchmark-lite.sh actix baseline First invocation builds gcannon:latest , h2load:latest , h2load-h3:local , and wrk:local from docker/*.Dockerfile . h2load-h3 is the slow one - expect a few minutes while quictls + ngtcp2 compile. Cached after that. To use the same docker images with the full benchmark.sh driver (all profiles, CPU pinning, etc.) set LOADGEN_DOCKER=true : LOADGEN_DOCKER=true ./scripts/benchmark.sh actix --save Native load generators Install these on the host only if you want benchmark.sh to run in its default native mode. You can pick and choose - every tool has a docker fallback, so missing one isn\'t fatal as long as you don\'t run the profiles that need it (or you flip on LOADGEN_DOCKER=true ). gcannon - h1 / pipelined / upload / api-4/16 / async-db / ws-echo Requires Linux kernel \\u2265 6.1 ( io_uring ). Build liburing 2.9 first to match the production binary. git clone --branch liburing-2.9 https://github.com/axboe/liburing.git cd liburing && ./configure --prefix=/usr && make -j\\"$(nproc)\\" -C src && sudo make install -C src && cd .. git clone https://github.com/MDA2AV/gcannon.git cd gcannon && make && sudo cp gcannon /usr/local/bin/ If you\'d rather not install system-wide, export GCANNON=/path/to/gcannon/gcannon before running the script. h2load - baseline-h2 / static-h2 / unary-grpc / gateway-64 Distro package - the Ubuntu 24.04 glibc build gives ~20\\u201340% more throughput than the alpine/musl one. sudo apt install nghttp2-client h2load-h3 - baseline-h3 / static-h3 No distro package exists. Either build it yourself from docker/h2load-h3.Dockerfile \'s recipe (quictls \\u2192 nghttp3 \\u2192 ngtcp2 \\u2192 nghttp2 --enable-http3 ) or use LOADGEN_DOCKER=true and let the docker image handle it. wrk - static / json-tls sudo apt install wrk Permission check Confirm your user can talk to Docker without sudo: docker ps If that prints a permission error, either add your user to the docker group ( sudo usermod -aG docker $USER , log out, log back in) or run the scripts as root. Host tuning (CPU governor, sysctl, docker daemon restart, MTU, page cache drop) does use sudo . Without it the scripts warn and continue - you\'ll still get usable results, just with noisier numbers."},{"u":"scoring","t":"Scoring","c":"Knowledge Base","d":"How HttpArena turns per-profile throughput into the composite score that ranks frameworks across the whole test suite.","x":"How HttpArena computes the composite score that ranks frameworks across all test profiles. Composite Score Sum of per-profile normalized scores (0\\u2013100 each) across all scored profiles, with an optional memory-efficiency bonus. Completeness Routing, middleware, request and response - each one a framework leaves to you takes 2.5% off its composite. Achievements Gold, silver and bronze for a top three place in a family composite, in a language and on a single test profile."},{"u":"scoring/achievements","t":"Achievements","c":"Knowledge Base \\u203a Scoring","d":"Medals on the leaderboard: top three of a family composite, of a language and of a single test profile, always inside the entry\'s own league.","x":"An achievement is a medal for a top three place: gold for first, silver for second, bronze for third. Medals are shown next to the name on the composite board, in the panel that opens when you click a row, and in full on every framework page. Nothing is scored again to award them. A medal is a place in a field the site already publishes, so a medal, a rank badge and the table you are looking at always say the same thing. The three awards Award Field Family composite The composite score of one family: HTTP/1.1, HTTP/2, HTTP/3, Gateway, gRPC, WebSocket Within its language The same composite, counting only entries written in the same language Test profile One profile, on the average RPS over its scored connection counts The field a medal is taken in Every field is the entry\'s own league. Frameworks, engines and reverse proxies are three separate competitions, and a framework never places against an engine. A profile only awards a medal to an entry that is scored on it. Reference-only profiles, Pipelined and Fortunes, award nothing to frameworks, and an engine holds no medal on a profile outside the engine set. Tuned entries follow the badge rule: a standard entry is placed in the field without tuned entries, a tuned entry in the field with them. It is always the smallest published field the entry belongs to. When a place is not a medal A field needs at least three entries, and somebody has to be beaten. So gold and silver need a field of three, bronze needs four, and last place is never a medal. The field size is written next to every medal, 1 of 71 , so you can always see what was won. Medals are not filtered by what you are looking at. Changing the league filter or the language filter changes who is listed, never who holds a medal."},{"u":"scoring/completeness","t":"Completeness","c":"Knowledge Base \\u203a Scoring","d":"The completeness factor: routing, middleware, request and response. Each one a framework leaves to you costs 2.5% of its composite score.","x":"Throughput is only half of what a framework is worth. A server that hands you nothing but a request callback will usually beat one that routes, hands you a parsed body and builds the response for you - and it should, because it is doing less. Ranking on speed alone quietly rewards giving the developer less. The completeness factor is the counterweight. It asks four yes/no questions about the path from an arriving request to a finished response, and each one an entry answers no takes 2.5% off its composite score : all four done \\u2192 \\u00d71.000 one missing \\u2192 \\u00d70.975 two missing \\u2192 \\u00d70.950 three missing \\u2192 \\u00d70.925 all four missing \\u2192 \\u00d70.900 There is no bonus half. Doing the work a framework exists to do is the baseline, not a credit - the factor is there so a bare request callback cannot out-rank a real framework on throughput it only has because it does less. The best any entry can do is lose nothing. The four Axis Counts as done when Routing The framework matches method and path for you, with path parameters - you register handlers, you do not branch on req.url yourself. Middleware There is a composable pipeline around the handler: ordered, able to run before and after it, scoped to the app or to a route, able to short-circuit with a response. Request The request arrives already made - path parameters, query parameters and headers read straight off it, and a body you can take buffered or as a stream , parsed as JSON or a form, without assembling it from raw chunks. Response The response is declared rather than assembled - status, headers and body together in one call, JSON serialized for you, a file or a stream usable as a body. Each axis is all-or-nothing. A server that gives you path parameters and a query string but makes you collect the body out of onData callbacks has not done the Request axis: the handler still cannot ask for what it needs and get it. Nothing outside that path is graded: not templating, ORM or database integration, DI containers, background jobs, CLI tooling, ecosystem size, popularity or documentation. Those matter when you choose a framework, but they are not the surface this benchmark exercises, and a server that ships none of them can still hand you a first-class request. Declaring it Framework entries declare the axes they do not do in meta.json . An axis you leave out reads as done, so a single false is the whole declaration for an entry that only lacks one: \\"completeness\\": { \\"middleware\\": false } An entry that omits the field entirely is unassessed and scores \\u00d71.00 - unassessed is not the same as missing everything. Propose the values in your PR with a sentence of justification; they may be adjusted on review, and they mean the same thing in every language. Two rules decide the values: Grade what the entry uses, not what the framework ships. A benchmark implementation that bypasses its framework\'s router to hand-roll a faster path has no routing. This is the same principle as Standard mode - it just becomes a declaration instead of a review argument. Grade the surface you write against. A server that embeds a fast C++ core but exposes only onData / onAborted for bodies is graded on the callbacks, not the core. Middleware is the one axis read off the surface rather than the run. A benchmark has no cross-cutting concern to install, so no entry would ever score it if it were read off the handler. It counts as done when the server the entry is written against gives the author a pipeline to put one in - not when this particular handler happens to use it. The other three axes are exercised by every entry on every request, so they are graded on the code. Only framework tiers are graded. Engine and infrastructure entries implement none of the four - a raw-socket server has no router, a reverse proxy has no handler to run middleware around - and that is exactly why grading them is pointless rather than generous. Every one of them would take the same \\u00d70.90, so nothing about their order would change, and they are never ranked against frameworks in the first place: each league is normalized against itself. Their factor is fixed at \\u00d71.00 and their rows say nothing about completeness. Not on the WebSocket and gRPC boards The four axes are the path from an arriving HTTP request to a finished response. Two families are not measuring that path, so the factor does not apply on them at all: WebSocket. There is no route to match, no body to hand over and no response to build. The handshake happened once; everything the profile measures after it is frames on an open socket. gRPC. A call has all four - it is dispatched, the request arrives deserialized, the response is serialized back - but the stub generated from the .proto does them, not the framework. Grading the framework for them would be crediting protoc. On those two boards every entry scores \\u00d71.00, including the ones that also serve HTTP and carry a real factor on the HTTP/1.1, HTTP/2, HTTP/3 and Gateway boards. It is the board that is exempt, not the entry. An entry that runs only WebSocket or gRPC profiles does not declare the field at all - there is no board on which it would ever be applied, and a grade nothing reads is a claim about an HTTP path the entry does not have. Why a multiplier on the total Completeness is a property of the entry, not of a workload. It does not change per profile the way throughput or memory does, so it is applied once, to the finished sum, rather than folded into each profile\'s 0-100 score. It lands after the memory-efficiency bonus, on whatever total the current toggles produce. It is also the only part of the composite that is not read off a benchmark, so the board never hides it. A graded entry shows its adjusted score with the points it cost beside it: 416 (-22) scored 438 on throughput, no routing and no middleware took 22 off Only rows that lost something carry a figure. An entry that does all four shows its score alone, and so does one that has not been assessed - most of the field loses nothing, so a (0) on all of them would bury the rows that did. Hovering gives the full arithmetic either way: the raw sum, the multiplier, which axes were deducted, or that the entry has not been assessed yet. That is where the difference between a checked clean sheet and an unassessed entry is carried. What it does not do The factor scales a score; it does not gate a tier. An entry that does none of the four is still a framework entry if it implements HTTP itself, and still ranks in the framework pool - it just needs a raw composite about 11% higher than a complete framework to finish level with it (\\u00d71.00 \\u00f7 \\u00d70.90)."},{"u":"scoring/composite-score","t":"Composite Score","c":"Knowledge Base \\u203a Scoring","d":"The composite score explained: per-profile normalization, the sum across scored profiles, the compression adjustment and the memory bonus.","x":"The composite score combines results from multiple test profiles into a single number that reflects overall framework performance. Each profile is normalized to a 0\\u20131,000 scale, then summed across all scored profiles. An optional memory toggle adds a 0\\u2013500 bonus per profile for memory-efficient frameworks on top of their raw-throughput score. How it works Step 1: Average RPS per profile For each framework and profile, compute the average RPS across all connection counts. This rewards frameworks that scale well across concurrency levels rather than just their peak. Step 2: Normalize per profile For each profile, normalize against the best-performing framework: rpsScore = (framework_avg_rps / best_avg_rps) \\u00d7 1000 This produces a 0\\u20131,000 value where the top framework scores 1,000. The scale is 1,000 rather than 100 for resolution, not for meaning. Nothing about the ranking depends on it: every score moves by the same factor. But summing two dozen profiles that each round to a whole number out of 100 put clusters of entries on identical totals, and the extra digit separates them at no cost. Exception: JSON Compressed. The json-comp profile applies a compression-ratio gain before normalization so frameworks that ship smaller response bodies are rewarded directly. Instead of averaging raw rps, avgRps is first scaled by (minBpr / myBpr)\\u00b2 where myBpr = avgBw / avgRps and minBpr is the smallest bytes-per-response across the field. Doubling the response size quarters the score. See the JSON Compressed implementation for the full formula and rationale. Normalized against the whole field, not the current view. The 1,000-point reference for each profile is the best result in the full field, regardless of what the leaderboard is currently filtering to. Filtering by language, hiding tuned entries or searching for a name changes which rows are displayed ; it does not change any score. Without this, filtering to a single language re-based every profile on the best entry of that language, and because each profile\'s leader moved by a different factor, the summed ranking could reorder, showing one framework above another only while the filter was applied. Framework types remain separate: engine entries are scored on their own subset of profiles, so an engine result never sets the reference for a framework entry. The Rescale to selection toggle opts back into the other behaviour, normalizing against only the frameworks currently shown. That is the more useful view when the question is how a subset compares against itself, for example ranking the Ruby entries against each other rather than against the field. Exception: Latency-1M. That profile pins its request rate, so every entry that holds the rate delivers the same one and normalizing it on rps would score all of them 1,000. It contributes its own score instead, a 0\\u2013100 figure built from CPU and both latency tails, multiplied by 10 onto this scale. One consequence is worth naming: because that score is not rebased on the field leader, the best entry on Latency-1M contributes about 970 rather than a full 1,000. No single entry is simultaneously cheapest and best on both tails, so nobody collects the whole column. That is deliberate, and it is the same reason the profile\'s own score is not rescaled. Step 3: Sum across scored profiles The final composite score is the sum of per-profile scores across all scored profiles: composite = sum(scored_profile_scores) Summing instead of averaging means the composite scales with the number of scored profiles: a framework that places well in many profiles separates cleanly from one that only wins a single profile. A perfect-across-the-board framework earns 1,000 points per profile, so with the current 21 scored profiles for framework (flagship and emerging) entries the raw-throughput ceiling is 21,000, rising to 31,500 when the memory-efficiency toggle is on (each profile adds up to 500 more points). Engine entries are scored on a smaller subset and have a correspondingly lower ceiling. Frameworks that don\'t participate in a scored profile receive 0 for that profile, which lowers their composite by the full 1,000-point ceiling of that profile. The finished sum is then cut by the entry\'s completeness factor : 2.5% for each of routing, middleware, the request it hands you and the response it builds that the framework leaves to you, so an entry that does all four keeps its full score and one that does none keeps 90% of it. It is applied once to the total rather than per profile, and only to framework entries; engine, infrastructure and unassessed entries score \\u00d71.00. It is also not applied on the WebSocket and gRPC boards, which do not measure the HTTP request-to-response path the four axes are about. Scored vs reference-only profiles Not all profiles count toward the composite score. Profiles marked as scored contribute to the composite. Reference-only profiles (marked with \\\\ *) are displayed for comparison but do not affect the ranking. H/1.1 Isolated Profile Scored Workload Baseline Yes Mixed GET/POST with query parsing Pipelined No (*) 16 requests batched per connection. Reference-only - a raw I/O and middleware-efficiency indicator; HTTP/1.1 pipelining is disabled in modern browsers and proxies, so it no longer counts toward the composite ranking Short-lived Yes Connections closed after 10 requests JSON Yes Dataset processing and serialization JSON Compressed Yes JSON with Accept-Encoding: gzip, br and multiplier ?m=N JSON TLS Yes JSON workload over HTTP/1.1 + TLS on port 8081 Upload Yes 20 MB body ingestion, return byte count Static Yes 20 static files served over HTTP/1.1 Async DB Yes Async Postgres query with connection pooling CRUD Yes Realistic REST API against Postgres: cached reads (75%), updates (15%), list (5%), upsert create (5%). Cache-aside with 200ms TTL (in-process or Redis sidecar) Fortunes No (*) DB query + HTML template render. Reference-only - engine-comparison test, not part of the composite ranking H/1.1 Workload Profile Scored Workload API-4 Yes Baseline + JSON + async-db on 4 CPUs API-16 Yes Baseline + JSON + async-db on 16 CPUs H/2 Profile Scored Workload Baseline Yes Query parsing over TLS with multiplexed streams Static Yes 20 static files served over TLS with multiplexed streams Baseline h2c Yes Query parsing over cleartext h2 on port 8082 (prior-knowledge) JSON h2c Yes JSON serialization workload over cleartext h2 on port 8082 H/3 Profile Scored Workload Baseline Yes Query parsing over QUIC (UDP) with TLS 1.3 Static Yes 20 static files served over QUIC (UDP) with TLS 1.3 Gateway Profile Scored Workload Gateway H2 Yes Two-service proxy + server stack over HTTP/2 + TLS, mixed workload (static 30%, JSON 35%, baseline 20%, async-db 15%), 64-CPU budget Gateway H3 Yes Same two-service stack over HTTP/3 + QUIC at the edge Production Stack H2 Yes Four-service CRUD API (edge + Redis + JWT auth + server) with 10K-item cache-aside, JWT verified every request, concurrent reads + writes gRPC Profile Scored Workload Unary Yes gRPC unary call over cleartext HTTP/2 Unary TLS Yes gRPC unary call over TLS Stream Yes Server-streaming gRPC over cleartext HTTP/2 Stream TLS Yes Server-streaming gRPC over TLS WebSocket Profile Scored Workload Echo Yes WebSocket echo throughput Echo Pipelined Yes Batched WebSocket echo throughput Echo Short-lived Yes WebSocket echo with each connection closed after 10 messages Fortunes, Pipelined, Static, Static TLS, Async Delay, Async DB and CRUD are the reference-only profiles - shown on the board as faded columns for comparison, but not counted in the composite score. The two database profiles were scored until recently. They stopped because the database and its driver dominate them far more than the framework does: a framework\'s async-db number mostly reports which Postgres driver its language has, which is not what this board sets out to compare. They are still run and still published, because the number is worth having; it just no longer decides the ranking. Memory efficiency bonus An optional toggle rewards memory efficiency with an additive bonus per profile. It never scales down the raw-throughput score - it only adds on top of it, up to +500 points for the most memory-efficient framework in that profile. This uses an efficiency ratio ( rps / memoryMB ), not absolute memory usage. A framework that is fast and lean gets the largest bonus; a framework that uses little memory only because it is slow earns less, because its rps is in the numerator of the ratio. CPU efficiency was intentionally dropped: a framework that leaves CPU on the table already scores worse on RPS, so penalizing it a second time for \\"low efficiency\\" double-counted the same signal and flattened the throughput differentiation that the benchmark exists to measure. How the memory bonus is computed For each profile, compute the efficiency ratio for every framework: memEfficiency = sqrt(rps) / memoryMB Why sqrt(rps) instead of rps ? A plain rps / MB ratio double-counts throughput: high-rps frameworks would win both rpsScore and memScore because rps dominates the ratio. Taking the square root dampens rps to log-scale - it still matters (a dead framework shouldn\'t win \\"efficiency\\"), but memory can now actually move the needle. Normalize against the best efficiency in that profile: memScore = (framework_memEff / best_memEff) \\u00d7 1000 Add half of it on top of the RPS score: profileScore = rpsScore + 0.5 \\u00d7 memScore With the toggle on, per-profile scores range 0\\u20131,500 (up to 1,000 from throughput, up to 500 from memory efficiency). Frameworks with no memory data keep their plain rpsScore . Example Framework RPS Mem (MB) sqrt(rps)/MB A 500,000 50 14.14 B 100,000 20 15.81 RPS scores: A = 1,000, B = 200 Memory efficiency scores: A = 894, B = 1,000 (best) With the memory toggle on: A: 1000 + 0.5 \\u00d7 894 = 1447 B: 200 + 0.5 \\u00d7 1000 = 700 B actually wins the memory term despite A\'s 5\\u00d7 throughput advantage, because sqrt(rps) only gives A a \\u221a5 \\u2248 2.24\\u00d7 boost in the numerator - not enough to beat B\'s 2.5\\u00d7 memory savings. A still wins overall thanks to its raw throughput lead, but B\'s lean memory footprint is now rewarded meaningfully instead of being drowned out. Type-specific scoring Types are scored separately - each has its own composite ranking and normalization pool. The scored profiles differ by type: Frameworks (Flagship, Emerging and Experimental, in either Standard or Tuned mode) are scored on all scored profiles across H/1.1, H/2, H/3, gRPC, and WebSocket. Engines are scored on a reduced set: Baseline, Short-lived, JSON TLS, async-db, H/2 (all three), H/3 (both), gRPC (all four), the gateway profiles, and WebSocket, since most engines don\'t implement the heavier endpoints (JSON, upload, CRUD, the API mixes). Infrastructure (nginx, Caddy, h2o and similar proxies / static-file servers) is scored on the eleven profiles a server can answer without an application framework behind it: Baseline, Pipelined, Short-lived, JSON, JSON TLS, Static, Static TLS, and Baseline and Static over both H/2 and H/3. Upload, the database profiles, the API mixes, gRPC, WebSocket and the gateway stacks may be displayed as reference data but do not count. Note that the infrastructure set is not a subset of the framework set. Pipelined is reference-only for frameworks and engines - it measures batching more than framework throughput - but for a proxy that behaviour is exactly what is being compared, so it counts there and only there. The Type filter on the composite leaderboard switches between these rankings. Flagship, Emerging and Experimental can be combined (they share the framework normalization pool); Engine and Infrastructure are each exclusive. Experimental is hidden by default and shown only when selected. Tuned entries (a mode , not a type) are shown within whichever framework types are selected, marked with a ring. Why this approach Sum across profiles - larger numbers separate strong all-rounders from frameworks that only win a single profile; a framework that covers 15 profiles at 80% crushes one that wins one profile at 100% Normalization - each profile contributes equally regardless of absolute RPS scale (baseline at 1M vs JSON at 200K), and is capped at 1,000 points per profile Additive memory bonus - memory-efficient frameworks earn up to +500 per profile on top of their RPS score; slow frameworks can\'t game the bonus because rps is in the efficiency numerator Average across connections - each framework is scored on its average RPS across all connection counts, rewarding consistent scaling"},{"u":"test-profiles","t":"Test Profiles","c":"Knowledge Base","d":"Every HttpArena benchmark profile, each isolating a different performance dimension so frameworks are compared on equal terms.","x":"HttpArena runs every framework through multiple benchmark profiles. Each profile isolates a different performance dimension, ensuring frameworks are compared fairly across varied workloads. Your framework must implement endpoints depending on which test profiles it participates in. All HTTP/1.1 endpoints are served on port 8080 . HTTPS/H2/H3 endpoints are served on port 8443 . Each profile is run at multiple connection counts to show how frameworks scale under increasing concurrency. API Specification All benchmark endpoints are described in machine-readable OpenAPI 3.0 specifications. Each spec covers request/response schemas, status codes, standard and tuned implementation rules, and links back to the relevant documentation pages. The endpoints are split across two files: Spec Profiles covered openapi.json All HTTP/1.1, HTTP/2, HTTP/3, gRPC, and WebSocket profiles Browse openapi-gateway.json Gateway profiles (production-stack) Browse Benchmark parameters Five load generators are dispatched per profile - each one is built for a specific protocol + workload shape. See Load Generators for per-tool details. Parameter Value Load generators gcannon (HTTP/1.1, upload, WebSocket), wrk (static + json-tls rotation), h2load (HTTP/2, h2c, gateway), h2load-h3 (HTTP/3 / QUIC) Threads 64 for gcannon / wrk / h2load / h2load-h3 ( $THREADS / $H2THREADS / $H3THREADS ) Duration 5s default; async-db 10s; api-4 , api-16 , crud 15s (hardcoded in the profile dispatcher) Runs 3 per (profile, connection count) - best RPS wins Networking Docker --network host for all containers (server + load generator + Postgres + Redis sidecars) Data mounts Data files are mounted automatically by the benchmark runner - your Dockerfile does not need to include them. The following paths are available inside the container at runtime: Path Description /data/dataset.json 50-item dataset for /json , /db , and /async-db /data/static/ 20 static assets for /static/* (HTML, JS, CSS, SVG, WebP, woff2, JSON). 15 assets ship with pre-built .gz and .br sibling files (e.g. app.js , app.js.gz , app.js.br ) so frameworks that support precompressed serving can skip on-the-fly compression. The 5 already-binary formats ( hero.webp , thumb1.webp , thumb2.webp , bold.woff2 , regular.woff2 ) have no precompressed variants. See the Static profile for how to wire Accept-Encoding lookup. /certs/server.crt , /certs/server.key TLS certificate and key for HTTPS / H2 / H2 h2c (port 8082 is cleartext) / H3 Environment variables Set by the benchmark runner when the relevant profile runs - your process will see them via os.environ / std::env::var / equivalent. Variable Profiles Value DATABASE_URL async-db , crud , api-4 , api-16 Postgres connection string ( postgres://bench:bench@127.0.0.1:5432/benchmark ) DATABASE_MAX_CONN same as above 256 - the Postgres sidecar\'s max_connections ; size your pool \\u2264 this REDIS_URL crud redis://127.0.0.1:6379 - multi-process frameworks can use Redis as a cross-process cache; single-heap frameworks (Go, ASP.NET, etc.) typically ignore it and keep their in-process cache Gateway and production-stack profiles are compose-orchestrated, so their services receive additional env (e.g. JWT_SECRET for the production-stack auth sidecar) via their compose.*.yml files rather than through the runner. See the per-profile pages under Gateway for details. H/1.1 Isolated single-endpoint benchmarks and multi-endpoint workload mixes over plain TCP. H/2 Baseline and static file benchmarks over encrypted TLS connections with stream multiplexing. Gateway Multi-service deployments: proxy + server (H2/H3) and full CRUD production stack with JWT auth + cache-aside. H/3 Baseline and static file benchmarks over QUIC for frameworks with native H/3 support. gRPC Unary RPC throughput over cleartext HTTP/2 using Protocol Buffers serialization. WebSocket WebSocket echo throughput measuring frame processing performance."},{"u":"test-profiles/gateway","t":"Gateway","c":"Knowledge Base \\u203a Test Profiles","d":"Multi-service benchmark profiles: reverse proxy plus application server, with optional auth sidecars, caches and a database.","x":"Gateway test profiles benchmark multi-service deployments - proxy + server, with optional auth sidecars, caches, and databases. Unlike isolated tests that measure a single framework container, gateway tests measure the end-to-end throughput of the entire stack as a unit. All gateway tests use Docker Compose for orchestration, pin services to specific CPU cores via cpuset , and give entries full control over their architecture within a fixed 64-CPU budget. Gateway H2 Two-service proxy + server stack over HTTP/2 + TLS. Mixed workload: static 30%, JSON 35%, baseline 20%, async-db 15%. Gateway H3 Same two-service stack as Gateway H2 but with HTTP/3 + QUIC at the edge. Production Stack H2 Four-service CRUD API: edge + Redis + JWT auth sidecar + server. 10K-item cache-aside, JWT verified every request, concurrent reads + writes."},{"u":"test-profiles/gateway/gateway-h2","t":"Gateway H2","c":"Knowledge Base \\u203a Test Profiles \\u203a Gateway","d":"A two-service proxy and server stack over HTTP/2 with TLS: the proxy serves static files from disk and forwards dynamic endpoints.","x":"Two-service proxy + server stack over HTTP/2 + TLS. Proxy serves static files directly from disk, forwards dynamic endpoints (baseline, JSON, async-db) to the application server. 64 CPUs split freely between the two services. Implementation Guidelines Compose file layout, endpoint responsibilities, proxy-to-server protocol, CPU allocation. Validation Checks executed by validate.sh against the running compose stack."},{"u":"test-profiles/gateway/gateway-h2/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a Gateway \\u203a Gateway H2","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the Gateway H2 proxy and server stack.","x":"Standard Tuned Engine Must ship exactly two services - one reverse proxy and one application server. The proxy must be a widely-used, production-grade server (Nginx, Caddy, Envoy, HAProxy, Traefik, etc.). No custom proxy implementations. No caches, load balancers, or additional sidecars beyond the two services. The proxy must serve /static/* directly from disk; the server must serve /baseline2, /json, and /async-db using standard framework middleware. Same two-service shape as production. May optimize proxy configuration (worker counts, buffer sizes, keepalive tuning, connection pooling). May tune the proxy-to-server protocol (h2c, Unix sockets, etc.). Server may use any caching or optimization strategy on its own endpoints. No specific rules. May use custom proxy implementations. Ranked separately from frameworks. The Gateway-64 test benchmarks a proxy + server combination as a unit. The load generator sends TLS-encrypted HTTP/2 requests to port 8443; the proxy terminates TLS, serves /static/* directly from disk, and forwards dynamic endpoints to the application server over the entry\'s choice of internal protocol. Architecture Exactly two services - nothing else. One proxy, one server. Splitting dynamic endpoints across multiple servers, adding a caching layer, load-balancing across replicas, or running a single combined service are all not allowed for this test. The shape is fixed so entries compete on proxy choice, proxy tuning, proxy-to-server protocol, and CPU split - not on architectural creativity. TLS (h2) proxy \\u2192 server \\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510 \\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500> \\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510 \\u2500\\u2500\\u2500\\u2500\\u2500\\u2500> \\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510 \\u2502 h2load \\u2502 \\u2502 Proxy \\u2502 \\u2502 Server \\u2502 \\u2502 (load \\u2502 port 8443 \\u2502 TLS term \\u2502 any proto \\u2502 baseline\\u2502 \\u2502 gen) \\u2502 HTTPS/h2 \\u2502 /static/* \\u2502 \\u2502 json \\u2502 \\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518 \\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518 \\u2502 async-db\\u2502 \\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518 CPU: N of 64 CPU: 64-N Endpoint responsibilities Path Handled by Role /static/* Proxy Static files served directly from /data/static/ /baseline2?a=N&b=M Server Query-parameter sum /json Server Dataset processing (~10 KB JSON response) /async-db?min=N&max=M Server Postgres range query Rules: The proxy must serve /static/* from disk. Forwarding static files to the server is not allowed. The server must serve all three dynamic endpoints. Proxy-level caching of dynamic responses is not allowed. The proxy must terminate TLS. Passing raw TLS through to the server is not allowed. See the individual test profile docs for each endpoint\'s exact request/response format: /static/* /baseline2 /json /async-db Docker Compose Every gateway entry is orchestrated via a compose.gateway.yml file with exactly two services named proxy and server . The benchmark script builds, starts, and tears down the stack for each run. Example services: proxy: build: ./proxy network_mode: host cpuset: \\"0-7,64-71\\" ulimits: memlock: -1 nofile: soft: 1048576 hard: 1048576 security_opt: - seccomp:unconfined volumes: - ${CERTS_DIR}:/certs:ro - ${DATA_DIR}/static:/data/static:ro depends_on: - server server: build: context: ../../ dockerfile: frameworks/my-framework/Dockerfile network_mode: host cpuset: \\"8-31,72-95\\" ulimits: memlock: -1 nofile: soft: 1048576 hard: 1048576 security_opt: - seccomp:unconfined environment: - DATABASE_URL=${DATABASE_URL} - DATABASE_MAX_CONN=256 volumes: - ${DATA_DIR}/dataset.json:/data/dataset.json:ro In this split: Proxy (Nginx) gets 8 physical cores (16 logical: 0-7,64-71 ) - terminates TLS, serves /static/* directly from /data/static/ , forwards dynamic endpoints to the server on localhost. Server gets 24 physical cores (48 logical: 8-31,72-95 ) - listens on an internal port (e.g. 8080 plain HTTP/1.1 or h2c) and handles /baseline2 , /json , /async-db . Total: 16 + 48 = 64 logical CPUs. Environment variables The benchmark script exports three environment variables before every compose command. Your compose file must use these - do not hardcode paths. Variable Value CERTS_DIR Absolute path to the TLS certificate directory ( server.crt , server.key ) DATA_DIR Absolute path to the data directory ( static/ , dataset.json ) DATABASE_URL Postgres connection string for /async-db (sidecar is started by the benchmark script) Required compose settings Every service in your compose file must include these settings: Setting Value Why network_mode: host Both services Bridge networking adds measurable latency; host networking keeps proxy-to-server at native localhost speed. cpuset CPU range string Pins the service to specific cores. See CPU allocation . security_opt: [seccomp:unconfined] Both services Allows io_uring and other syscalls that the default seccomp profile blocks. ulimits.memlock: -1 Both services Allows memory locking for performance-critical operations. ulimits.nofile: { soft: 1048576, hard: 1048576 } Both services Raises the file descriptor limit. Proxy \\u2192 server protocol The proxy-to-server transport is the entry\'s choice and one of the most important tuning decisions: Protocol Pros Cons HTTP/1.1 over TCP Simplest to configure, universally supported No multiplexing - one request per connection at a time HTTP/1.1 + keepalive pool Reuses connections, avoids handshake per request Still no multiplexing h2c (HTTP/2 cleartext) Multiplexing without the TLS cost of a second hop Not all frameworks support h2c Unix domain sockets Lowest latency, no TCP stack overhead Server must listen on a UDS The benchmark measures end-to-end throughput from the load generator\'s perspective. How the proxy talks to the server is an implementation detail, but it materially affects the score. CPU allocation The test uses 64 logical CPUs - 32 physical cores (0\\u201331) plus their SMT siblings (64\\u201395). The entry\'s compose.gateway.yml defines how these are split between proxy and server using the cpuset directive. The split is the entry\'s choice - give the proxy as little or as much as you want, as long as the total across both services equals exactly 64 logical CPUs. CPU topology rules CPUs are allocated in physical core pairs - each allocation includes both the physical core and its SMT sibling. This avoids two services sharing a physical core and ensures consistent per-service performance. Proxy cpuset Server cpuset Split 0-1,64-65 2-31,66-95 4 + 60 = 64 0-3,64-67 4-31,68-95 8 + 56 = 64 0-7,64-71 8-31,72-95 16 + 48 = 64 0-15,64-79 16-31,80-95 32 + 32 = 64 Understanding SMT pairing Each physical core has two logical CPUs: the core itself and its SMT (HyperThreading) sibling. On the benchmark machine, physical core N maps to logical CPUs N and N+64 : Physical core 0 \\u2192 logical CPUs 0 and 64 Physical core 15 \\u2192 logical CPUs 15 and 79 Physical core 31 \\u2192 logical CPUs 31 and 95 When you allocate cores, you must always allocate both siblings to the same service. Two threads sharing a physical core compete for ALU, cache, and branch predictor - splitting siblings across services causes unpredictable cross-service interference. Correct - proxy gets physical cores 0\\u20137 (16 logical CPUs): cpuset: \\"0-7,64-71\\" Incorrect - splits a physical core across services: proxy: cpuset: \\"0-3\\" # gets core 3 without its sibling (67) server: cpuset: \\"4-31,64-95\\" # gets sibling 67 without core 3 Incorrect - doesn\'t allocate all 64 CPUs: proxy: cpuset: \\"0-3,64-67\\" # 8 CPUs server: cpuset: \\"4-15,68-79\\" # 24 CPUs - 32 CPUs wasted Choosing the split Lightweight proxy (plain forwarding, little static traffic) - 2\\u20134 physical cores is usually enough TLS-terminating proxy with heavy static - 4\\u20138 physical cores. TLS handshakes and static I/O compete for CPU at high connection counts. Even split - 16+16 physical cores is a good starting point if you don\'t know which side is the bottleneck meta.json { \\"display_name\\": \\"my-framework + nginx\\", \\"language\\": \\"C#\\", \\"type\\": \\"flagship\\", \\"mode\\": \\"standard\\", \\"engine\\": \\"kestrel\\", \\"description\\": \\"ASP.NET minimal API behind Nginx reverse proxy.\\", \\"repo\\": \\"https://github.com/dotnet/aspnetcore\\", \\"tests\\": [\\"gateway-64\\"] } The tests array only needs gateway-64 for gateway-only entries. Frameworks that also participate in other tests can subscribe to both - the standard Dockerfile is used for non-gateway tests and compose.gateway.yml for gateway-64 . Directory structure frameworks/my-framework/ compose.gateway.yml # Two services: proxy + server Dockerfile # Server image meta.json proxy/ Dockerfile # Proxy image (e.g. Nginx with custom config) nginx.conf # Proxy configuration Workload The load generator ( h2load ) requests 20 URIs in a round-robin across multiplexed HTTP/2 streams, all requests sent with Accept-Encoding: br;q=1, gzip;q=0.8 : Category URIs Count Weight Handled by Static files /static/reset.css , components.css , app.js , vendor.js , header.html , hero.webp - a mix of CSS, JS, HTML, and an image for sendfile -path coverage 6 30% Proxy JSON /json/{count} with count \\u2208 {1, 5, 10, 15, 25, 40, 50} - 7 payload sizes, same set as the h1-isolated JSON profile 7 35% Server Baseline /baseline2?a=N&b=M with 4 distinct parameter combinations to defeat URI-keyed caches 4 20% Server Async DB /async-db?min=10&max=50&limit=N with limit \\u2208 {10, 25, 50} 3 15% Server The mix is JSON-weighted (35%) because server-side compute is the most expensive part of the stack; static serving (30%) keeps the proxy\'s I/O path under meaningful load; baseline (20%) measures raw forwarding efficiency at minimal server cost; async-db (15%) is the smallest slice because it\'s latency-bound on the Postgres round-trip rather than CPU-bound on either service. The leaderboard shows a colored breakdown of how many requests each category handled. What it measures Reverse proxy overhead - how much throughput is lost by adding a proxy layer vs. direct server access TLS termination efficiency - the proxy\'s cost to terminate HTTPS/h2 at high connection counts HTTP/2 multiplexing through a proxy - whether the proxy can efficiently forward multiplexed streams Static file serving from the proxy - disk I/O, precompressed asset selection, kernel sendfile usage Mixed workload throughput - static I/O (proxy) vs. compute (server JSON) vs. database (server async-db) all competing for the same 64-CPU budget Proxy-to-server protocol choice - HTTP/1.1, h2c, or Unix domain sockets CPU split - how the entry balances proxy and server workload on a fixed CPU budget Parameters Parameter Value Endpoints /static/* , /json , /async-db , /baseline2 Connections 512, 1,024 Streams per connection 32 ( -m 32 ) Duration 5s Runs 3 (best taken) Load generator h2load with -i (multi-URI round-robin) Total CPU budget 64 logical (32 physical + 32 SMT), split freely between proxy and server Memory limit Unlimited Port 8443 (HTTPS/H2) Orchestration Docker Compose ( compose.gateway.yml )"},{"u":"test-profiles/gateway/gateway-h2/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a Gateway \\u203a Gateway H2","d":"The correctness checks validate.sh runs against the Gateway H2 proxy and server stack before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the gateway-64 test. The validation script builds and starts the full compose stack from compose.gateway.yml before running checks. All requests are sent over HTTPS with HTTP/2 to port 8443 - the same port the load generator uses during benchmarking. This validates the full end-to-end path through the proxy (or directly to the server if no proxy is used), not just the server in isolation. Stack startup The Postgres sidecar is started (needed for /async-db endpoint) The compose stack is built and started via docker compose -f compose.gateway.yml up --build -d Validation waits up to 30 seconds for HTTPS on port 8443 to respond All checks run against https://localhost:8443 The compose stack is torn down after validation completes HTTP/2 protocol negotiation Sends a request to https://localhost:8443/static/reset.css and verifies the response uses HTTP/2 (via ALPN). This confirms TLS and h2 are correctly configured at the entry point (proxy or server). Static file checks Content-Type headers Verifies correct Content-Type headers for representative file types over HTTPS with HTTP/2: GET /static/reset.css - expects Content-Type: text/css GET /static/app.js - expects Content-Type: application/javascript Note: text/javascript is accepted as equivalent to application/javascript per RFC 9239. Response size Requests GET /static/app.js over HTTP/2 and verifies the response size is greater than 0 bytes. This confirms the proxy is correctly forwarding to the server and the server is returning file content. 404 for nonexistent file Sends GET /static/nonexistent.txt over HTTP/2 and verifies the response is HTTP 404 . The 404 must propagate correctly through the proxy. JSON endpoint checks Response structure Sends GET /json over HTTP/2 and validates: Response contains exactly 50 items Every item carries the full schema - id , name , category , price , quantity , active , tags (array), rating (object with score and count ), and total Each total is correctly computed as price * quantity (rounded to 2 decimal places) This is the same validation as the JSON Processing test , but routed through the proxy. Content-Type header Verifies the response has Content-Type: application/json . Async database checks Response structure Sends GET /async-db?min=10&max=50 over HTTP/2 and validates: Response count is between 1 and 50 Every item has a nested rating object with a score field Every item has a tags array Every item\'s active field is a boolean (not a string or integer) This is the same validation as the Async Database test , but routed through the proxy. Content-Type header Verifies the response has Content-Type: application/json . Anti-cheat: empty range Sends GET /async-db?min=9999&max=9999 and verifies the response returns count: 0 . This detects hardcoded or cached responses that don\'t actually query the database. Baseline endpoint checks Fixed input Sends GET /baseline2?a=13&b=42 over HTTP/2 and verifies the response body is 55 (the sum of the two query parameters). This confirms the proxy correctly forwards query parameters to the application server. Anti-cheat: randomized input Sends GET /baseline2?a=<random>&b=<random> with random values and verifies the response matches the expected sum. This detects hardcoded responses. Prerequisite checks The gateway-64 test relies on endpoint implementations that are validated individually in other test profiles: /json - JSON Processing validation /static/* - Static Files validation /async-db - Async Database validation /baseline2 - Baseline H2 validation Frameworks subscribing to gateway-64 should also subscribe to these individual tests to ensure each endpoint passes validation independently before testing through the proxy."},{"u":"test-profiles/gateway/gateway-h3","t":"Gateway-H3","c":"Knowledge Base \\u203a Test Profiles \\u203a Gateway","d":"The HTTP/3 sibling of the Gateway H2 profile: QUIC at the edge, the same mixed workload and the same 64-CPU budget.","x":"Proxy + server stack with HTTP/3 at the edge and a realistic mixed workload - static files served by the proxy from disk, dynamic endpoints forwarded to the application server. Implementation Guidelines Compose file layout, endpoint responsibilities, proxy-to-server protocol choices, CPU allocation. Validation Checks executed by validate.sh against the running compose stack."},{"u":"test-profiles/gateway/gateway-h3/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a Gateway \\u203a Gateway-H3","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the Gateway H3 proxy and server stack.","x":"Standard Tuned Engine Must ship exactly two services - one HTTP/3-capable reverse proxy and one application server. The proxy must be a widely-used, production-grade server with QUIC support (Caddy, nginx 1.25+ with ngx_http_v3_module, Envoy, HAProxy 2.8+, h2o). No custom QUIC implementations. No caches, load balancers, or additional sidecars beyond the two services. The proxy must serve /static/* directly from disk; the server must serve /baseline2, /json, and /async-db using standard framework middleware. Same two-service shape as production. May optimize proxy configuration (worker counts, buffer sizes, keepalive tuning, QUIC parameter tuning). May tune the proxy-to-server protocol (h1/h2c/UDS). Server may use any caching or optimization strategy on its own endpoints. No specific rules. May use custom QUIC implementations. Ranked separately from frameworks. The Gateway-H3 test is the HTTP/3 sibling of Gateway-64 . Same endpoint surface, same two-service shape, same 64-CPU budget, same 20-URI round-robin mix - the only difference is the edge protocol . The load generator sends requests over QUIC to port 8443 (UDP), the proxy terminates h3 + TLS, and the upstream backend is still reached over plain h1 (or whatever the entry chooses internally). Architecture Exactly two services: one HTTP/3-capable reverse proxy and one application server. The proxy handles QUIC termination and serves /static/* directly from disk. The application server handles /baseline2 , /json/{count} , and /async-db . h3/QUIC (UDP) proxy \\u2192 server \\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510 \\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500> \\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510 \\u2500\\u2500\\u2500\\u2500\\u2500\\u2500> \\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510 \\u2502 h2load \\u2502 \\u2502 Proxy \\u2502 \\u2502 Server \\u2502 \\u2502 (-h3) \\u2502 port 8443/udp \\u2502 h3 + TLS \\u2502 any proto \\u2502 baseline\\u2502 \\u2502 \\u2502 \\u2502 /static/* \\u2502 \\u2502 json \\u2502 \\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518 \\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518 \\u2502 async-db\\u2502 \\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518 CPU: N of 64 CPU: 64-N Why split out from Gateway-64? HTTP/3 shifts work around compared to HTTP/2 in ways that are worth measuring separately: No head-of-line blocking at the TCP layer - QUIC streams are independent, so a slow response on one stream doesn\'t stall others on the same connection Stream and datagram framing happens in userspace , not in the kernel\'s TCP stack - moves CPU cost from softirq to the proxy process Encryption is per-packet, not per-record - different cost profile than TLS-over-TCP UDP send/recv syscall overhead is higher than TCP sendfile() , but SO_TXTIME / SO_TIMESTAMPING / GRO/GSO mitigations vary by kernel version and proxy implementation Connection migration and 0-RTT are h3-specific features that production proxies handle very differently The H/2 Gateway numbers can\'t predict any of this. Running the same workload over h3 gives you the other half of the picture. Endpoint responsibilities Same as Gateway-64 : Path Handled by Role /static/* Proxy Static files served directly from /data/static/ (precompressed .br / .gz sidecars allowed) /baseline2?a=N&b=M Server Query-parameter sum /json/{count} Server Dataset processing (~10 KB JSON response) /async-db?min=N&max=M&limit=L Server Postgres range query Rules (identical to Gateway-64): The proxy must serve /static/* from disk. Forwarding static files to the server is not allowed. The server must serve all three dynamic endpoints. Proxy-level caching of dynamic responses is not allowed. The proxy must terminate QUIC + TLS at the edge. Docker Compose Entries ship a compose.gateway-h3.yml file with exactly two services named proxy and server . The benchmark script builds, starts, and tears down the stack for each run. Example services: proxy: build: ./proxy network_mode: host cpuset: \\"0-19,64-83\\" ulimits: memlock: -1 nofile: soft: 1048576 hard: 1048576 security_opt: - seccomp:unconfined volumes: - ${CERTS_DIR}:/certs:ro - ${DATA_DIR}/static:/data/static:ro depends_on: - server server: build: context: ../../ dockerfile: frameworks/my-framework/Dockerfile network_mode: host cpuset: \\"20-31,84-95\\" ulimits: memlock: -1 nofile: soft: 1048576 hard: 1048576 security_opt: - seccomp:unconfined environment: - DATABASE_URL=${DATABASE_URL} - DATABASE_MAX_CONN=256 volumes: - ${DATA_DIR}/dataset.json:/data/dataset.json:ro Proxy options (pick one): Caddy - h3 is enabled by default when you bind a TLS listener. Stock caddy:2-alpine image works out of the box. See the reference entry at frameworks/aspnet-minimal_caddy/ for a minimal working Caddyfile. nginx with QUIC - nginx 1.25+ supports h3 via ngx_http_v3_module , but the stock nginx:alpine image is not built with it. You need to either build from source or use a community image that includes QUIC. Envoy - supports h3 via the envoy.quic.connection_id_generator + QuicProtocolOptions listener config. HAProxy 2.8+ - supports h3 via the quic4@:8443 bind spec. Required compose settings Setting Value Why network_mode: host Both services Bridge networking adds measurable latency; host networking keeps proxy-to-server at native localhost speed, and lets the proxy bind UDP 8443 directly on the host for QUIC. cpuset CPU range string Pins the service to specific cores. See CPU allocation . security_opt: [seccomp:unconfined] Both services Allows io_uring and other syscalls the default seccomp profile blocks. QUIC-specific syscalls like SO_TXTIME also benefit. ulimits.memlock: -1 Both services Allows memory locking for performance-critical operations. ulimits.nofile: { soft: 1048576, hard: 1048576 } Both services Raises the file descriptor limit. CPU allocation Identical to Gateway-64 - 64 logical CPUs (32 physical + 32 SMT siblings) split freely between proxy and server, with SMT-sibling pairing required. See the Gateway-64 page for the full rules. Workload The load generator ( h2load-h3 ) requests 20 URIs in a round-robin across multiplexed HTTP/3 streams. All requests include Accept-Encoding: br;q=1, gzip;q=0.8 : Category URIs Count Weight Handled by Static files /static/reset.css , components.css , app.js , vendor.js , header.html , hero.webp 6 30% Proxy JSON /json/{count} with count \\u2208 {1, 5, 10, 15, 25, 40, 50} 7 35% Server Baseline /baseline2?a=N&b=M with 4 distinct parameter combinations 4 20% Server Async DB /async-db?min=10&max=50&limit=N with limit \\u2208 {10, 25, 50} 3 15% Server Same mix and weighting as Gateway-64 - the requests/gateway-64-uris.txt URI file is shared between both profiles so benchmark numbers are directly comparable across the edge protocol dimension. What it measures QUIC termination cost at the proxy at realistic connection counts HTTP/3 stream multiplexing through a proxy Static file serving over h3 - disk I/O + precompressed asset selection + UDP send path Mixed workload throughput when the edge is QUIC instead of TCP+TLS h3-vs-h2 delta for the same stack - comparing Gateway-H3 to Gateway-64 numbers tells you how much of a framework\'s gateway performance is attributable to edge protocol choice Parameters Parameter Value Endpoints /static/* , /json/{count} , /async-db , /baseline2 Connections 64, 256 Streams per connection 32 ( -m 32 ) Duration 5s Runs 3 (best taken) Load generator h2load-h3 with -i (multi-URI round-robin) and --alpn-list=h3 Total CPU budget 64 logical (32 physical + 32 SMT), split freely between proxy and server Memory limit Unlimited Port 8443 (UDP for h3, TCP as fallback for h1/h2 depending on proxy) Orchestration Docker Compose ( compose.gateway-h3.yml )"},{"u":"test-profiles/gateway/gateway-h3/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a Gateway \\u203a Gateway-H3","d":"The correctness checks validate.sh runs against the Gateway H3 proxy and server stack before a framework\'s results are accepted.","x":"The Gateway-H3 validation flow reuses the same checks as Gateway-64 - same endpoints, same expected responses, same anti-cheat probes. The only differences: A separate compose.gateway-h3.yml is built and started (instead of compose.gateway.yml ). Endpoint probes go to the same proxy on port 8443 but use curl --http2 rather than curl --http3 . Why curl --http2 for an h3 test? Most curl builds don\'t ship HTTP/3 support by default, and requiring QUIC-enabled curl would break validate.sh on contributors\' machines. The compromise: h3-capable proxies (Caddy, nginx-quic, Envoy, HAProxy) advertise h2 on the same port anyway, so curl can reach the same endpoints for correctness validation. The h3-specific path is exercised at benchmark time by h2load-h3 with --alpn-list=h3 - if h3 is broken, the benchmark will show 0 req/s and you\'ll see it immediately. Checks performed Same set as Gateway-64 validation : HTTPS port responds within 30 seconds HTTP/2 protocol negotiation (as a proxy-health sanity check) /static/reset.css - Content-Type: text/css /static/app.js - Content-Type: application/javascript /static/app.js - non-zero response body /static/nonexistent.txt - HTTP 404 /json/50 - returns 50 items, each with the full schema ( id , name , category , price , quantity , active , tags , rating with score + count ) and a computed total /json/50 - Content-Type: application/json /async-db?min=10&max=50&limit=50 - returns 1\\u201350 items with nested rating , tags , boolean active /async-db - Content-Type: application/json /async-db?min=9999&max=9999 - returns count: 0 (anti-cheat) /baseline2?a=13&b=42 - returns 55 /baseline2?a=<random>&b=<random> - returns correct sum (anti-cheat) What validation does NOT check HTTP/3 protocol negotiation at the edge - requires QUIC-capable curl, not available in most builds. Covered implicitly by the benchmark: if the proxy can\'t speak h3, h2load-h3 will report 0 req/s. QUIC connection migration, 0-RTT, or specific QUIC features - out of scope for correctness validation. Precompressed sidecar serving - validated at runtime by the benchmark\'s Accept-Encoding: br;q=1, gzip;q=0.8 request header. If the proxy serves the raw file instead of the .br / .gz sidecar, response size will be larger and bandwidth numbers will reflect that."},{"u":"test-profiles/gateway/production-stack","t":"Production Stack","c":"Knowledge Base \\u203a Test Profiles \\u203a Gateway","d":"A four-service CRUD API deployment: reverse proxy, JWT auth sidecar, Redis cache and application server under concurrent reads and writes.","x":"The Production Stack profile benchmarks a realistic multi-service CRUD API deployment - reverse proxy, JWT auth sidecar, Redis cache, application server, and Postgres database - with a 10,000-item working set that exercises a real three-tier cache hierarchy (L1 in-process \\u2192 L2 Redis \\u2192 Postgres) under authenticated load. This is the closest any HttpArena test comes to modeling real production traffic: JWT authentication on every API call - a shared Rust sidecar verifies HMAC-SHA256 signatures with no caching. Real cryptographic work on every request, not a cached lookup. Cache-aside required - frameworks must implement cache-aside on reads (check cache \\u2192 miss \\u2192 DB \\u2192 populate) and invalidation on writes. How the caching works (in-process, Redis, two-tier, framework-native) is the framework\'s choice - the cache strategy is part of the competition. Cache-aside with short TTL - items cached \\u22641 second so the cache constantly cycles: L1 hits \\u2192 L2 (Redis) hits \\u2192 Postgres queries \\u2192 populate both layers. 10K unique IDs mean the cache hierarchy is under real pressure, not permanently warm. CRUD writes - POST requests parse a JSON body, UPDATE in Postgres, and invalidate both cache layers. Reads and writes target overlapping IDs to create realistic cache churn. Implementation Guidelines Four-service compose, JWT auth contract, HybridCache rules, cache-aside pattern, endpoint specs, CPU allocation. Validation 15 checks: auth wall (4), cache-aside cycle, POST invalidation, user lookup chain."},{"u":"test-profiles/gateway/production-stack/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a Gateway \\u203a Production Stack","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the four-service Production Stack deployment.","x":"Standard Tuned Engine Must ship exactly four services: edge (reverse proxy), cache (Redis), authsvc (the shared JWT verifier from frameworks/_shared/authsvc/, built as-is), and server (the framework). No custom auth implementations - JWT verification must happen at the edge via auth_request / forward_auth using the shared authsvc. The framework must implement cache-aside on /api/items reads (check cache \\u2192 miss \\u2192 query DB \\u2192 populate cache with \\u22641 second TTL) and cache invalidation on /api/items writes (clear cache after DB update). How the caching is implemented is the framework\'s choice - any combination of in-process cache, Redis, or framework-specific cache abstraction is allowed. The server must wait on a healthy cache - depends_on with condition: service_healthy, not the short list form - so a cache that cannot start fails the stack instead of leaving the run measured against whatever else holds 6379. Same four-service shape as production. May tune proxy configuration, connection pools, CPU split, and cache TTLs. May NOT replace authsvc with a custom implementation or skip JWT verification. The server must wait on a healthy cache - depends_on with condition: service_healthy, not the short list form - so a cache that cannot start fails the stack instead of leaving the run measured against whatever else holds 6379. No specific rules. May replace any service with a custom implementation. Ranked separately from frameworks. Overview The Production Stack test benchmarks a realistic multi-service CRUD API deployment as a unit. The load generator sends HTTPS/h2 requests to the edge. Request flow depends on the path: Path Flow /static/* edge serves from disk (no auth, no backend) /public/* edge \\u2192 server (no auth, no cache) /api/* edge \\u2192 authsvc (JWT verification) \\u2192 server (HybridCache L1 \\u2192 Redis L2 \\u2192 Postgres) Architecture Four services, plus the shared Postgres sidecar (external, managed by the benchmark script): h2 + TLS \\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510 \\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500> \\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510 auth_request \\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510 \\u2502 h2load \\u2502 port 8443 \\u2502 edge \\u2502 \\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u25ba \\u2502 authsvc \\u2502 \\u2502 \\u2502 \\u2502 (nginx) \\u2502 \\u2502 (Rust) \\u2502 \\u2502 \\u2502 Authorization \\u2502 \\u2502 \\u25c4\\u2500\\u2500 200 + X-User-Id \\u2502 JWT+HMAC \\u2502 \\u2502 \\u2502 Bearer <jwt> \\u2502 \\u2502 \\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518 \\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518 \\u2502 \\u2502 \\u2502 \\u2502 /api/* \\u2502 \\u2502 \\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u25ba \\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510 \\u2502 \\u2502 X-User-Id header \\u2502 server \\u2502 \\u2502 \\u2502 \\u2502(framework\\u2502 \\u2502 \\u2502 /public/* \\u2502 \\u2502 \\u2502 \\u2502 \\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u25ba \\u2502 L1 cache \\u2502 \\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518 \\u2502 L2 Redis \\u2502 \\u2502 Postgres \\u2502 \\u2514\\u2500\\u2500\\u2500\\u2500\\u252c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518 \\u25bc \\u250c\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2510 \\u2502 cache \\u2502 \\u2502 (Redis) \\u2502 \\u2514\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2500\\u2518 Authentication - JWT HMAC-SHA256 Every /api/* request carries an Authorization: Bearer <jwt> header. The edge proxy fires an auth_request subrequest to the shared authsvc sidecar on every single API call - no caching, no shortcuts. authsvc: Reads the Authorization: Bearer <token> header Splits the JWT into header.payload.signature Computes HMAC-SHA256(secret, header.payload) and compares with the provided signature Base64-decodes the payload and extracts the sub claim (user_id) Returns 200 OK with X-User-Id: <user_id> or 401 Unauthorized The JWT secret is shared between the token generator ( data/jwt-token.txt ) and authsvc via the JWT_SECRET environment variable. The pre-generated token embeds {\\"sub\\":\\"42\\",\\"name\\":\\"Alice Chen\\",\\"exp\\":<future>} . Key rules: Entries must use the shared authsvc binary unmodified. The Rust sidecar at frameworks/_shared/authsvc/ is the same for every framework entry. Do not reimplement JWT verification in the framework\'s language. No auth caching at the edge. No proxy_cache on the /_auth location. Every /api/* request must trigger a real HMAC-SHA256 verification. The CPU cost of this verification is part of what the test measures. No auth logic in the framework. The framework receives requests with a trusted X-User-Id header set by the edge after successful JWT verification. The framework never parses JWTs, never validates tokens, never touches the Authorization header. Caching The framework must implement cache-aside on /api/items reads: check cache first, fall through to Postgres on miss, populate cache with a short TTL (\\u22641 second). On writes, the framework must invalidate the cache entry so the next read sees fresh data. How the caching is implemented is entirely the framework\'s choice. Examples: Two-tier L1+L2 : in-process memory cache + Redis (e.g. .NET HybridCache, Rails ActiveSupport::Cache with memory + Redis). Highest performance because hot keys never leave the process. Redis-only : every cache-aside read goes to Redis. Simpler but slower - Redis single-thread becomes the ceiling at high rps. In-process only : no Redis for caching, just a local hashmap with TTL. Works but loses the shared cache on multi-instance deployments. Framework-native cache : whatever the framework ships (Django cache, Spring Cache, etc.). The .NET reference entry uses HybridCache (L1 in-process + L2 Redis) because that\'s the idiomatic .NET 9+ pattern. Other frameworks should use whatever caching approach is natural and documented for their ecosystem. The test measures how well the framework + its cache choice perform together - the cache strategy is part of the competition. Cache-aside pattern GET /api/items/{id} : Check L1 (in-process memory) by key item:{id} L1 miss \\u2192 check L2 (Redis via the distributed cache abstraction) L2 miss \\u2192 SELECT ... FROM items WHERE id = $1 in Postgres Populate both L1 and L2 with the result, TTL \\u2264 1 second Return JSON with X-Cache: HIT or X-Cache: MISS header POST /api/items/{id} : Parse JSON body ( name , price , quantity ) UPDATE items SET ... WHERE id = $1 in Postgres Invalidate cache key item:{id} via the abstraction\'s RemoveAsync / equivalent (must clear both L1 and L2) Return 204 No Content GET /api/me : Read X-User-Id header (set by edge from authsvc JWT verification) Cache-aside lookup on user:{id} with TTL \\u2264 30 seconds Miss \\u2192 SELECT id, name, email, plan FROM users WHERE id = $1 Return user JSON Cache TTLs Cache key L1 TTL L2 TTL Rationale item:{id} \\u2264 1 second \\u2264 1 second Short TTL so the cache constantly cycles and Postgres sees regular traffic. At 10K unique IDs and ~250K rps, each ID is read ~12\\u00d7/sec \\u2192 ~92% L1 hit rate with 1s TTL. user:{id} \\u2264 30 seconds \\u2264 30 seconds User profiles change rarely. Longer TTL means near-100% hit rate during a 5-second benchmark. What the cache hierarchy looks like under load With 10,000 unique item IDs at ~250K rps (50% items): Layer Ops/sec What it does L1 (in-process memory) ~115K hits ~100 ns hashmap lookup L2 (Redis) ~5\\u201310K HMGET on L1 miss, ~50 \\u00b5s Postgres ~5\\u201310K SELECT on L2 miss, ~200 \\u00b5s L2 writeback ~5\\u201310K HMSET + EXPIRE on Postgres hit This is a real three-tier cache hierarchy under real load. Every layer does measurable work. Endpoint responsibilities Endpoint Method Auth Cache Database What the framework does /static/* GET no no no Never reached - edge serves from disk /public/baseline GET no no no a + b query-sum /public/json/{count} GET no no no Slice dataset, serialize JSON /api/items/{id} GET JWT L1 \\u2192 L2 \\u2192 DB SELECT on miss Cache-aside read /api/items/{id} POST JWT invalidate L1+L2 UPDATE Parse body, write DB, invalidate cache /api/me GET JWT L1 \\u2192 L2 \\u2192 DB SELECT on miss Read user by X-User-Id Workload Two h2load instances run in parallel against the same compose stack: Reads (20,000 URIs in production-stack-reads.txt , round-robin): Category Count Share Handled by /static/* 6,000 30% edge (disk, gzip_static) /public/baseline 2,000 10% server (no auth, no cache) /api/items/{id} (IDs 1\\u201310,000) 10,000 50% edge \\u2192 authsvc \\u2192 server (cache-aside) /api/me 2,000 10% edge \\u2192 authsvc \\u2192 server (cache-aside) Writes (20 URIs in production-stack-writes.txt , round-robin, with JSON body): Category URIs What POST /api/items/{id} (IDs 1\\u201320) 20 UPDATE + cache invalidate The reads instance uses -c $conns -m 16 . The writes instance uses -c $(conns/8) -m 8 . Both carry Authorization: Bearer <jwt> so all /api/* requests authenticate successfully. IDs 1\\u201320 are targeted by both reads and writes, creating realistic cache churn on those entries. IDs 21\\u201310,000 are read-only with stable cache behavior. Docker Compose Every production-stack entry ships compose.production-stack.yml with exactly four services: Service Role Image edge Reverse proxy, TLS, static files, auth_request nginx (or equivalent) cache Redis for L2 cache (framework\'s HybridCache backend) redis:7-alpine with seed entrypoint authsvc JWT HMAC-SHA256 verifier (shared, unmodified) Built from frameworks/_shared/authsvc/ server The framework - CRUD handlers with HybridCache Built from the framework\'s Dockerfile Required compose settings Same as Gateway-64 : network_mode: host , pinned cpuset , seccomp:unconfined , and memlock/nofile ulimits on all four services. Two more are specific to this profile, and both exist so the stack fails loudly instead of quietly measuring something else: server must wait for a healthy cache . Use the map form with a condition, not the short list form: ` yaml cache: healthcheck: test: [\\"CMD\\", \\"redis-cli\\", \\"-p\\", \\"6379\\", \\"ping\\"] interval: 30s timeout: 2s retries: 3 start_period: 30s start_interval: 1s server: depends_on: cache: condition: service_healthy ` depends_on: - cache waits only for the container to start . A cache that dies on boot (because something else already holds 6379, say) leaves the server running happily against whatever Redis does hold it, and the profile publishes numbers measured against a cache it never configured. Entries ran this way for weeks without anyone noticing. edge must wait for a healthy authsvc ( condition: service_healthy ). Without it a run can measure 5xx from the edge and never reach the server at all. Ports the stack owns All four services run on network_mode: host , so for the duration of the profile these are host ports, not container ports: Port Service 8443 edge (h2 over TLS) 9090 authsvc 8080 server 6379 cache Two things the benchmark driver does so they are actually free: It stops its own Redis sidecar before bringing the stack up, and restarts it afterwards. That sidecar is started once per run whenever the entry also subscribes to CRUD and holds 6379 for the whole run, so an entry subscribed to both would otherwise collide with itself on every run. It keeps all four out of the ephemeral port range . ip_local_port_range is widened to 1024 65535 for connection-churning profiles, which would otherwise let a load generator be holding one of these when the stack starts. See Kernel tuning . Environment variables Variable Set on Value JWT_SECRET authsvc httparena-bench-secret-do-not-use-in-production AUTHSVC_LISTEN authsvc 0.0.0.0:9090 DATABASE_URL server Postgres connection string (set by benchmark script) REDIS_URL server redis://127.0.0.1:6379 CERTS_DIR edge TLS certificate directory (set by benchmark script) DATA_DIR edge, cache Dataset + static files + seed data CPU allocation 64 logical CPUs (32 physical + 32 SMT siblings) split across four services. The split is the entry\'s choice, but the reference entry\'s empirically-tuned split is: Service Physical Logical Rationale edge 15 30 TLS termination + h2 framing + auth_request subrequests cache 1 2 Redis is single-threaded; more cores don\'t help authsvc 4 8 HMAC-SHA256 is CPU-bound and parallelizable. 4 cores is empirically optimal on the reference hardware - going to 5 causes a cliff (likely CCX/L3 boundary). server 12 24 Framework CRUD handlers + HybridCache + Postgres client Important tuning finding: authsvc scales linearly with cores (HMAC-SHA256 is embarrassingly parallel) BUT stealing cores from server or edge to give to authsvc often makes overall throughput worse because the downstream services can\'t keep up. The optimal split is where edge, authsvc, and server are all near their utilization ceiling simultaneously. Database Two tables, both seeded by data/pgdb-seed.sql : items - 100,000 rows, used by /api/items/{id} : CREATE TABLE items ( id INTEGER PRIMARY KEY, name TEXT, category TEXT, price INTEGER, quantity INTEGER, active BOOLEAN, tags JSONB, rating_score INTEGER, rating_count INTEGER ); users - 4 rows, used by /api/me : CREATE TABLE users ( id INTEGER PRIMARY KEY, name TEXT, email TEXT, plan TEXT ); What this test measures Full production stack throughput with real JWT auth, real cache hierarchy, real DB writes Framework cache abstraction efficiency - HybridCache, Rails.cache, Django cache, etc. under L1/L2 pressure with 10K-item working set JWT verification cost - every /api/* request does real HMAC-SHA256 crypto at the edge Cache-aside correctness - reads populate cache on miss, writes invalidate, TTL cycles naturally CRUD write path - JSON body parsing, Postgres UPDATE, cache invalidation CPU allocation strategy - how entries balance edge/auth/server on a fixed 64-CPU budget Parameters Parameter Value Endpoints /static/* , /public/baseline , /public/json/{count} , /api/items/{id} (GET+POST), /api/me Connections 256, 1,024 Streams per connection 16 ( -m 16 ) for reads, 8 for writes Duration 5s Runs 3 (best taken) Load generator Two parallel h2load instances (reads + writes) Auth JWT HMAC-SHA256 verified on every /api/* request (no caching) Cache L1 in-process + L2 Redis, \\u22641s TTL for items, \\u226430s for users Item working set 10,000 unique IDs (IDs 1\\u201310,000) Write targets 20 IDs (1\\u201320), overlapping with reads Total CPU budget 64 logical, split across 4 services Port 8443 (HTTPS/h2) Orchestration Docker Compose ( compose.production-stack.yml )"},{"u":"test-profiles/gateway/production-stack/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a Gateway \\u203a Production Stack","d":"The correctness checks validate.sh runs against the four-service Production Stack deployment before a framework\'s results are accepted.","x":"validate.sh brings up the full production-stack compose (edge + cache + authsvc + server) with up --build -d , waits up to 60 seconds for the HTTPS port to respond, runs the checks below, and tears the stack down. Checks performed Protocol and static files HTTP/2 protocol negotiation - curl --http2 /static/reset.css must report HTTP/2 /static/reset.css - Content-Type text/css /static/app.js - non-zero response body Public endpoints (no auth) /public/baseline?a=13&b=42 - returns 55 /public/baseline?a=<random>&b=<random> - correct sum (anti-cheat) /public/json/25 - 25 items, each with the full schema ( id , name , category , price , quantity , active , tags , rating with score + count ) and computed total = price \\u00d7 quantity Auth wall - JWT required GET /api/items/1 (no Authorization header) - HTTP 401 GET /api/items/1 (Authorization: Bearer invalid.token.here) - HTTP 401 POST /api/items/1 (no Authorization header) - HTTP 401 POST /api/items/1 (Authorization: Bearer invalid.token.here) - HTTP 401 Authenticated CRUD - valid JWT GET /api/items/1 with valid JWT - returns item with id=1 Cache-aside cycle - GET /api/items/7 twice: First call: X-Cache: MISS (L1+L2 cold \\u2192 Postgres query \\u2192 populate both layers) Second call: X-Cache: HIT (served from L1 or L2) POST /api/items/2 with valid JWT + JSON body - returns 204 No Content (Postgres UPDATE + cache invalidation) POST invalidation - warm cache for item 3, POST to item 3, GET item 3 again: GET after POST shows X-Cache: MISS (proves RemoveAsync cleared both L1 and L2) GET /api/me with valid JWT - returns user JSON with id=42 (authsvc extracts sub:42 from the JWT, edge forwards X-User-Id: 42 , server reads user from Postgres via HybridCache) Total: 15 production-stack checks plus the compose startup itself. What validation proves JWT verification works end-to-end : valid tokens authenticate, invalid/missing tokens get 401 Both GET and POST are behind the auth wall : can\'t read or write without a valid JWT Cache-aside populates on miss : first read of a cold key hits Postgres and populates cache Cache-aside serves from cache on hit : second read returns cached data POST invalidates cache : write clears both L1 and L2, forcing next read to re-query Postgres User lookup chain works : JWT sub claim \\u2192 authsvc \\u2192 X-User-Id header \\u2192 server \\u2192 Postgres/cache \\u2192 user JSON JWT token for validation Validation uses the pre-generated token at data/jwt-token.txt , signed with the secret httparena-bench-secret-do-not-use-in-production . The token contains {\\"sub\\":\\"42\\",\\"name\\":\\"Alice Chen\\",\\"exp\\":<far-future>} . authsvc verifies the HMAC-SHA256 signature and returns X-User-Id: 42 ."},{"u":"test-profiles/grpc","t":"gRPC","c":"Knowledge Base \\u203a Test Profiles","d":"gRPC benchmark profiles measuring Protocol Buffers over HTTP/2, in both cleartext (h2c) and TLS variants.","x":"gRPC test profiles measure framework performance using Protocol Buffers over HTTP/2. The server listens on port 8080 (h2c) and port 8443 (h2 TLS) and implements the BenchmarkService defined in benchmark.proto . Unary Single unary RPC throughput - GetSum request/response over h2c and h2 TLS."},{"u":"test-profiles/grpc/unary","t":"Unary (gRPC)","c":"Knowledge Base \\u203a Test Profiles \\u203a gRPC","d":"Measures unary gRPC call throughput over cleartext HTTP/2 using a simple GetSum remote procedure call.","x":"Measures unary gRPC call throughput over cleartext HTTP/2 (h2c) using a simple GetSum RPC. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/grpc/unary/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a gRPC \\u203a Unary (gRPC)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the unary gRPC benchmark.","x":"Standard Tuned Engine Must use the standard gRPC library for the language with default configuration. No custom protobuf serialization. May optimize gRPC channel settings, thread pools, and use custom protobuf serialization. No specific rules. Ranked separately from frameworks. Measures unary gRPC call throughput over cleartext HTTP/2 (h2c). The server implements a simple GetSum RPC that adds two integers. Connections: 256, 1,024 Concurrent streams per connection: 100 Proto definition syntax = \\"proto3\\"; package benchmark; service BenchmarkService { rpc GetSum (SumRequest) returns (SumReply); } message SumRequest { int32 a = 1; int32 b = 2; } message SumReply { int32 result = 1; } Workload POST /benchmark.BenchmarkService/GetSum sent as a gRPC unary call over h2c. The load generator ( h2load ) sends pre-encoded protobuf frames with gRPC headers ( content-type: application/grpc , te: trailers ). The request payload is a 9-byte gRPC frame: 5-byte frame header + 4-byte protobuf-encoded SumRequest{a=1, b=2} . What it measures gRPC/HTTP2 frame processing overhead Protocol Buffers serialization/deserialization performance HTTP/2 stream multiplexing efficiency for gRPC Server-side gRPC service dispatch latency Expected request/response POST /benchmark.BenchmarkService/GetSum HTTP/2 content-type: application/grpc te: trailers <binary: 00 00000004 08011002> HTTP/2 200 OK content-type: application/grpc <binary: 00 00000002 0803> grpc-status: 0 How it differs from HTTP/2 baseline Baseline (HTTP/2) Unary (gRPC) Protocol HTTP/2 over TLS gRPC over h2c Serialization Plain text Protocol Buffers Port 8443 8080 Request method GET POST Load generator h2load h2load (raw gRPC frames) Parameters Parameter Value RPC BenchmarkService/GetSum Connections 256, 1,024 Streams per connection 100 ( -m 100 ) Duration 5s Runs 3 (best taken) Load generator h2load"},{"u":"test-profiles/grpc/unary/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a gRPC \\u203a Unary (gRPC)","d":"The correctness checks validate.sh runs against the unary gRPC benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the unary-grpc or unary-grpc-tls tests. validate.sh builds the gRPC frames itself rather than shelling out to a gRPC client, so server reflection is not required . A framework that only registers BenchmarkService without a reflection service still validates. Readiness gRPC servers speak HTTP/2 and never answer the plaintext HTTP/1.1 probe used for most frameworks. For gRPC subscribers the readiness probe is a prior-knowledge h2c request to port 8080 (or an ALPN-h2 TLS request to 8443 for the -tls variant). Any HTTP response counts as ready, and a bare GET to a gRPC server correctly returns 415 . GetSum over HTTP/2 Sends BenchmarkService/GetSum as a unary call with content-type: application/grpc and te: trailers , over h2c on port 8080 ( unary-grpc ) or h2 + TLS on port 8443 ( unary-grpc-tls ). Verifies: The response is delivered over HTTP/2 ( %{http_version} must report 2 ) grpc-status is 0 . A non-zero status fails the check and its grpc-message is reported The SumReply frame decodes to result = a + b Anti-cheat: randomized operands Each call uses freshly randomized a and b (100\\u2013999), and the check runs twice with independent pairs . A server returning a canned reply passes a single fixed input but cannot satisfy two random pairs, so hardcoded responses are rejected. Frame-level strictness The reply is decoded from the raw wire bytes: the 5-byte gRPC frame header (compression flag + big-endian length) must be well formed, the length prefix must match the payload actually delivered, and the protobuf body must carry field 1 as a varint. Truncated frames, a set compression flag, or a wrong field tag are each reported with the offending bytes."},{"u":"test-profiles/h1","t":"H/1.1","c":"Knowledge Base \\u203a Test Profiles","d":"Single-endpoint HTTP/1.1 benchmarks, each isolating one dimension: throughput, JSON, uploads, static files, concurrency, CPU cost and database access.","x":"Every HTTP/1.1 profile measures one thing at a time, so a number can be attributed to a single part of the framework rather than to a mix. Isolated Single-endpoint benchmarks - baseline, JSON, upload, static files, concurrency, CPU cost and database access."},{"u":"test-profiles/h1/isolated","t":"Isolated","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1","d":"Single-endpoint benchmarks that measure one dimension at a time: throughput, JSON serialization, uploads, static files and database access.","x":"Single-endpoint benchmarks that measure framework performance on one task at a time. Baseline Primary throughput benchmark with persistent keep-alive connections and mixed GET/POST workload. Short-lived Connection Connections closed after 10 requests, measuring TCP handshake and connection setup overhead. JSON Processing Loads a dataset, computes derived fields, and serializes a JSON response - testing real-world API workloads. JSON Compressed Same JSON workload with Accept-Encoding: gzip, br and a multiplier parameter - measures serialization plus compression throughput. JSON over TLS Same JSON workload transported over HTTP/1.1 + TLS on port 8081 - measures the cost of encryption on top of serialization. Upload (20 MB) Sends a 20 MB binary payload, server returns byte count. Measures body ingestion throughput. Async Delay A 15ms wait named in the route, at 64K held connections - measures what the framework does while a request waits. Latency-1M One million req/s offered at a fixed rate; the metric is the CPU each framework spends to serve it, read exactly from its cgroup. Async Database (Postgres) Async Postgres range query over 100K rows, connection pooling, and JSON serialization. Framework-only benchmark. Static Files Serves 20 static files - CSS, JS, HTML, fonts, images - over HTTP/1.1. Static Files over TLS Same 20-file static workload transported over HTTP/1.1 + TLS on port 8081 - measures the cost of encryption on bulk file serving. Pipelined (16x) * 16 requests sent back-to-back per connection, testing raw I/O and pipeline batching. Reference-only - not part of the composite score. CRUD (REST API) Realistic REST API with paginated list, cached reads, create, and update against Postgres. Fortunes (Templates) * DB query + HTML template rendering with auto-escape. Reference-only - measures template-engine throughput, not part of the composite score."},{"u":"test-profiles/h1/isolated/async","t":"Async Delay","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"Measures how a framework holds tens of thousands of in-flight requests that are each waiting on a timer, with the wait length parsed from the route.","x":"Every request asks to be answered after a delay it names in its own path, and tens of thousands of them are in flight at once. What the profile measures is what the framework does while it waits. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/async-database","t":"Async Database (Postgres)","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"Measures concurrent asynchronous Postgres queries over a network connection, exercising the framework\'s connection pooling and async I/O.","x":"Measures how efficiently a framework handles concurrent async database queries over a network connection with connection pooling. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/async-database/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Async Database (Postgres)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the async Postgres benchmark.","x":"Standard Tuned Engine Must use an async PostgreSQL driver with standard connection pooling. Size the pool from DATABASE_MAX_CONN (currently 256), not from CPU count. May use custom pool sizes, prepared statement caching, or driver-specific optimizations beyond defaults. No specific rules. The Async Database profile measures how efficiently a framework handles concurrent database queries over a network connection - exercising async I/O scheduling, connection pooling, and async Postgres driver efficiency. This test is for framework-type entries only - engines (nginx, h2o, etc.) are excluded. Reference-only. This profile is measured and published but does not contribute to the composite score. The database and its driver dominate the result far more than the framework does, so the number mostly reports which Postgres driver a language has rather than anything about the HTTP path this board sets out to compare. It is still worth having, it just no longer decides the ranking. Connections: 1,024 How it works A Postgres container runs alongside the framework container on the same host, listening on localhost:5432 The framework reads the DATABASE_URL environment variable at startup and initializes a connection pool On each GET /async-db?min=10&max=50&limit=20 request, the framework: Parses min , max , and limit as integers (defaults: min=10 , max=50 , limit=50 ; limit clamped to 1\\u201350) Executes an async range query with the parameterized LIMIT against the Postgres items table Restructures rating_score and rating_count into a nested rating object Serializes the result as JSON Returns Content-Type: application/json What it measures Async I/O scheduling - how efficiently the event loop handles network round-trips to Postgres while serving concurrent HTTP requests Connection pooling - maintaining and multiplexing a pool of Postgres connections across thousands of concurrent requests Async driver quality - the efficiency of the language\'s async Postgres driver (e.g., asyncpg , tokio-postgres , pg ) Result parsing + JSON serialization - converting Postgres rows to structured JSON with nested objects Database schema The items table in Postgres (100,000 rows): CREATE TABLE items ( id INTEGER PRIMARY KEY, name TEXT NOT NULL, category TEXT NOT NULL, price INTEGER NOT NULL, quantity INTEGER NOT NULL, active BOOLEAN NOT NULL, tags JSONB NOT NULL, rating_score INTEGER NOT NULL, rating_count INTEGER NOT NULL ); -- No index on price - forces sequential scan SQL query SELECT id, name, category, price, quantity, active, tags, rating_score, rating_count FROM items WHERE price BETWEEN $1 AND $2 LIMIT $3 Expected response GET /async-db?min=10&max=50&limit=20 HTTP/1.1 { \\"items\\": [ { \\"id\\": 42, \\"name\\": \\"Alpha Widget 42\\", \\"category\\": \\"electronics\\", \\"price\\": 30, \\"quantity\\": 5, \\"active\\": true, \\"tags\\": [\\"fast\\", \\"new\\"], \\"rating\\": { \\"score\\": 42, \\"count\\": 127 } } ], \\"count\\": N } All numeric fields ( price , quantity , rating.score , rating.count ) are integers - no floats anywhere. The count field must be dynamically computed from the number of returned items, not hardcoded. When Postgres is unavailable or the query returns no rows, return: {\\"items\\":[],\\"count\\":0} Environment variables The benchmark runner provides these environment variables to your container: Variable Value Description DATABASE_URL postgres://bench:bench@localhost:5432/benchmark Postgres connection string. Always read from this - never hardcode. DATABASE_MAX_CONN 256 Maximum connections allowed by the Postgres instance. Use this to size your connection pool. May be lower for CPU-constrained tests (e.g. API-4, API-16). Implementation notes Async driver required - use your language\'s async Postgres driver (e.g., asyncpg for Python, tokio-postgres for Rust, pg for Node.js, r2d2 / deadpool for connection pools) Connection pool - initialize a pool at startup. Size it from DATABASE_MAX_CONN (currently 256). Going higher than that will cause Postgres to reject connections under load Prepared statements - prepare the query once per connection, reuse across requests Default parameters - all three query parameters are integers. If min or max is missing, default to 10 and 50 . If limit is missing, default to 50 . Clamp limit to the range 1\\u201350 Integer types matter - price and rating_score are INTEGER columns. Read them as i32 / int /equivalent - using f64 / double will fail with type-mismatch errors in strict drivers like tokio-postgres Tags are JSONB - Postgres returns them as native JSON, no string parsing needed Important: environment variables and initialization Never hardcode connection details. Always read DATABASE_URL for the connection string and DATABASE_MAX_CONN for pool sizing. The benchmark runner starts Postgres and waits for the seed data to be fully loaded before starting your framework container. By the time your server starts, Postgres is ready and accepting connections. Recommended: lazy initialization with retry. As a safety net, handle the case where the initial connection fails gracefully. Do not crash the server - return the empty fallback response and retry on the next request. # Pseudocode pg_pool = null on_startup: try: pg_pool = connect(DATABASE_URL) catch: pg_pool = null # don\'t crash on_request /async-db: if pg_pool is null: try: pg_pool = connect(DATABASE_URL) catch: return {\\"items\\":[],\\"count\\":0} try: return query(pg_pool) catch: pg_pool = null; return {\\"items\\":[],\\"count\\":0} Parameters Parameter Value Endpoint GET /async-db?min=X&max=Y&limit=N Limits 5, 10, 20, 35, 50 (rotated with -r 25 ) Connections 1,024 Pipeline 1 Duration 10s Runs 3 (best taken) Database Postgres 18 (Debian, glibc), 100,000 rows, no index on price"},{"u":"test-profiles/h1/isolated/async-database/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Async Database (Postgres)","d":"The correctness checks validate.sh runs against the async Postgres benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the async-db test. A Postgres sidecar container is started automatically before these checks run. Response structure with varying limits Sends GET /async-db?min=M&max=N&limit=L with varying price ranges and limits (different from the benchmark values to prevent hardcoded responses). Tested combinations: min=5&max=80&limit=7 , min=20&max=150&limit=18 , min=100&max=400&limit=33 , min=10&max=50&limit=50 . For each request, verifies: Item count equals the requested limit Every item has a nested rating object with a score field Every item has a tags field that is an array Every item has an active field that is a boolean ( true / false ) Content-Type header Sends GET /async-db?min=10&max=50&limit=50 and verifies the Content-Type response header is application/json . Anti-cheat: empty range Sends GET /async-db?min=9999&max=9999&limit=50 (a price range with no matching items) and verifies the response has count equal to 0 . This detects hardcoded responses or implementations that ignore the query parameters."},{"u":"test-profiles/h1/isolated/async/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Async Delay","d":"Endpoint contract, response shape, and the anti-cheat constraints a framework must satisfy for the async delay benchmark.","x":"Standard Tuned Engine Bind /delay/{ms} with the framework\'s own router and wait with the framework\'s own idiomatic mechanism. Blocking the request\'s thread for the duration is permitted - it is a legitimate implementation and the profile exists to price it - but you may not answer before the delay has elapsed, and the delay must be read from the path on every request. May use a custom timer wheel, a dedicated timer thread, coarser timer granularity, or batched expiry, so long as no response leaves before its own delay has elapsed. Must derive the wait from the request path with a real timer. A fixed configured interval, a static delay directive, or anything that answers without reading {ms} is not an implementation of this profile. The Async Delay profile measures what a framework does with a request it cannot answer yet. Every other HTTP/1.1 profile here is answered the moment it arrives; the only thing separating frameworks is how fast they can do that. Real services do not work that way. They call a database, a cache, another service, and while that call is outstanding the request is idle. A framework that suspends the request and keeps the thread carries thousands of those concurrently. A framework that blocks the thread carries as many as it has threads. This profile isolates that difference and nothing else. There is no database, no network hop, no serialization, no I/O of any kind - just a timer, so the number that comes out is a property of the framework\'s concurrency model rather than of a driver someone else wrote. Connections: 64,000 How it works Each request is GET /delay/{ms} , where {ms} is a whole number of milliseconds The framework parses {ms} from the path The framework waits that long The framework responds 200 with the parsed number as the body and Content-Type: text/plain The load generator holds every connection open for the whole run, so the server has 64,000 requests outstanding at all times, each with its own deadline. Expected response GET /delay/37 HTTP/1.1 37 with Content-Type: text/plain . The body is the parsed integer, in decimal, with no surrounding whitespace or JSON. GET /delay/0 is a valid request that must answer immediately with 0 - zero is a delay of zero, not a missing parameter. Parameters Parameter Value Endpoint GET /delay/{ms} Delay 15 ms Connections 64,000 Pipeline 1 Requests per connection unlimited (connections are held for the whole run) Duration 10s Runs 3 (best taken) The delay is flat, so the benchmark itself is not doing any anti-cheat work with the value - all of that lands on validation, which draws its own delays after the container is already running and asserts that the response time follows them. The throughput ceiling A connection cannot have more than one request in flight, and each request occupies its connection for at least its delay. So the whole profile has an arithmetic ceiling: max rps = connections / mean(delay) At 15 ms and 64,000 connections that is 4.27M rps , just under the fastest number any entry has ever posted on the plain baseline profile. Nothing measured here comes close - the best so far is 2.24M - so the ceiling does not bind, and what the profile reports is the framework\'s own capacity. It still works as a free correctness check in the other direction. Nothing can exceed the ceiling while honouring the delay, so a result above it is not a fast server, it is a server that did not wait. For a blocking implementation the ceiling is much lower, and it is set by the thread count rather than the connection count: max rps = threads / mean(delay) A 64-thread server at 15 ms tops out near 4,270 rps no matter how many connections are offered - against 2.24M for the best async entry measured so far. Note which way the delay moves that gap. Shortening it frees a blocked thread sooner and raises the blocking ceiling proportionally, so a longer delay is what separates the two models most sharply. The delay is chosen to keep that separation wide while still leaving the async ceiling clear of what any framework can actually deliver. Implementation notes Prefer a real async wait. await Task.Delay(ms) , tokio::time::sleep , asyncio.sleep , setTimeout , time.After , a suspended coroutine - whatever your framework\'s own idiom is. These cost a timer entry and keep the worker free Do not Thread.sleep on an event loop. On a single-threaded or thread-per-core runtime this stalls every other connection that thread owns, not just this request. It is the one shape that will look worse here than a plain thread-per-request server Per-request state. The delay belongs to the request. A field on the server, the connection, or a shared handler instance is read back by whichever request finishes parsing last, and validation runs 32 overlapping requests with 32 different delays specifically to find that Timer granularity is priced in. The delay is 15 ms, so a runtime that habitually overshoots by 3-4 ms is handing back a quarter of the wait before it has served a single request. That cost is real and the profile charges for it. Validation never asserts an upper bound on a single response, though - only the benchmark prices it Do not create a thread per request. At 64,000 concurrent requests that is 64,000 threads. Frameworks without an async model should block on whatever pool they already have and accept the result - see the ceiling above No I/O. Do not sleep by polling a socket, opening a file, or querying anything. The profile is a timer and nothing else Scoring This profile is currently reference-only : it is measured, published and shown on the board, but it does not contribute to the composite score while its delay range and connection counts are still being tuned ( #1310 ). When it is scored, it will count for framework entries (flagship, emerging, experimental) and for engines. Infrastructure entries are excluded - a reverse proxy has no application handler to wait in, so the thing being measured does not exist for that tier."},{"u":"test-profiles/h1/isolated/async/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Async Delay","d":"The correctness checks validate.sh runs against the async delay benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the async test. None of them care how the wait was implemented. Blocking the request\'s thread is a permitted implementation on this profile - it is meant to lose the benchmark, not to fail validation - so every timing assertion here is a lower bound on a single response, never a bound on total throughput or on wall time. Randomized delay Sends GET /delay/{ms} with {ms} drawn fresh between 10 and 90. Verifies: the response is 200 the body is the requested number the response did not arrive before the requested delay had elapsed Because the value is chosen after the container is up, nothing prepared at startup can answer it. The benchmark itself asks for a flat 15 ms, so this section carries all of the anti-cheat weight for the parameter. Content-Type header Verifies the Content-Type response header is text/plain . Zero delay Sends GET /delay/0 and verifies the body is 0 . Zero is a delay of zero, not a missing parameter; an implementation that treats a falsy value as absent and substitutes a default answers with the wrong number here. The delay tracks the parameter Sends GET /delay/10 and GET /delay/500 and requires the second to take at least 300 ms longer than the first. Echoing the parameter back in the body proves it was parsed. This proves it reached the sleep. An implementation that returns the right number after a fixed wait passes every other check in this section and fails this one. Not a constant long sleep Requires GET /delay/10 to complete within 2 seconds. This is the only upper bound in the section and it is deliberately loose. It exists to catch a delay that ignores the parameter in the other direction - a fixed multi-second sleep would otherwise satisfy every lower bound above. It is not a measurement of timer precision. 32 overlapping requests, 32 different delays Issues 32 requests at once, each carrying a different delay between 100 ms and 499 ms, and verifies that every one of them comes back 200 , echoes its own value, and took at least its own delay. This is the check a per-server or per-connection global cannot pass. Storing \\"the current delay\\" in one place answers every sequential check above correctly and falls apart as soon as two requests overlap, which is the only state this profile is ever benchmarked in. The elapsed wall time for the batch is printed alongside the result. It is reported, not asserted: a thread-per-request server legitimately takes the sum of the delays here."},{"u":"test-profiles/h1/isolated/baseline","t":"Baseline","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"The primary throughput benchmark: one request at a time per connection over persistent keep-alive connections.","x":"The primary throughput benchmark. Each connection sends one request at a time over persistent keep-alive connections. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/baseline/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Baseline","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the HTTP/1.1 baseline throughput benchmark.","x":"Standard Tuned Engine Infrastructure Must use the framework standard HTTP server with default configuration. No custom TCP tuning, no experimental flags, no worker count beyond framework defaults. May adjust worker counts, thread pools, TCP socket options, and use framework-specific performance flags. Custom buffer sizes allowed. No specific rules. Ranked separately from frameworks. Configuration is free - worker counts, buffer sizes, event-loop and socket tuning are all allowed. The endpoint must be answered by a module or handler loaded into the server itself, not proxied to an application process behind it - an upstream app makes it a gateway entry, not an infrastructure one. The primary throughput benchmark. Each connection sends one request at a time over persistent keep-alive connections. Connections: 512, 4,096 CPU limit: 64 threads (container pinned to cores 0-31, 64-95 via --cpuset-cpus ) Workload A mix of three request types, rotated across connections: GET /baseline11?a=13&b=42 - query parameter parsing, response: sum of values POST /baseline11?a=13&b=42 with Content-Length body - query params + body parsing POST /baseline11?a=13&b=42 with chunked Transfer-Encoding body - chunked decoding This exercises the full HTTP handling path: request line parsing, header parsing, query string extraction, body reading (both Content-Length and chunked), integer arithmetic, and response serialization. Expected request/response GET - sum of query parameters: GET /baseline11?a=13&b=42 HTTP/1.1 HTTP/1.1 200 OK Content-Type: text/plain 55 POST with body - sum of query parameters + body: POST /baseline11?a=13&b=42 HTTP/1.1 Content-Type: text/plain Content-Length: 2 20 HTTP/1.1 200 OK Content-Type: text/plain 75 What it measures Raw request throughput under ideal conditions Full HTTP parsing pipeline performance Keep-alive connection handling efficiency How frameworks scale with increasing connection counts"},{"u":"test-profiles/h1/isolated/baseline/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Baseline","d":"The correctness checks validate.sh runs against the HTTP/1.1 baseline throughput benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the baseline or limited-conn test. GET with query parameters Sends GET /baseline11?a=13&b=42 and verifies the response body is 55 (sum of a and b ). POST with Content-Length body Sends POST /baseline11?a=13&b=42 with body 20 and Content-Type: text/plain . Verifies the response body is 75 (sum of a , b , and body). POST with chunked Transfer-Encoding Sends POST /baseline11?a=13&b=42 with body 20 and Transfer-Encoding: chunked . Verifies the response body is 75 . Anti-cheat: randomized query parameters Generates random values for a and b (100-999), sends GET /baseline11?a={a}&b={b} , and verifies the response matches the expected sum. This detects hardcoded responses. Anti-cheat: POST body cache detection Sends two POST requests with different random body values to the same endpoint ( /baseline11?a=13&b=42 ). Verifies each response reflects the correct sum for that specific body. This detects response caching or hardcoded POST handling. Request 1: body= {random1} - expects 13 + 42 + random1 Request 2: body= {random2} - expects 13 + 42 + random2 TCP fragmentation Each request below is sent over a raw TCP socket ( TCP_NODELAY , no Nagle coalescing) in multiple sendall() writes with a 30 ms pause between fragments. Every framework\'s HTTP parser must reassemble these partial reads and produce the correct response. Simulates realistic network behavior - slow clients, small MTU, intermediate proxies that chunk data. Every fragmented request sets Connection: close so the server closes the socket after the response and the test can read until EOF. Split request line - the request line arrives in two halves ( \\"GET /baseli\\" + \\"ne11?a=13&b=42 HTTP/1.1\\\\r\\\\n\\u2026\\" ). The parser sees an incomplete method/path on the first recv() . Expects body 55 . Split before headers - the request line arrives in one write, then each header line arrives in its own write ( Host: , User-Agent: , Connection: ). Expects body 55 . POST split headers/body - the full header block (including terminating \\\\r\\\\n\\\\r\\\\n ) is one write, then the body arrives in a separate write after the pause. Expects body 75 . POST split body bytes - headers in one write, then the 2-byte body ( \\"20\\" ) arrives as two 1-byte writes. Stresses the parser\'s ability to reassemble a Content-Length body across multiple recv() calls. Expects body 75 . POST lower-cased field names - the same POST, spelled host: , content-type: , content-length: , connection: . HTTP field names are case-insensitive (RFC 9110 \\u00a75.1), so a parser that only matches Content-Length would not find the body length. Only meaningful over HTTP/1.1 - HTTP/2 and HTTP/3 mandate lowercase on the wire. Expects body 75 . Exhaustive TCP fragmentation The splits above land where a person chose to put them, which only covers the boundaries someone thought of. This check sweeps every one: validate-frag.py takes nine request shapes and splits each at every byte offset - roughly 1,000 in total - then requires HTTP 200 with the exact expected body on all of them. For each offset it opens a connection, writes the first part, pauses 200 ms , writes the rest, and reads to EOF. The pause is the substance of the test: it guarantees the server\'s read loop returns holding a partial request and has to carry parser state across recv() calls, rather than the kernel coalescing both writes into one segment. Connections are opened in batches of 384 and each batch pays the pause once, so the whole sweep runs in about two seconds. Every shape carries a ?a=\\u2026&b=\\u2026 query string, so query parsing sits on the split path too - not just the request line and headers. The nine shapes: Shape Expected GET , minimal headers 55 GET , several headers ( User-Agent , Accept , Accept-Encoding ) 55 GET , lower-cased field names 55 GET , randomized a and b a + b POST , Content-Length body 75 POST , lower-cased content-length 75 POST , randomized query and body a + b + body POST , chunked body in one chunk 75 POST , chunked body in two chunks 75 Two of the shapes randomize their operands, so a hardcoded or cached response fails here as well as in the anti-cheat checks above. The offsets that matter are the ones nobody picks by hand: between the \\\\r and the \\\\n of a header line, midway through the Content-Length digits, inside the chunk-size hex, and one byte into the terminating 0\\\\r\\\\n\\\\r\\\\n . The chunked shapes are the only place in the suite where a chunked body is fragmented at all - the chunked check above sends its request through curl in a single write, and the hand-picked splits only ever fragment Content-Length bodies. The approach is borrowed from uWebSockets\' fragment_test.ts , which does the same byte-by-byte sweep. Two differences here: the response body is asserted rather than just the status code, since a server can answer 200 with the wrong sum; and connections are batched so one pause covers a batch instead of one offset."},{"u":"test-profiles/h1/isolated/crud","t":"CRUD (REST API)","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"A realistic REST API workload against Postgres: paginated list, cached single-item read, create and update, with cache-aside invalidation.","x":"Realistic REST API benchmark with paginated list, cached single-item read, create, and update operations against Postgres. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/crud/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a CRUD (REST API)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the CRUD REST API benchmark.","x":"Standard Tuned Engine Must use a standard async Postgres driver with connection pooling. Cache-aside with 200 ms absolute TTL on single-item reads, invalidated on PUT. In-process cache is the default; multi-process runtimes (e.g. SO_REUSEPORT with one process per core) may use the provided Redis sidecar for a shared cache. No pre-warming or background refresh. May use custom pool sizes, prepared statements, multi-tier caches, or any cache backend including the Redis sidecar. No specific rules. The CRUD profile benchmarks a realistic REST API with four operations against Postgres: paginated list, cached single-item read, create (upsert), and update with cache invalidation. Reference-only. This profile is measured and published but does not contribute to the composite score. The database and its driver dominate the result far more than the framework does, so the number mostly reports which Postgres driver a language has rather than anything about the HTTP path this board sets out to compare. It is still worth having, it just no longer decides the ranking. This test is for framework-type entries only - engines (nginx, h2o, etc.) are excluded. Connections: 4096 Endpoints GET /crud/items - Paginated list Accepts query parameters: category (string, default \\"electronics\\" ) - filter by category page (integer, default 1 ) - page number (1-indexed) limit (integer, default 10 , max 50 ) - items per page Executes a single SQL query: SELECT ... FROM items WHERE category = $1 ORDER BY id LIMIT $2 OFFSET $3 \\"Load-more\\" pagination: the response returns the items on the requested page. The total field reports the number of items in this response (equivalent to the client\'s items.length ) - not the full-filter row count. A separate SELECT COUNT(*) was originally part of the spec but removed because concurrent writes kept the visibility map dirty, making it ~90% of Postgres CPU for a value most real-world list APIs either skip or compute out-of-band. Returns: { \\"items\\": [ { \\"id\\": 42, \\"name\\": \\"Alpha Widget 42\\", \\"category\\": \\"electronics\\", \\"price\\": 30, \\"quantity\\": 5, \\"active\\": true, \\"tags\\": [\\"fast\\", \\"new\\"], \\"rating\\": { \\"score\\": 42, \\"count\\": 127 } } ], \\"total\\": 10, \\"page\\": 1, \\"limit\\": 10 } GET /crud/items/{id} - Single item read (cached) Reads a single item by ID using a cache with 200 ms absolute expiration. Cache backend options: In-process (e.g. IMemoryCache , HashMap , sync.Map ) - the default. Works for single-process frameworks and for multi-threaded runtimes that share a heap. Redis (the provided sidecar at REDIS_URL ) - for multi-process frameworks (Bun with reusePort , PHP FPM, Python with uWSGI workers, etc.) where a per-process cache would fragment the working set. Cache the pre-serialized JSON body so HIT responses skip re-serialization. Behavior: On cache miss: query Postgres, populate cache, return item with X-Cache: MISS header On cache hit: return cached item with X-Cache: HIT header If item not found: return HTTP 404 { \\"id\\": 1, \\"name\\": \\"Alpha Widget 1\\", \\"category\\": \\"electronics\\", \\"price\\": 30, \\"quantity\\": 5, \\"active\\": true, \\"tags\\": [\\"fast\\", \\"new\\"], \\"rating\\": { \\"score\\": 42, \\"count\\": 127 } } POST /crud/items - Create item Accepts a JSON body: { \\"id\\": 200001, \\"name\\": \\"New Product\\", \\"category\\": \\"test\\", \\"price\\": 25, \\"quantity\\": 10 } Inserts into Postgres with ON CONFLICT (id) DO UPDATE (upsert). Returns HTTP 201 with the created item. PUT /crud/items/{id} - Update item Accepts a JSON body: { \\"name\\": \\"Updated Name\\", \\"price\\": 30, \\"quantity\\": 5 } Updates the item in Postgres and invalidates the cache entry for the updated ID (from whichever cache backend is in use). Returns HTTP 200 with the updated fields. Returns 404 if the item doesn\'t exist. What it measures Connection pooling - maintaining a Postgres connection pool under mixed read/write load Cache-aside correctness - cache hit/miss with TTL-based expiration and write-through invalidation, whether the cache is in-process or external Query diversity - paginated list queries mixed with high-rate single-item lookups, inserts, and updates running concurrently JSON parsing and serialization - deserializing request bodies (POST/PUT) and serializing response payloads Workload mix The load generator (gcannon) rotates across 20 raw HTTP templates. Each connection reconnects every 200 requests ( -r 200 ) so templates rotate within a connection\'s lifetime rather than sticking to whichever template it first picked. Operation Templates Weight ID distribution Read (GET /crud/items/{id}) 15 75% Random IDs via {RAND:1:50000} Update (PUT /crud/items/{id}) 3 15% Random IDs via {RAND:1:50000} List (GET /crud/items) 1 5% category=office , random page via {RAND:1:10} (OFFSET \\u2208 0..90) Create (POST /crud/items) 1 5% Sequential IDs via {SEQ:100001} - iteration 1 is pure INSERT, iterations 2+ upsert via ON CONFLICT (gcannon resets the counter per invocation) Environment variables Variable Value Description DATABASE_URL postgres://bench:bench@localhost:5432/benchmark Postgres connection string DATABASE_MAX_CONN 256 Hint for pool sizing. Frameworks should honor this rather than hardcoding. REDIS_URL redis://localhost:6379 Optional - present when the harness runs the crud profile. Use only if the framework\'s architecture (multi-process) needs a shared cache. Parameters Parameter Value Connections 4096 Pipeline 1 Requests per connection 200 (then reconnect) CPU limit (server) 62 threads (cores 1-31, 65-95) CPU limit (Redis sidecar, when used) 2 threads (cores 0, 64), --io-threads 2 Duration 15s per run Runs 3 (best taken) Database Postgres 18 (Debian/glibc), 100,000 rows, max_connections=256 , tmpfs-backed"},{"u":"test-profiles/h1/isolated/crud/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a CRUD (REST API)","d":"The correctness checks validate.sh runs against the CRUD REST API benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the crud test. A Postgres sidecar container is started automatically before these checks run. List pagination Sends GET /crud/items?category=electronics&page=1&limit=5 and verifies: Exactly 5 items returned total field is present and greater than 0 (with load-more semantics, total equals the returned item count, so a successful response with 5 items satisfies this) page equals 1 Every item has a nested rating object Single item read Sends GET /crud/items/1 and verifies the response contains id equal to 1 . Cache-aside (MISS then HIT) Sends two consecutive GET /crud/items/42 requests and checks the X-Cache response header: First request must return X-Cache: MISS Second request must return X-Cache: HIT Not found Sends GET /crud/items/999999 and verifies the response status is HTTP 404. Create (POST) Sends POST /crud/items with body {\\"id\\":200001,\\"name\\":\\"Bench Test\\",\\"category\\":\\"test\\",\\"price\\":25,\\"quantity\\":10} and verifies: Response status is HTTP 201 Read back created item Sends GET /crud/items/200001 and verifies the response contains id equal to 200001 . Update with cache invalidation Warms the cache with GET /crud/items/200001 Sends PUT /crud/items/200001 with updated fields and verifies HTTP 200 Sends GET /crud/items/200001 and verifies X-Cache: MISS (cache was invalidated by the PUT)"},{"u":"test-profiles/h1/isolated/fortunes","t":"Fortunes (Templates)","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"Database-backed HTML rendering end to end: a Postgres read, runtime row injection, sorting and escape-aware template output.","x":"Database-backed HTML rendering benchmark - measures template-engine throughput end-to-end: Postgres read, runtime row injection, sort, escape-aware HTML rendering. Reference-only profile (not part of the composite score). Implementation Guidelines Endpoint specification, expected output, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/fortunes/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Fortunes (Templates)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the Fortunes template rendering benchmark.","x":"Standard Tuned Engine Must render the response with a real template engine - one with a documented templating syntax, variable interpolation, control flow (loop/if), and HTML auto-escaping by default. Examples: Razor / Razor Pages / Razor Components, Jinja2, ERB, Twig, Blade, html/template, Thymeleaf, Freemarker, Handlebars, Pug, Liquid, Tera, Askama, Maud, templ. The template must be a separate artifact (file, embedded resource, or compile-time component) - not inline string concatenation in the handler. No pre-rendered or cached response bodies; the template must execute per request. May use any rendering strategy, including hand-rolled string concatenation, custom HTML emitters, StringBuilder / Buffer-based builders, byte-slice writers, or compile-time templating with framework-specific source generators. The handler must still query the database, append the runtime-injected row in memory, sort, and HTML-escape user content per request - pre-rendered response bodies and bypass-the-engine response caches are not allowed on either type. The runtime row\'s text is fixed, but the page still must be rendered per request because validation reads the body byte-for-byte. No specific rules. Engines without an HTTP application layer typically don\'t subscribe to this profile. The Fortunes profile measures template-engine throughput on a realistic page-rendering pipeline: query 200 rows from Postgres, append a runtime row in memory, sort, and render an HTML page via the framework\'s chosen template engine. Diverges from TechEmpower Fortunes (12-row spec) by design. With only 12 rows the per-request render time is dwarfed by the PG round-trip, so the profile ends up measuring the database driver more than the template engine. Bumping the seed to 200 rows scales render cost linearly while leaving query cost effectively constant (still one round-trip, still single-page table read), shifting the bottleneck from PG-throughput to framework-CPU - which is what the profile name actually claims to measure. This test is reference-only - it does not contribute to the composite score. It exists for engine-to-engine comparison (Razor vs Jinja2 vs ERB vs Askama, etc.), not framework ranking. Frameworks without a template engine should leave it out of meta.json.tests . Connections: 1,024 How it works A Postgres container runs alongside the framework container on the same host, listening on localhost:5432 . The same connection pool used by async-db / crud is shared. On each GET /fortunes request the framework: Reads all 200 rows from the fortune table ( id , message ). Appends a runtime row in memory: { id: 0, message: \\"Additional fortune added at request time.\\" } - total 201 rows. Sorts the combined list by message using ASCII / ordinal byte order (not locale-aware). Renders an HTML page containing a <table> with one <tr> per fortune. Returns Content-Type: text/html (with optional ; charset=utf-8 ). The runtime-injected row is the load-bearing design decision - it forces the rendered HTML to differ structurally from the raw DB result, which makes \\"cache the whole page once and serve bytes\\" optimizations invalid. What it measures Template engine throughput - interpolation, loop expansion, layout/partial composition. HTML escape correctness - row 11 contains a raw <script> tag in the database; the engine must emit it as &lt;script&gt; in the rendered output. Auto-escape is the safe default in every modern engine; this profile pins that down as a contractual requirement. End-to-end pipeline cost - Postgres read + sort + render + response write, all on one event-loop tick. Database schema The fortune table in Postgres (200 rows seeded; same Postgres instance as async-db / crud ): CREATE TABLE fortune ( id INTEGER PRIMARY KEY, message TEXT NOT NULL ); Rows 1\\u201312 match the TechEmpower Fortunes seed (including the <script> row at id 11, which is the load-bearing escape check). Rows 13\\u2013200 are synthetic adages generated to give the renderer per-row work - each contains & , \' , \\" , and an em-dash so the engine\'s escape codepath runs on every cell, not just on row 11. See data/pgdb-seed.sql for the exact rows. SQL query SELECT id, message FROM fortune No ORDER BY - the framework sorts in-memory after appending the runtime row. Runtime row injection After the DB query and before sorting, append: { id: 0, message: \\"Additional fortune added at request time.\\" } The id 0 and the literal text are fixed across all frameworks for validation parity. Sort Sort the combined 201-row list by message using ordinal / byte / ASCII comparison. With the seed data this puts the <script>... row first ( < is 0x3C, lower than any letter) and the multi-byte UTF-8 rows last. Locale-aware comparison is not allowed because it produces different orderings on different runtimes - use the ordinal/byte equivalent of your language\'s String.CompareOrdinal / bytes.Compare / strcmp . Expected response GET /fortunes HTTP/1.1 A complete HTML document with: Content-Type: text/html (charset optional but recommended) <!DOCTYPE html> declaration A <table> with one header row ( <tr><th>id</th><th>message</th></tr> ) and 201 data rows. All message values HTML-escaped - row 11 must render as &lt;script&gt;...&lt;/script&gt; , never as a raw <script> tag. Total body size lands between roughly 18\\u201340 KB depending on engine formatting. Validation does not enforce byte-for-byte equality - it checks features (Content-Type, DOCTYPE, row count, runtime-row text, escape correctness, body size band). See Validation for exact checks. Production vs Tuned Production entries should look like idiomatic templated HTML in the framework - a .cshtml Razor page, a Jinja2 template loaded via flask.render_template , an ERB view in Sinatra, a templ component in Go, etc. The template lives in its own file and is composed (or compiled) by the engine. Auto-escaping is on; user content goes through @variable / {{ variable }} syntax that escapes by default. Tuned entries are free to skip the engine entirely - emit HTML via StringBuilder.Append , bytes.Buffer.WriteString , manual << concatenation, custom byte-slice writers. This mirrors what many TechEmpower Fortunes entries do to chase peak throughput. The handler must still: Query the DB per request (no pre-rendered response cache). Append the runtime row (the page must vary per request relative to a hypothetical cache that snapshots only the DB rows). HTML-escape user content correctly. A custom emitter must implement escaping for < , > , & , \\" , \' at minimum. The Standard/Tuned distinction is recorded in meta.json.mode and shown on the leaderboard so users can compare \\"what the framework\'s template engine does\\" against \\"what the framework\'s hand-tuned hot path does.\\" Why this profile is unscored Template engine choice dominates the result far more than HTTP-stack quality. A framework using a compile-time engine like Razor source-gen, Askama, Maud, or templ will look 5\\u201310\\u00d7 faster than the same framework using a runtime-parsed engine like Jinja2 or ERB. That gap is real and worth showing - but rolling it into the composite would let template-engine choice swamp legitimate framework comparisons on baseline / JSON / async-db. So Fortunes is published, comparable, and visible on the leaderboard, but does not contribute to the composite score. Why 200 rows instead of TE\'s 12 TechEmpower Fortunes uses 12 rows - at that size, the per-request render takes microseconds and the dominant cost is the PG round-trip + network protocol, not the template engine. Frameworks with the same template engine but different DB drivers end up ranked by DB driver, defeating the profile\'s purpose. Bumping to 200 rows scales render cost linearly (more interpolation, more escape work, more bytes to write) while leaving query cost effectively flat (the table still lives in a single 8 KB shared-buffer page; one round-trip serializes 200 rows nearly as fast as 12). At 1024 connections the bottleneck shifts from PG-throughput to framework-CPU - i.e. the actual template engine. That\'s the trade: lose direct TE leaderboard comparability, gain a column that means what its name says."},{"u":"test-profiles/h1/isolated/fortunes/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Fortunes (Templates)","d":"The correctness checks validate.sh runs against the Fortunes template rendering benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the fortunes test. A Postgres sidecar container is started automatically before these checks run. Validation is feature-based, not byte-exact . Engines disagree on whitespace, attribute order, quote style, and self-closing tags - enforcing byte equality would turn the contest into \\"who can torture their template into emitting the canonical form\\" rather than \\"whose engine renders fast.\\" The checks below pin down the work envelope (loop iterated, runtime row appended, escape applied, layout rendered, body has a real size) without dictating output format. Content-Type header Sends GET /fortunes and verifies the Content-Type response header contains text/html (a ; charset=utf-8 suffix is allowed and recommended). DOCTYPE present Verifies the response body contains <!DOCTYPE html> (case-insensitive). This proves a layout / partial / outer template fired - a handler that only emits a fragment will fail this check. Row count Counts <tr occurrences in the response body. The expected band is 201\\u2013210 : 201 data rows (200 seeded + 1 runtime-injected) are required. A <tr> header row ( <tr><th>id</th><th>message</th></tr> ) is allowed but not required. The upper bound absorbs implementation-specific extras (a footer summary row, etc.) without forcing a specific shape. A count below 201 means the loop didn\'t iterate every row. Above 210 means the engine emitted unexpected scaffolding. Runtime-injected row present Verifies the body contains the literal string Additional fortune added at request time. . This proves the handler appended the in-memory row instead of rendering only the DB rows. Without this check, \\"cache the rendered HTML once and serve bytes\\" passes everything else. XSS escape (load-bearing) The DB seed contains <script>alert(\\"This should not be displayed in a browser alert box.\\");</script> as the message of row 11. The validator asserts: Body contains &lt;script&gt; (the escape happened), AND Body does NOT contain the raw <script>alert substring anywhere. This is the most important check in the profile. Without it, a \\"template engine\\" that skips escaping wins the bench - which is both a security regression and the obvious gaming vector. Frameworks failing this check are not running a real template engine, full stop. Body size band Verifies the response body length is between 18 KB and 64 KB . A 201-row HTML table with a layout typically lands around 22\\u201330 KB; the band catches: Stripped pages (handler returned only fragments to win throughput). Empty bodies (template failed silently and the engine emitted nothing). Pathologically large output (an unintended layout loop or duplicated content). The upper bound is generous - engines that emit more whitespace, longer DOCTYPEs, or a bigger header / footer will still pass. What is NOT validated Sort order. The implementation guidelines specify ordinal-byte sort, but the validator does not parse the rendered HTML and verify row positions. Frameworks shipping a different sort still pass - the production rules govern correctness here, not the validator. Exact byte output. Two correct implementations can produce different HTML and both pass. Header row presence. A <tr><th>...</th></tr> header row is recommended for parity with the reference rendering but not enforced. Performance under load. Validation is correctness-only; the benchmark driver measures throughput separately."},{"u":"test-profiles/h1/isolated/json-compressed","t":"JSON Compressed","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"The JSON workload under content negotiation: the client advertises gzip and Brotli, and scoring accounts for the response size achieved.","x":"Same workload as JSON Processing , but with HTTP content negotiation: the client advertises Accept-Encoding: gzip, br and the server must return a compressed response. Measures serialization plus compression throughput. Implementation Guidelines Endpoint specification, compression rules, and multiplier parameter. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/json-compressed/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a JSON Compressed","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the compressed JSON benchmark.","x":"Standard Tuned Engine Must use the framework standard JSON serialization and the framework or engine\'s built-in response compression (middleware, filter, or equivalent). No pre-compressed caches, no bypassing the response pipeline. May use alternative JSON libraries, tuned compression libraries, and framework-specific optimizations as long as the output is valid gzip or brotli. The JSON body must still be serialized and compressed per request - pre-computed / pre-serialized / pre-compressed response caches are not allowed on either type; they defeat the serialization and compression workload the profile exists to measure. No specific rules. The JSON Compressed profile is the same workload as JSON Processing with one difference: the client sends Accept-Encoding: gzip, br and the server must return a compressed response with a matching Content-Encoding header. How it works Server reads /data/dataset.json at startup (same 50-item dataset as JSON Processing) On each GET /json/{count}?m={multiplier} request, the server: Takes the first count items from the dataset (1\\u201350) Computes total = price \\u00d7 quantity \\u00d7 m per item Serializes to JSON Compresses the response body with gzip or brotli Returns Content-Type: application/json and Content-Encoding: gzip (or br ) When the client does not send Accept-Encoding , the server must not set Content-Encoding - compression is per-request, driven by the client header The benchmark round-robins across counts 25, 40, and 50 paired with multipliers 4, 8, and 6. What it measures Everything JSON Processing measures Response compression throughput - gzip or brotli encoding of the serialized body Content negotiation - honoring Accept-Encoding per request Framework compression middleware overhead - how cheaply the framework wires compression into the response pipeline Expected response For GET /json/5?m=3 with Accept-Encoding: gzip, br : HTTP/1.1 200 OK Content-Type: application/json Content-Encoding: gzip Decompressed body: { \\"items\\": [ { \\"id\\": 1, \\"name\\": \\"Alpha Widget\\", \\"category\\": \\"electronics\\", \\"price\\": 328, \\"quantity\\": 15, \\"active\\": true, \\"tags\\": [\\"fast\\", \\"new\\"], \\"rating\\": { \\"score\\": 48, \\"count\\": 127 }, \\"total\\": 14760 } ], \\"count\\": 5 } total is price * quantity * m - integer arithmetic, no rounding. For GET /json/5?m=1 , total equals price * quantity ; the multiplier is never implicitly 1. Parameters Parameter Value Endpoint GET /json/{count}?m={multiplier} Counts \\u00d7 multipliers (25,4), (40,8), (50,6) (round-robin) Connections 512, 4096, 16384 Pipeline 1 Duration 5s Runs 3 (best taken) Request headers Accept-Encoding: gzip, br Dataset 50 items, mounted at /data/dataset.json Scoring Unlike most profiles where rank follows raw requests per second, JSON Compressed is scored by a compression-weighted formula so a framework that ships smaller response bodies wins over one that only serves more requests. This is the only profile in the suite that explicitly rewards compression ratio. The formula For each framework, the server\'s actual bytes-per-response is computed from the benchmark output: myBpr = observed_bandwidth / observed_rps The framework with the smallest myBpr across the field sets the reference - that\'s the compression winner. Every framework\'s effective rps is then scaled by the squared ratio of reference to actual: effectiveRps = rps \\u00d7 (minBpr / myBpr)\\u00b2 The quadratic exponent is deliberate: doubling the response size quarters the score . A framework that serves twice the rps but ships 1.4\\u00d7 the bytes roughly breaks even; one that ships 2\\u00d7 the bytes pays 4\\u00d7 in score and needs to match 4\\u00d7 the rps just to stay level. The BW/req column on the leaderboard shows each framework\'s myBpr so you can see exactly what compression ratio the server picked (gzip at its chosen level, brotli, deflate - whatever Accept-Encoding: gzip, br returned). Per-conn-count table (per-profile leaderboard) On the JSON Compressed tab of the main leaderboard, the Score column is computed independently per connection count . Each conn panel (512, 4096, 16384) runs its own minBpr normalization so the top framework in that panel scores 100. The Best panel picks each framework\'s highest score across the three conn counts. Composite score column On the composite leaderboard , the JSON Compressed column follows a different aggregation path: Average rps and bandwidth across the 3 conn counts (arithmetic mean, equal weight). Compute myBpr = avgBw / avgRps from those averages. Apply the same effectiveRps = avgRps \\u00d7 (minBpr / myBpr)\\u00b2 formula. Normalize to 0\\u2013100. The resulting per-profile score feeds into the composite like every other profile - averaged across all scored profiles for that framework\'s type. Consequences A framework that picks brotli can dominate even at lower rps , provided the smaller bytes-per-response wins back more score than the rps gap costs. This reflects real-world bandwidth-constrained serving. Gzip-only frameworks are competitive when their compression level is aggressive enough to keep myBpr near the brotli leader. The formula rewards ratio, not the specific encoding chosen. Pre-computed compressed payloads are out of the question for production type - the type rules require the response pipeline to actually compress per-request. See type rules . The best-conn-count panel on the profile tab can rank differently from the composite column , because one picks the highest-scoring conn count and the other averages. Both are intentional: the profile tab answers \\"which framework wins at its best setting?\\"; the composite answers \\"which framework is most consistent across loads?\\"."},{"u":"test-profiles/h1/isolated/json-compressed/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a JSON Compressed","d":"The correctness checks validate.sh runs against the compressed JSON benchmark before a framework\'s results are accepted.","x":"The validation script ( scripts/validate.sh ) runs these checks for the json-comp test profile. All must pass for a framework to be considered valid for this benchmark. Checks Content-Encoding is set when Accept-Encoding is sent GET /json/50?m=1 Accept-Encoding: gzip, br The response must include Content-Encoding: gzip or Content-Encoding: br . Any other value (including absent) is a failure. Response body is correct for multiple (count, m) pairs Three requests are sent with different counts and multipliers: Count Multiplier 25 3 40 7 50 2 For each response, after decompressing, the validator checks: count field equals the route count Every item in items contains the full schema - id , name , category , price , quantity , active , tags (array), rating (object with score and count ), and total total == price * quantity * m for every item (integer, exact) Any missing field or incorrect arithmetic is a failure. Partial payloads that omit fields are rejected. This confirms the server honors the m parameter and applies it per item. No Content-Encoding when Accept-Encoding is absent GET /json/50?m=1 Without Accept-Encoding , the response must not include a Content-Encoding header. Compression is driven per request by the client - servers that unconditionally compress fail this check. Running locally ./scripts/validate.sh <framework> Filter to this profile only: ./scripts/validate.sh <framework> json-comp"},{"u":"test-profiles/h1/isolated/json-processing","t":"JSON Processing","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"A typical API workload: load a dataset, compute derived fields and serialize the JSON response under load.","x":"Measures how efficiently a framework handles a typical real-world API workload: loading data, computing derived fields, and serializing a JSON response. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/json-processing/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a JSON Processing","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the JSON processing benchmark.","x":"Standard Tuned Engine Infrastructure Must use the framework standard JSON serialization. No pre-serialized caches, no custom serializers, no bypassing the framework response pipeline. May use alternative JSON libraries (simd-json, sonic-json) and framework-specific optimizations. The JSON body must still be serialized per request from live data - pre-computed / pre-serialized response caches or response-lookup tables are not allowed on either type; they short-circuit the serialization workload the profile exists to measure. No specific rules. The JSON body must be serialized per request by the handler module. A static file on disk, a literal-response config directive, or any pre-serialized response cache does not qualify - the profile exists to measure serialization work. Configuration is otherwise free. The JSON Processing profile measures how efficiently a framework handles a typical real-world API workload: loading data, computing derived fields, and serializing a JSON response. How it works At startup, the server reads /data/dataset.json - a file containing 50 items with mixed types (strings, numbers, booleans, arrays, nested objects) On each GET /json/{count}?m={multiplier} request, the server: Takes the first count items from the dataset (1\\u201350) Computes a total field ( price \\u00d7 quantity \\u00d7 m ) for each item - m is an integer path-query multiplier that varies per request template Builds the response object Serializes everything as JSON Returns Content-Type: application/json The benchmark round-robins across 7 fixed (count, m) pairs: (1, 3) , (5, 7) , (10, 2) , (15, 5) , (25, 4) , (40, 8) , (50, 6) . Different multipliers per template mean every response body is unique - a framework that tries to cache GET /json/5 by path alone will return wrong totals for other requests. What it measures Object allocation - 1\\u201350 new objects built per request, varying across requests JSON serialization - converting native data structures to JSON text Mixed-type handling - strings, numbers, booleans, arrays, nested objects Route parameter parsing - extracting the count from the URL path Framework response overhead - buffering, headers, content-type handling Dataset format Each item in dataset.json : { \\"id\\": 1, \\"name\\": \\"Alpha Widget\\", \\"category\\": \\"electronics\\", \\"price\\": 328, \\"quantity\\": 15, \\"active\\": true, \\"tags\\": [\\"fast\\", \\"new\\"], \\"rating\\": { \\"score\\": 48, \\"count\\": 127 } } Expected response For GET /json/5?m=3 : { \\"items\\": [ { \\"id\\": 1, \\"name\\": \\"Alpha Widget\\", \\"category\\": \\"electronics\\", \\"price\\": 328, \\"quantity\\": 15, \\"active\\": true, \\"tags\\": [\\"fast\\", \\"new\\"], \\"rating\\": { \\"score\\": 48, \\"count\\": 127 }, \\"total\\": 14760 } ], \\"count\\": 5 } The count field must match the number of items returned and the route parameter. The total field is computed as price * quantity * m (all integers - no rounding needed). The server must return the first count items from the dataset. Parameters Parameter Value Endpoint GET /json/{count}?m={multiplier} Count \\u00d7 multiplier pairs (1,3) , (5,7) , (10,2) , (15,5) , (25,4) , (40,8) , (50,6) (round-robin) Connections 4,096 Pipeline 1 Duration 5s Runs 3 (best taken) Dataset 50 items, mounted at /data/dataset.json"},{"u":"test-profiles/h1/isolated/json-processing/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a JSON Processing","d":"The correctness checks validate.sh runs against the JSON processing benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the json test. Response structure and computed totals Sends GET /json/{count} for counts 12, 22, 31, and 50 (different from the benchmark counts to prevent hardcoded responses). For each request, verifies: The response contains exactly count items Every item contains the full schema - id , name , category , price , quantity , active , tags (array), rating (object with score and count ), and total . Partial payloads that omit fields are rejected. Each total is correctly computed as price * quantity * m Content-Type header Sends GET /json/50 and verifies the Content-Type response header is application/json ."},{"u":"test-profiles/h1/isolated/json-tls","t":"JSON over TLS","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"The JSON processing workload transported over HTTP/1.1 with TLS on a dedicated port, isolating encryption overhead.","x":"Same workload as JSON Processing - a dataset of 50 items serialized to JSON with a per-request multiplier - but transported over HTTP/1.1 on top of TLS through a dedicated port (8081). Measures how much of a framework\'s plaintext throughput survives TLS record framing, symmetric cipher work, and record-boundary scheduling. Implementation Guidelines Endpoint specification, TLS port, ALPN requirements. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/json-tls/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a JSON over TLS","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the JSON over TLS benchmark.","x":"Standard Tuned Engine Infrastructure Must use the framework standard JSON serialization and a standard TLS stack (OpenSSL, BoringSSL, rustls, SChannel, JDK JSSE, etc.). No pre-serialized caches, no bypassing the framework response pipeline, no TLS session-ticket shortcuts that skip real handshakes. May use alternative JSON libraries, tuned TLS providers, and framework-specific optimizations. The JSON body must still be serialized per request from live data - pre-computed / pre-serialized response caches are not allowed on either type; they short-circuit the serialization workload the profile exists to measure. No specific rules. The JSON body must be serialized per request by the handler module. A static file on disk, a literal-response config directive, or any pre-serialized response cache does not qualify - the profile exists to measure serialization work. Configuration is otherwise free. TLS must come from a standard stack (OpenSSL, BoringSSL, rustls, quictls) and every connection must complete a real handshake. The JSON over TLS profile is the JSON Processing workload transported over HTTP/1.1 + TLS on a dedicated port. It measures how much of a framework\'s plaintext JSON throughput survives encryption. How it works The framework loads /data/dataset.json at startup (same 50-item mixed-type dataset as the plain json profile) The framework listens on port 8081 with HTTPS, serving HTTP/1.1 only (ALPN advertises http/1.1 ) On each GET /json/{count}?m={multiplier} request, the server returns the same response shape as the json profile: first count items with total = price \\u00d7 quantity \\u00d7 m , wrapped in {items, count} Returns Content-Type: application/json Client sends no Accept-Encoding header - compression is out of scope for this profile The load generator is wrk with a Lua rotation script ( requests/json-tls-rotate.lua ). gcannon is not used for this test because it doesn\'t support TLS. What it measures Everything JSON Processing measures TLS handshake cost amortized over keep-alive - connections are long-lived at 4096 concurrent Record framing overhead - every HTTP request gets wrapped in one or more TLS records Symmetric cipher throughput - AES-GCM / ChaCha20-Poly1305 on the hot path Certificate private-key operations - RSA/ECDSA cost per new connection, mostly negligible with keep-alive but visible during ramp Port, ALPN, and certificates Port : 8081 (distinct from 8080 plaintext and 8443 which is dedicated to HTTP/2 / HTTP/3 profiles) ALPN : advertise http/1.1 only. HTTP/1.1-only clients (wrk) negotiate correctly and never upgrade to h2. Certificates : the same PEM files used by baseline-h2 / static-h2 , mounted at /certs/server.crt and /certs/server.key . Frameworks typically read them via environment variables ( TLS_CERT , TLS_KEY ) or a hardcoded path, same pattern as the other TLS tests. Expected response For GET /json/5?m=3 over HTTPS on port 8081: HTTP/1.1 200 OK Content-Type: application/json Body (same as the plain json profile): { \\"items\\": [ { \\"id\\": 1, \\"name\\": \\"Alpha Widget\\", \\"category\\": \\"electronics\\", \\"price\\": 328, \\"quantity\\": 15, \\"active\\": true, \\"tags\\": [\\"fast\\", \\"new\\"], \\"rating\\": { \\"score\\": 48, \\"count\\": 127 }, \\"total\\": 14760 } ], \\"count\\": 5 } Parameters Parameter Value Endpoint GET /json/{count}?m={multiplier} Transport HTTP/1.1 over TLS Port 8081 ALPN http/1.1 Count \\u00d7 multiplier pairs (1,3) , (5,7) , (10,2) , (15,5) , (25,4) , (40,8) , (50,6) (round-robin) Connections 4,096 Pipeline 1 Duration 5s Runs 3 (best taken) Load generator wrk + requests/json-tls-rotate.lua Certificates mounted at /certs/server.crt + /certs/server.key Dataset 50 items, mounted at /data/dataset.json"},{"u":"test-profiles/h1/isolated/json-tls/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a JSON over TLS","d":"The correctness checks validate.sh runs against the JSON over TLS benchmark before a framework\'s results are accepted.","x":"The validation script ( scripts/validate.sh ) runs these checks for the json-tls test profile. All must pass for a framework to be considered valid for this benchmark. Checks ALPN negotiates HTTP/1.1 curl -sk --http1.1 https://localhost:8081/json/1?m=1 The response must report http_version = 1.1 . A framework that advertises only h2 on port 8081, or that refuses HTTP/1.1 clients, fails this check. Response body is correct for multiple (count, m) pairs Three requests are sent over HTTPS on port 8081 with different counts and multipliers: Count Multiplier 7 2 23 11 50 1 For each response the validator checks: count field equals the route count Every item in items contains the full schema - id , name , category , price , quantity , active , tags (array), rating (object with score and count ), and total total == price * quantity * m for every item (integer, exact) These (count, m) pairs are deliberately different from the json-comp validation pairs so a framework that tries to cache validation results across profiles can\'t pass both. Content-Type is application/json curl -sk -D- -o /dev/null https://localhost:8081/json/1?m=1 The response must include a Content-Type header containing application/json . TLS checks These run on every profile that uses TLS: json-tls and static-tls on port 8081, baseline-h2 and static-h2 on 8443. They ask two separate questions: what this connection negotiated, and what the server is willing to negotiate at all. Serves the certificate the harness mounted openssl s_client -connect localhost:8081 -servername localhost The leaf certificate must match certs/server.crt by SHA-256 fingerprint. An entry that ignores /certs and generates its own pair fails. This is not a formality. Every TLS handshake costs the server one signature with that key, and the mounted pair is RSA-2048. On the benchmark host: key signatures/sec RSA-2048 3,052 ECDSA P-256 77,124 An entry that quietly swaps in an EC certificate gets handshakes 25\\u00d7 cheaper on the signing step than every entry that uses the mounted one. Nothing else in the suite would notice. Negotiates TLS 1.3 The client offers TLS 1.3. A server that settles on 1.2 fails: the 1.2 handshake costs an extra round trip, so its connection-setup numbers are not comparable with the rest of the field. Uses a TLS 1.3 AEAD cipher The negotiated suite must be one of TLS_AES_128_GCM_SHA256 , TLS_AES_256_GCM_SHA384 or TLS_CHACHA20_POLY1305_SHA256 . This rules out a NULL, anonymous, export or RC4 suite making \\"TLS\\" effectively free. Which of the three is chosen is reported, not required. In TLS 1.3 the server picks, and the field is split. Some entries choose AES-128-GCM and some AES-256-GCM, which measure roughly 17% apart on bulk encryption at static-file block sizes. Not every framework exposes cipher preference, so the choice is recorded in the validation output rather than failed. ALPN never names a protocol the client did not offer Selecting nothing is fine: a server without ALPN omits the extension and the client falls back, which is correct on the HTTP/1.1 ports. What fails is answering with a protocol that was not offered, which would silently measure something other than the profile names. Accepts no obsolete protocol or weak cipher A server can hand a modern client TLS 1.3 and still accept TLS 1.0, RC4 or a NULL cipher from anything else that asks. The validator offers each of these directly and fails when a handshake actually completes. An alert, a reset or a timeout counts as a refusal: SSLv2 , SSLv3 , TLS 1.0 , TLS 1.1 NULL ciphers (no encryption) anonymous ciphers (no authentication) export-grade ciphers 64-bit, DES, RC2, RC4 or MD5 ciphers 3DES / IDEA OpenSSL will not offer an SSLv3 or TLS 1.0 handshake, nor a NULL/EXPORT/RC4 one, at its default security level, so the probes use @SECLEVEL=0 to make the question askable at all. A protocol the local OpenSSL cannot offer is reported as unprobed rather than counted as refused. The whole set costs about 70ms per port. testssl.sh is the reference tool for this question and agrees with these checks. It is what surfaced the first real failure here. It is the better choice for an audit, where its much wider suite coverage and its SSL Labs style grade are worth the time: a full run costs ~48s per port and caps every entry at B on chain incomplete , which is an artifact of the self-signed certificate the harness mounts rather than anything about the entry. Running locally ./scripts/validate.sh <framework> Filter to this profile only: ./scripts/validate.sh <framework> json-tls"},{"u":"test-profiles/h1/isolated/latency-1m","t":"Latency-1M","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"Pins the offered load at one million requests per second and measures the CPU each framework spends to serve it, read exactly from the container\'s cgroup.","x":"One million requests per second, offered at a fixed rate to every entry that can take it. The rate is not the result. Everyone who finishes serves the same million. What separates them is what it cost. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/latency-1m/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Latency-1M","d":"How the one-million-requests-per-second fixed-rate CPU profile is run, what it measures, and the type-specific rules that apply to it.","x":"Standard Tuned Engine Nothing to implement - the profile drives GET /baseline11 , which the baseline profile already specifies and validates. What it asks of you is a serving model that does not spend CPU it is not using: no busy-wait loop, no spin-poll on a ring, no timer thread waking at a fixed frequency to find nothing to do. All of those are charged here and no other profile can see them. May tune poll intervals, batching, affinity and ring sizing freely. Note that submission-queue polling and every other spin mode is charged in full: it buys latency at a fixed CPU price, and this is the one profile that prices that trade rather than rewarding it. Same as above, and it matters more: an engine with a fixed worker pool polling at a set cadence spends the same CPU at one million req/s as it does at ten thousand, which this profile is designed to expose. Configuration that scales the poll to the load is in scope; a build that cannot idle is a real result, not a disqualification. Every other profile here drives a server until it stops and reports where that was. This one fixes the load and reports the bill. The rate is one million requests per second , offered at a paced, constant rate. It is not a target to be beaten. Every entry that completes the run serves exactly the same million requests. The only thing that varies is the CPU each one burned doing it. Connections: 1,024 \\u00b7 Rate: 1,000,000 req/s \\u00b7 Duration: 20s There is an entry fee A million requests per second is not a load every framework on this board can take. At the time of writing, 46 of 103 enabled entries with a published baseline number can reach a million at all, and reaching it means running flat out with nothing left. Only the 19 entries above two million have genuine headroom at this rate. That is the profile\'s shape, not an accident of tuning. It is a high-water-mark test: among the entries fast enough that a million requests per second is comfortable, which of them is cheap . Two frameworks can post the same ceiling on baseline and spend very different amounts of machine getting to a fixed fraction of it, and nothing else here would tell you which. An entry that cannot hold the rate is not failed for it. It is flagged instead, because its number then describes a lighter workload than everyone else\'s. See Validity below. Nothing to implement The load is GET /baseline11?a=1&b=2 , the same endpoint the baseline profile specifies, with the same response, and only ever the GET. The mixed GET/POST/chunked rotation belongs to baseline ; there are no request bodies here at all. If your entry already subscribes to baseline , subscribing to latency-1m costs you no code. That thinness is deliberate. The handler is as small as the framework allows, so what is left in the CPU figure is the framework\'s own overhead (accept loop, event loop, parser, router, response path) rather than anything the workload contributed. What is measured The number is the CPU time the server\'s container actually consumed , taken from cgroup v2\'s cpu.stat usage_usec immediately before and after the load window. That counter is kept by the kernel and is monotonic, so the difference is exact to the microsecond. It is reported two ways: field meaning cpu_usec total CPU microseconds consumed across the run cpu_per_req_us the same figure divided by requests served, and the metric this profile ranks on Every other profile reports CPU as a percentage averaged from docker stats samples taken about twice a second. That is fine where CPU is context for a throughput number. It is not fine where CPU is the number: a 20s run yields only ~40 samples, and each one is whatever the container happened to be doing at that instant. Nothing here is sampled. Connections are part of the workload A million requests per second at sub-millisecond latency needs only a few dozen requests in flight. The profile holds 1,024 connections anyway. The rest are not there to carry load, they are there to be managed : registered with the event loop, polled, kept alive, holding whatever per-connection state the framework allocates. That management is a cost a server pays whether or not the connections are busy, and it is the cost a real server pays all day. Measuring with only the connections strictly needed would measure a machine nobody runs. Validity: the rate has to have been delivered A fixed-rate result only means something if the rate was actually offered and served. The load generator reports rate_ratio , which is achieved over target, and it is published with every row. A run with rate_ratio meaningfully below 1.0 did not measure this profile: the load was not the load, so its CPU figure describes a lighter workload than the one every other entry was measured on. Those runs are flagged rather than silently ranked as though they were cheap. The load generator This profile is driven by zrk rather than gcannon, because gcannon does not pace. It has no rate limiter, by design: it was built to find ceilings. zrk is a Zig rewrite of wrk2 , the original constant-throughput generator, and is used here in preference to it for three reasons: wrk2\'s last commit is from 2019; zrk schedules sends on a closed-form nanosecond offset rather than wrk2\'s millisecond timer wheel, which rounds every wait up and adds about half a millisecond of the tool\'s own noise to every sample; and zrk emits a JSON summary, so the harness parses a document instead of scraping a report. Its version is pinned with a checksum. The premise of this profile is that the offered load is identical across entries and across rounds, so a generator that drifts underneath it would invalidate comparisons against numbers already published. Parameters Parameter Value Endpoint GET /baseline11?a=1&b=2 (GET only, no body) Offered rate 1,000,000 req/s, paced Connections 1,024 Duration 20s Runs 3, and the highest-scoring one is kept Load generator zrk (constant throughput, coordinated-omission corrected) Metric CPU microseconds per request, from cgroup cpu.stat Three runs are taken and the best of them is kept, judged by the same formula described under Scoring . Since every run delivers the same rps by construction, keeping the fastest would be choosing between them at random. Scoring them also discards the warm-up for free: an unsettled JIT, or a GC heap that has not reached steady state, shows up precisely as CPU, and the first run is the one carrying it. Implementation notes Do not busy-wait. A spin loop, a submission-queue poller, or a worker that never sleeps costs a full core whether it is serving a million requests per second or none. That is a real trade, since it buys latency, and this is the one profile that prices it instead of rewarding it Watch fixed-cadence timers. A background thread that wakes every millisecond to check for work spends measurable CPU doing nothing, a thousand times a second, forever Per-connection state is charged here. 1,024 connections holding large per-connection buffers cost memory, and allocating and touching that memory costs CPU Garbage collection lands in the number. This is not a distortion; a collector running at a million requests per second is genuinely spending the machine. Three runs and lowest-wins keeps startup allocation out of it, while steady-state collection correctly stays in Scoring Every other profile ranks on one number, requests per second. This one cannot: the rate is pinned, so every entry that finishes serves the same load and throughput is identical by construction. The score combines what it cost with how the tail behaved. rateFactor = min(1, achieved_rps / 950,000) quality = 0.60 x cpuScore + 0.25 x p99Score + 0.15 x p999Score score = 100 x rateFactor x quality measured against the best value present in the field: term shape at the best 10x worse 100x worse 1000x worse cpuScore bestCpu / cpu 1.00 0.10 0.01 0.00 p99Score 1 - log10(p99 / bestP99) / 3 1.00 0.67 0.33 0.00 p999Score 1 - log10(p999 / bestP999) / 3 1.00 0.67 0.33 0.00 All three are clamped to 0\\u20131. Why the two shapes differ. CPU per request spans about 3.3x across the entries that hold the rate, so a plain ratio behaves well over it. The latency tails span five orders of magnitude, 151 \\u00b5s to 7.6 s at p99 among rate-holders, and a plain ratio there collapses to near zero for everything but the leader, spending 40% of the weight without separating anybody. A decade scale keeps the whole field distinguishable while still charging heavily for a bad tail. Why the 3-decade span is fixed rather than derived from the worst entry: one pathological entry joining the board would otherwise move everybody else\'s score. rateFactor is the gate. Full credit at 950,000 req/s and above. The practical ceiling is around 998,000, so everything that holds the rate sits comfortably clear of it. Below that it falls proportionally, so an entry serving 50,000 req/s keeps 50/950 \\u2248 0.053 of whatever quality it earned. This is what stops a server that quietly serves less from looking cheap: its CPU per request may be excellent, but it is multiplied by a rate factor near zero. Nothing is rescaled so the leader lands on exactly 100. The top entry scores in the low-to-mid 90s because no single entry is simultaneously best on cost and on both tails, and that gap is information. It also means a score means the same thing across rounds instead of being silently rebased whenever a new leader arrives. Best of three runs is decided by this same formula, with each metric normalised against the best of that entry\'s own three runs rather than against the field, which does not exist yet while one entry is being measured. Choosing on CPU alone could keep a cheap run with a wrecked tail. On the composite board this score is shown \\u00d710, on the 0\\u20131,000 basis every profile column uses there. The detail view and --table report it as defined above, out of 100. The reference implementation is scripts/latency_1m_score.py ; the board mirrors it in JavaScript. Run python3 scripts/latency_1m_score.py --table to score the published results from the command line. Composite This profile counts toward the composite score , for framework and engine entries alike. It is the one profile the composite does not normalize on requests per second, because it cannot: the rate is pinned, so every entry that holds it delivers the same one and rps would score them all identically. The score above is contributed directly, multiplied by 10 onto the composite\'s 0\\u20131,000 per-profile scale. Because that score is not rebased on the field leader, the best entry here contributes about 970 rather than a full 1,000, because nobody is simultaneously cheapest and best on both tails. That is the same deliberate choice as above, carried through to the sum."},{"u":"test-profiles/h1/isolated/latency-1m/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Latency-1M","d":"How correctness is established for the Latency-1M fixed-rate CPU profile.","x":"This profile has no validation section of its own, and that is not an omission. It drives GET /baseline11?a=1&b=2 , the baseline endpoint unchanged, so an entry subscribed to latency-1m is run through the full baseline validation suite : the arithmetic, the Content-Type , the randomized-input anti-cheat, the TCP fragmentation checks, and the exhaustive per-byte fragmentation sweep. There is no second endpoint to check. The profile is also unusually hard to game, because the thing an entry would have to fake is not in its control: The load is fixed. Every entry is offered the same 1,000,000 req/s over the same 1,024 connections for the same 20 seconds. There is no throughput number to inflate. The measurement is taken outside the container. CPU comes from the kernel\'s own cpu.stat accounting for the container\'s cgroup, read by the harness. Nothing running inside the container reports it, and nothing running inside can alter it. Serving fewer requests does not help. The metric is CPU per request , so a server that quietly drops load spends less CPU and serves proportionally less, leaving the ratio where it was while rate_ratio records that it fell short. The one check that is specific to this profile rate_ratio , the achieved rate over the offered rate, is recorded on every run and published with the row. It is the profile\'s validity gate rather than a correctness check. A run that did not hold 1,000,000 req/s was not measured under this profile\'s load, so its CPU figure is not comparable with entries that were. Those runs are flagged rather than ranked as though they were cheap."},{"u":"test-profiles/h1/isolated/pipelined","t":"Pipelined (16x)","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"Sixteen requests are sent back to back per connection before any response is read, isolating raw I/O throughput from application logic.","x":"16 HTTP requests are sent back-to-back on each connection before waiting for responses, isolating raw I/O throughput from application logic. This test is reference-only - it does not contribute to the composite score. HTTP/1.1 pipelining is disabled in modern browsers and unsupported by mainstream proxies and CDNs, so the profile is kept as a middleware-efficiency indicator rather than a ranked benchmark (see issue #1058). Results appear on the board as a faded column. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/pipelined/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Pipelined (16x)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the HTTP pipelining benchmark.","x":"Standard Tuned Engine Infrastructure Must use the framework standard request handling. No custom pipeline batching or read-ahead optimizations. May implement custom pipeline batching, read buffer optimizations, or framework-specific pipelining flags. No specific rules. Ranked separately from frameworks. Configuration is free - worker counts, buffer sizes, event-loop and socket tuning are all allowed. Each request in a batch must produce its own response; coalescing the batch into one pre-built buffer, or answering from a cache keyed on the request line, is not allowed. This is the one profile scored for infrastructure and not for frameworks - for a proxy, pipelining behaviour is the thing being compared. 16 HTTP requests are sent back-to-back on each connection before waiting for responses. Uses a lightweight GET /pipeline endpoint that returns a fixed ok response, isolating raw I/O throughput from application logic. This test is reference-only - it does not contribute to the composite score. HTTP/1.1 pipelining is disabled in modern browsers and unsupported by mainstream proxies, so the profile is kept as a raw I/O and middleware-efficiency indicator (issue #1058). Results still appear on the board as a faded column. Connections: 512, 4,096 Expected request/response GET /pipeline HTTP/1.1 HTTP/1.1 200 OK Content-Type: text/plain ok What it measures HTTP pipelining support and efficiency Frameworks that parse multiple requests from a single read buffer gain a major advantage Frameworks processing one request at a time per connection see minimal improvement over baseline Network batching, write coalescing, and syscall reduction Why a separate endpoint? The /pipeline endpoint removes application-level variance (query parsing, body handling) so the benchmark measures pure I/O and protocol handling throughput. This isolates the framework ability to batch and process pipelined requests efficiently."},{"u":"test-profiles/h1/isolated/pipelined/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Pipelined (16x)","d":"The correctness checks validate.sh runs against the HTTP pipelining benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the pipelined test. GET /pipeline response Sends GET /pipeline and verifies the response body is exactly ok ."},{"u":"test-profiles/h1/isolated/short-lived","t":"Short-lived Connection","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"The baseline workload with each connection closed after ten requests, measuring TCP handshake and connection setup overhead.","x":"Same workload as baseline, but each connection is closed and re-established after 10 requests, forcing frequent TCP handshakes. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/short-lived/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Short-lived Connection","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the short-lived connection benchmark.","x":"Standard Tuned Engine Infrastructure Must use the framework default connection handling. No custom keep-alive tuning or connection pooling optimizations. May optimize connection recycling, TCP fast-open, and socket reuse settings. No specific rules. Ranked separately from frameworks. Configuration is free - worker counts, buffer sizes, event-loop and socket tuning are all allowed. Every connection must complete a real accept and close - reuseport and accept-queue tuning are allowed, carrying connections across the 10-request window is not. Same workload as baseline, but each connection is closed and re-established after 10 requests. This forces frequent TCP handshakes. Connections: 512, 4,096 Expected request/response Same as baseline - sum of query parameters: GET /baseline11?a=13&b=42 HTTP/1.1 HTTP/1.1 200 OK Content-Type: text/plain 55 What it measures Socket creation and teardown overhead Connection accept rate Per-connection memory allocation/deallocation Any connection pooling or caching strategies Real-world relevance Many clients (mobile, IoT, load balancers without keepalive) do not maintain long-lived connections. This profile captures how well a framework handles the constant churn of short-lived connections - a common pattern in production environments."},{"u":"test-profiles/h1/isolated/short-lived/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Short-lived Connection","d":"The correctness checks validate.sh runs against the short-lived connection benchmark before a framework\'s results are accepted.","x":"The short-lived connection profile uses the same /baseline11 endpoint as the baseline test. Its validation is covered by the Baseline validation checks, which run for every framework subscribed to either baseline or limited-conn . No additional validation checks are specific to this profile - the difference is in benchmark behavior (connections close after 10 requests), not in endpoint correctness."},{"u":"test-profiles/h1/isolated/static","t":"Static Files (HTTP/1.1)","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"Serves twenty static files of mixed type and size over HTTP/1.1, approximating a realistic page load.","x":"Serves 20 static files of various types and sizes over HTTP/1.1, simulating a realistic page load. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/static-tls","t":"Static Files (HTTP/1.1 + TLS)","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"Serves twenty static files of mixed type and size over HTTP/1.1 + TLS, approximating a realistic page load over an encrypted connection.","x":"Serves 20 static files of various types and sizes over HTTP/1.1 + TLS on port 8081, simulating a realistic page load over an encrypted connection. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/static-tls/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Static Files (HTTP/1.1 + TLS)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the HTTP/1.1 + TLS static file benchmark.","x":"Standard Tuned Engine Infrastructure File contents may be served from memory, but the cache must be the framework\'s own - a documented static file handler with whatever caching it comes with. A cache assembled in the entry does not count: no reading the directory into a map at startup, no pre-loaded buffers, no mapping the files by hand. The cache must also follow the disk: replace a file and the next response must carry the new bytes. Compression must use the framework\'s standard middleware or built-in static file handler - no handmade compression code. Serving the pre-compressed .br / .gz variants that sit on disk next to the originals is allowed , by a documented framework API (e.g. ASP.NET MapStaticAssets , nginx gzip_static / brotli_static , Caddy precompressed , Hono serveStatic({ precompressed: true }) ) where the framework has one, and otherwise by selecting the variant in the entry off Accept-Encoding . Those bytes already exist, so choosing one is a file read rather than compression - which is why this is allowed while compressing by hand is not. The response must carry the original file\'s Content-Type , the matching Content-Encoding , and must still follow the disk. TLS must come from a standard stack (OpenSSL, BoringSSL, rustls, SChannel, JDK JSSE, etc.) - no TLS session-ticket shortcuts that skip real handshakes. File contents may be served from memory, the same rule as Standard: the cache must be the framework\'s own rather than one assembled in the entry, and it must follow the disk - replace a file and the next response must carry the new bytes. Pre-rendered response headers stay allowed, as does serving pre-compressed .gz/.br variants from disk via any mechanism, with any compression approach and tuned TLS providers. File contents may be served from memory, the same rule as Standard: the cache must be the framework\'s own rather than one assembled in the entry, and it must follow the disk - replace a file and the next response must carry the new bytes. No other restrictions. Configuration is free, including sendfile, open_file_cache, mmap and in-memory caching - serving files fast from a tuned cache is the job. Pre-compressed .br/.gz variants may be served through the server\'s own mechanism (nginx gzip_static / brotli_static, Caddy precompressed). Bodies must come from the mounted static directory; a handler that synthesizes them is not serving files. TLS must come from a standard stack (OpenSSL, BoringSSL, rustls, quictls) and every connection must complete a real handshake. The Static Files over TLS profile is the Static Files workload transported over HTTP/1.1 + TLS on a dedicated port. It measures how much of a framework\'s plaintext static-serving throughput survives encryption. Connections: 1,024, 4,096, 6,800 How it works The framework serves the same 20 files from /data/static/ as the plain static profile The framework listens on port 8081 with HTTPS, serving HTTP/1.1 only (ALPN advertises http/1.1 ) The load generator ( wrk ) requests the 20 files in a round-robin pattern using the same Lua rotation script as static ( requests/static-rotate.lua ), over https:// All requests include Accept-Encoding: br;q=1, gzip;q=0.8 CSS (5 files, 8\\u2013200 KB): reset.css , layout.css , theme.css , components.css , utilities.css JavaScript (5 files, 12\\u2013300 KB): analytics.js , helpers.js , app.js , vendor.js , router.js HTML (2 files, 55\\u2013120 KB): header.html , footer.html Fonts (2 files, 18\\u201322 KB): regular.woff2 , bold.woff2 SVG (2 files, 15\\u201370 KB): logo.svg , icon-sprite.svg Images (3 files, 6\\u201345 KB): hero.webp , thumb1.webp , thumb2.webp JSON (1 file, 3 KB): manifest.json Total payload: ~842 KB across 20 files (~743 KB compressible text + ~99 KB binary). Brotli-compressed total: ~219 KB. Pre-compressed versions of all text files ( .gz at level 9, .br at level 11) are available in the data/static/ directory alongside the originals. Compression Identical to the plain static profile : compression is optional and must come from standard middleware on production entries, while serving the pre-compressed .br / .gz files already on disk is allowed for every entry type. Tuned entries are unrestricted. Port, ALPN, and certificates Port : 8081 (distinct from 8080 plaintext and 8443 which is dedicated to HTTP/2 / HTTP/3 profiles), shared with the json-tls profile ALPN : advertise http/1.1 only. HTTP/1.1-only clients (wrk) negotiate correctly and never upgrade to h2. Certificates : the same PEM files used by json-tls / baseline-h2 / static-h2 , mounted at /certs/server.crt and /certs/server.key . Frameworks typically read them via environment variables ( TLS_CERT , TLS_KEY ) or a hardcoded path, same pattern as the other TLS tests. What it measures Everything Static Files measures TLS handshake cost amortized over keep-alive - connections are long-lived at 1,024\\u20136,800 concurrent Record framing overhead on large payloads - multi-hundred-KB responses span many TLS records, unlike the small JSON bodies of json-tls Symmetric cipher throughput - AES-GCM / ChaCha20-Poly1305 on the hot path, dominated by bulk encryption of file bodies Expected request/response GET /static/reset.css HTTP/1.1 Host: localhost:8081 Accept-Encoding: br;q=1, gzip;q=0.8 HTTP/1.1 200 OK Content-Type: text/css Content-Encoding: br (compressed file contents) Or without compression: HTTP/1.1 200 OK Content-Type: text/css (file contents) Parameters Parameter Value Endpoint 20 URIs under /static/* Transport HTTP/1.1 over TLS Port 8081 ALPN http/1.1 Connections 1,024, 4,096, 6,800 Pipeline 1 Duration 5s Runs 3 (best taken) Load generator wrk + requests/static-rotate.lua Certificates mounted at /certs/server.crt + /certs/server.key"},{"u":"test-profiles/h1/isolated/static-tls/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Static Files (HTTP/1.1 + TLS)","d":"The correctness checks validate.sh runs against the HTTP/1.1 + TLS static file benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the static-tls test. They mirror the static checks , performed over HTTPS on port 8081. ALPN negotiates HTTP/1.1 curl -sk --http1.1 https://localhost:8081/static/reset.css The response must report http_version = 1.1 . A framework that advertises only h2 on port 8081, or that refuses HTTP/1.1 clients, fails this check. Content-Type headers Verifies correct Content-Type headers for representative file types: GET /static/reset.css - expects Content-Type: text/css GET /static/app.js - expects Content-Type: application/javascript GET /static/manifest.json - expects Content-Type: application/json Note: text/javascript is accepted as equivalent to application/javascript per RFC 9239. File size verification (uncompressed) Requests all 20 static files without Accept-Encoding and compares the response size against the actual file size on disk. All 20 files must match exactly. This ensures the server returns uncompressed content when no compression is requested. reset.css , layout.css , theme.css , components.css , utilities.css , analytics.js , helpers.js , app.js , vendor.js , router.js , header.html , footer.html , regular.woff2 , bold.woff2 , logo.svg , icon-sprite.svg , hero.webp , thumb1.webp , thumb2.webp , manifest.json Compression verification Requests all 20 static files with Accept-Encoding: br;q=1, gzip;q=0.8 and checks: If the server returns a Content-Encoding header (br or gzip), the decompressed response size must match the original file size on disk If the server does not compress a file, it is counted as skipped (not a failure - compression is optional) PASS if all compressed files decompress to the correct size. SKIP if the server does not compress any files. FAIL if any compressed file decompresses to the wrong size. 404 for nonexistent file Sends GET /static/nonexistent.txt and verifies the server returns HTTP 404 . TLS checks This profile\'s TLS listener on port 8081 also goes through the shared TLS checks. The certificate must be the one the harness mounted, the connection must negotiate TLS 1.3 with an AEAD cipher, ALPN must not name a protocol the client did not offer, and the server must accept no obsolete protocol or weak cipher. They are documented once, under json-tls validation ."},{"u":"test-profiles/h1/isolated/static/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Static Files (HTTP/1.1)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the HTTP/1.1 static file benchmark.","x":"Standard Tuned Engine Infrastructure File contents may be served from memory, but the cache must be the framework\'s own - a documented static file handler with whatever caching it comes with. A cache assembled in the entry does not count: no reading the directory into a map at startup, no pre-loaded buffers, no mapping the files by hand. The cache must also follow the disk: replace a file and the next response must carry the new bytes. Compression must use the framework\'s standard middleware or built-in static file handler - no handmade compression code. Serving the pre-compressed .br / .gz variants that sit on disk next to the originals is allowed , by a documented framework API (e.g. ASP.NET MapStaticAssets , nginx gzip_static / brotli_static , Caddy precompressed , Hono serveStatic({ precompressed: true }) ) where the framework has one, and otherwise by selecting the variant in the entry off Accept-Encoding . Those bytes already exist, so choosing one is a file read rather than compression - which is why this is allowed while compressing by hand is not. The response must carry the original file\'s Content-Type , the matching Content-Encoding , and must still follow the disk. File contents may be served from memory, the same rule as Standard: the cache must be the framework\'s own rather than one assembled in the entry, and it must follow the disk - replace a file and the next response must carry the new bytes. Pre-rendered response headers stay allowed, as does serving pre-compressed .gz/.br variants from disk via any mechanism, with any compression approach. File contents may be served from memory, the same rule as Standard: the cache must be the framework\'s own rather than one assembled in the entry, and it must follow the disk - replace a file and the next response must carry the new bytes. No other restrictions. Configuration is free, including sendfile, open_file_cache, mmap and in-memory caching - serving files fast from a tuned cache is the job. Pre-compressed .br/.gz variants may be served through the server\'s own mechanism (nginx gzip_static / brotli_static, Caddy precompressed). Bodies must come from the mounted static directory; a handler that synthesizes them is not serving files. Serves 20 static files of various types and sizes over HTTP/1.1, simulating a realistic page load with diverse file types and sizes. Connections: 1,024, 4,096, 6,800 Workload The load generator ( wrk ) requests 20 static files in a round-robin pattern using a Lua rotation script. All requests include Accept-Encoding: br;q=1, gzip;q=0.8 . CSS (5 files, 8\\u2013200 KB): reset.css , layout.css , theme.css , components.css , utilities.css JavaScript (5 files, 12\\u2013300 KB): analytics.js , helpers.js , app.js , vendor.js , router.js HTML (2 files, 55\\u2013120 KB): header.html , footer.html Fonts (2 files, 18\\u201322 KB): regular.woff2 , bold.woff2 SVG (2 files, 15\\u201370 KB): logo.svg , icon-sprite.svg Images (3 files, 6\\u201345 KB): hero.webp , thumb1.webp , thumb2.webp JSON (1 file, 3 KB): manifest.json Total payload: ~842 KB across 20 files (~743 KB compressible text + ~99 KB binary). Brotli-compressed total: ~219 KB. Pre-compressed versions of all text files ( .gz at level 9, .br at level 11) are available in the data/static/ directory alongside the originals. Compression All requests include Accept-Encoding: br;q=1, gzip;q=0.8 , indicating the client prefers Brotli but accepts gzip. Compression is optional. Frameworks that don\'t compress will serve files uncompressed - there is no penalty or validation failure. However, frameworks that do compress will benefit from reduced I/O, which naturally improves throughput. Text files (CSS, JS, HTML, SVG, JSON): good candidates for compression (68\\u201394% size reduction with brotli) Binary files (woff2, webp): already compressed formats - servers should skip compression for these Pre-compressed files : .gz and .br versions are available on disk. Serving them directly costs no CPU and is allowed for every entry type - through a documented API (e.g. Nginx gzip_static / brotli_static , Caddy precompressed , ASP.NET MapStaticAssets , Hono serveStatic({ precompressed: true }) ) where the framework has one, and otherwise by selecting the variant off Accept-Encoding in the entry. Production rule: compression must come from the framework\'s standard middleware or its built-in static file handler. No handmade compression code - nothing may be compressed at runtime by code written in the entry. What a framework\'s own handler does internally is its business. Selecting an already-compressed file is a separate thing and is not restricted: the .br / .gz variants sit on disk next to the originals, and picking one off Accept-Encoding reads a different path rather than compressing anything. Use the framework\'s own pre-compressed API where there is one; where there is not, select it in the entry. The response must carry the original file\'s Content-Type and the matching Content-Encoding . This is a deliberate change from the earlier rule, which allowed pre-compressed files only behind a documented API. That made the profile turn on whether a framework happened to ship one feature, and an entry serving full-size bodies against entries serving brotli is not measuring the same work - the gap swamped everything else the profile measures. Watch the q-values The header is sent with q-values - br;q=1, gzip;q=0.8 - which is ordinary HTTP but defeats any handler that matches the encoding by exact token. A common shape: const accepted = new Set(header.split(\\",\\").map(s => s.trim())) if (!accepted.has(\\"br\\")) { /* skipped */ } That set holds \\"br;q=1\\" , not \\"br\\" , so it never matches and every response goes out uncompressed. Hono\'s serveStatic had exactly this, and it cost the entry about a third of its throughput while looking correct in every hand check. It is easy to miss, because the usual ways of checking send no q-values: curl --compressed sends deflate, gzip, br, zstd , and a hand-written -H \'Accept-Encoding: br\' matches too. Only the load generator sends the real header. If you are adding pre-compressed support, verify with the header the profile actually sends: curl -sI -H \'Accept-Encoding: br;q=1, gzip;q=0.8\' localhost:8080/static/app.js and check for Content-Encoding: br in the response. Tuned rule: free to use any approach - custom compression, manual .br / .gz lookup, etc. What it measures Static file serving throughput over HTTP/1.1 Content-Type handling for different file types File serving strategy efficiency - how well the framework moves bytes to the client (sendfile, readahead, buffer sizing, and its own caching where it has any) Response efficiency with varied payload sizes Compression efficiency (optional - reduces I/O at the cost of CPU) Expected request/response GET /static/reset.css HTTP/1.1 Host: localhost:8080 Accept-Encoding: br;q=1, gzip;q=0.8 HTTP/1.1 200 OK Content-Type: text/css Content-Encoding: br (compressed file contents) Or without compression: HTTP/1.1 200 OK Content-Type: text/css (file contents) Parameters Parameter Value Endpoint 20 URIs under /static/* Connections 1,024, 4,096, 6,800 Pipeline 1 Duration 5s Runs 3 (best taken) Load generator wrk with Lua rotation script"},{"u":"test-profiles/h1/isolated/static/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Static Files (HTTP/1.1)","d":"The correctness checks validate.sh runs against the HTTP/1.1 static file benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the static test. Content-Type headers Verifies correct Content-Type headers for representative file types: GET /static/reset.css - expects Content-Type: text/css GET /static/app.js - expects Content-Type: application/javascript GET /static/manifest.json - expects Content-Type: application/json Note: text/javascript is accepted as equivalent to application/javascript per RFC 9239. File size verification (uncompressed) Requests all 20 static files without Accept-Encoding and compares the response size against the actual file size on disk. All 20 files must match exactly. This ensures the server returns uncompressed content when no compression is requested. reset.css , layout.css , theme.css , components.css , utilities.css , analytics.js , helpers.js , app.js , vendor.js , router.js , header.html , footer.html , regular.woff2 , bold.woff2 , logo.svg , icon-sprite.svg , hero.webp , thumb1.webp , thumb2.webp , manifest.json Compression verification Requests all 20 static files with Accept-Encoding: br;q=1, gzip;q=0.8 and checks: If the server returns a Content-Encoding header (br or gzip), the decompressed response size must match the original file size on disk If the server does not compress a file, it is counted as skipped (not a failure - compression is optional) PASS if all compressed files decompress to the correct size. SKIP if the server does not compress any files. FAIL if any compressed file decompresses to the wrong size. 404 for nonexistent file Sends GET /static/nonexistent.txt and verifies the server returns HTTP 404 ."},{"u":"test-profiles/h1/isolated/tls/validation","t":"Validation","c":"Knowledge Base","d":"The opt-in TLS section: certificate rotation, SNI, resumption, close_notify and the vulnerability suite, on the HTTP/1.1 TLS listener.","x":"An opt-in section. Nothing here is measured and nothing here affects a score. It is a hardening bar an entry chooses to be held to, and passing it earns the TLS badge on the HTTP/1.1 composite. Subscribe with the tls_check field in meta.json . It is a capability the entry opts into rather than a profile it is measured on, which is why it has its own field instead of an entry in tests : \\"enabled\\": true, \\"tls_check\\": true A listener of its own, on :9000 The section needs a second TLS listener on port 9000 , reading its certificate and key from /certs-tls . That directory is mounted for this entry alone and seeded from the usual pair. This is not incidental. The section replaces certificates underneath a running server, and doing that to the shared /certs would move the ground under json-tls , static-tls and every h2 profile in the same validation run. A dedicated port and a private directory keep it from touching anything else, and /certs is never written to. The listener is HTTP/1.1 over TLS. The h2 and h3 listeners are separate and not covered here, which is why the badge only appears on the H1 composite. An entry that opts in without opening :9000 fails the section with a clear message rather than being skipped. Why it is opt-in Most of these need the entry to have done something deliberate. Binding a certificate once at startup, which is what almost every entry does, fails the first check on this page. Opting in is a statement that the entry has gone further. Checks Certificate rotation The certificate and key at /certs-tls are replaced with a freshly generated RSA-2048 pair while the server is running. The server must serve the new certificate without a restart , within 30 seconds, and must still answer requests on it. A certificate is renewed roughly every 60 days in production. A server that needs a restart to pick one up is a weaker server, and nothing else in the suite notices the difference. The usual way to pass is a per-handshake certificate callback rather than a value bound at startup: ServerCertificateSelector in Kestrel, tls.Config.GetCertificate in Go, a ResolvesServerCert in Rust with rustls. Rotation keeps serving Thirty requests are issued across the swap. All of them must succeed. Rotating by dropping traffic is not rotating. SNI The server must complete a handshake both with a server name and without one. A client that omits SNI has to get a usable answer rather than a dropped connection. Session resumption Reported, not required. If the server issues a session ticket, a second connection presenting it should resume. An entry that issues no ticket makes every connection pay a full handshake, which is worth knowing but is not a failure. close_notify The server must close at the TLS layer rather than dropping the socket. Without the alert, a truncated response is indistinguishable from a complete one. Vulnerability suite testssl.sh -U , which covers Heartbleed, CCS, Ticketbleed, ROBOT, secure renegotiation, CRIME, BREACH, POODLE, TLS_FALLBACK_SCSV , SWEET32, FREAK, DROWN, LOGJAM, BEAST, LUCKY13, Winshock and RC4. It takes about 30 seconds. Any HIGH or CRITICAL finding fails the section. Set HTTPARENA_SKIP_TLS_SCAN=1 to skip it; it also skips itself rather than failing an entry when the scanner image is unavailable. The shared TLS checks The section also runs everything the TLS-carrying profiles already run. The certificate must be the one the harness mounted, the connection must negotiate TLS 1.3 with an AEAD cipher, ALPN must not name a protocol the client did not offer, and no obsolete protocol or weak cipher may be accepted. See json-tls validation . The badge Two badges, and they mean different things: badge meaning green shield the TLS basics, checked on any entry with a TLS profile gold shield opted into this section and passed it Both are earned by the probes, never declared in meta.json . No badge means not verified , since most entries have no TLS profile at all. It never means failed . Running locally ./scripts/validate.sh <framework> The rotation checks replace files in the private /certs-tls directory and put them back afterwards, including when a check fails midway. The shared certs/ directory is never written to."},{"u":"test-profiles/h1/isolated/upload","t":"Upload (20 MB)","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated","d":"Measures large request body ingestion: the client posts a 20 MB payload and the server returns a checksum of what it received.","x":"Measures how efficiently a framework handles large request body ingestion. Each request sends a 20 MB binary payload and the server returns the byte count. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h1/isolated/upload/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Upload (20 MB)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the 20 MB upload benchmark.","x":"Standard Tuned Engine Must use the framework standard body reading API. Streaming is allowed if the framework supports it natively. May use custom buffer sizes, direct socket reads, or bypass framework body parsing for maximum throughput. No specific rules. The Upload profile measures how efficiently a framework handles large request body ingestion. The benchmark rotates across four payload sizes: 500 KB, 2 MB, 10 MB, and 20 MB. The server returns the byte count. Connections: 32, 256 How it works The load generator sends POST /upload with a binary body (500 KB, 2 MB, 10 MB, or 20 MB) using pre-built raw request files, rotating every 5 requests The server reads the entire request body Returns the total number of bytes received as plain text What it measures Request body ingestion throughput - reading large payloads from the network Memory management - buffering 20 MB per concurrent request I/O handling efficiency - how the framework manages sustained large transfers Connection overhead - at 20 MB per request, connection setup/teardown is significant Implementation rules The upload endpoint must actually read and process the request body. The returned byte count must be computed by reading the uploaded data, not inferred from request metadata. Do not return the value of the Content-Length header - this defeats the purpose of the test, which is to measure how efficiently the framework processes uploaded content. Frameworks may use small read buffers to process the upload incrementally. Holding the entire payload in memory is allowed but not required. The goal is to prove that the framework can efficiently compute a result over a large blob being sent to it. Expected response POST /upload HTTP/1.1 Content-Length: 20971520 HTTP/1.1 200 OK Content-Type: text/plain 20971520 Parameters Parameter Value Endpoint POST /upload Connections 32, 256 Pipeline 1 Duration 5s Runs 3 (best taken) Payloads 500 KB, 2 MB, 10 MB, 20 MB (rotated with -r 5 ) Notes I/O is the primary bottleneck - body ingestion dominates request handling time Lower connection counts are used because each request transfers 20 MB The load generator\'s bandwidth metric only measures response data (~8 bytes), not the uploaded payload"},{"u":"test-profiles/h1/isolated/upload/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/1.1 \\u203a Isolated \\u203a Upload (20 MB)","d":"The correctness checks validate.sh runs against the 20 MB upload benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the upload test. Small body upload Sends POST /upload with body Hello, HttpArena! (17 bytes) and Content-Type: application/octet-stream . Verifies the response body is 17 (the byte count of the uploaded data). Anti-cheat: random body upload Generates a random 48-byte body (base64-encoded from 64 random bytes), sends POST /upload with that body, and verifies the response matches the exact byte count of the random payload. This detects hardcoded responses or implementations that return Content-Length instead of actually reading the body. Varying payload sizes Sends POST /upload with four different payload sizes: 500 KB, 2 MB, 10 MB, and 20 MB (random binary data). Verifies the returned byte count matches the exact payload size for each."},{"u":"test-profiles/h2","t":"H/2","c":"Knowledge Base \\u203a Test Profiles","d":"HTTP/2 profiles measuring multiplexed stream performance, in both TLS and cleartext prior-knowledge variants.","x":"H/2 test profiles measure framework performance under multiplexed streams. The TLS variants model edge-facing deployments; the cleartext (h2c) variants model reverse-proxy-to-origin and service-to-service deployments inside a trust boundary. Baseline (TLS) Same workload as baseline over encrypted HTTP/2 connections with TLS and stream multiplexing. Static Files (TLS) Serves 20 static files of various types over HTTP/2 with multiplexed streams, simulating a browser page load. Baseline (h2c) Same /baseline2 endpoint over HTTP/2 cleartext on port 8082 - no TLS, prior-knowledge framing. Anti-cheat verifies the port refuses HTTP/1.1. JSON (h2c) JSON serialization workload over HTTP/2 cleartext on port 8082. Same 7 (count, m) rotation as the H/1 json profile."},{"u":"test-profiles/h2/baseline-h2","t":"Baseline (HTTP/2)","c":"Knowledge Base \\u203a Test Profiles \\u203a H/2","d":"The baseline throughput workload over encrypted HTTP/2 connections negotiated with TLS and ALPN.","x":"Same workload as the HTTP/1.1 baseline but over encrypted HTTP/2 connections using TLS and ALPN negotiation. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h2/baseline-h2/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/2 \\u203a Baseline (HTTP/2)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the HTTP/2 TLS baseline benchmark.","x":"Standard Tuned Engine Infrastructure Must use the framework standard HTTP/2 + TLS configuration. No custom ALPN settings or TLS cipher tuning. May optimize TLS settings, HTTP/2 stream limits, window sizes, and connection parameters. No specific rules. Ranked separately from frameworks. Configuration is free - worker counts, buffer sizes, event-loop and socket tuning are all allowed. ALPN, stream limits, window sizes and cipher selection are all fair game. HTTP/2 must be the server\'s own implementation: the endpoint may be a loaded module, the protocol may not be. The endpoint must be answered by a module or handler loaded into the server itself, not proxied to an application process behind it - an upstream app makes it a gateway entry, not an infrastructure one. Same workload as the HTTP/1.1 baseline - query parameter parsing and sum computation - but over encrypted HTTP/2 connections using TLS and ALPN negotiation. Connections: 256, 1,024 Concurrent streams per connection: 100 Workload GET /baseline2?a=1&b=1 sent over HTTP/2 with TLS. The load generator ( h2load ) opens multiple connections, each multiplexing up to 100 concurrent streams. What it measures HTTP/2 multiplexing efficiency TLS handshake and encryption overhead How frameworks handle many concurrent streams per connection HPACK header compression performance Expected request/response GET /baseline2?a=1&b=1 HTTP/2 HTTP/2 200 OK Content-Type: text/plain 2 How it differs from baseline Baseline (HTTP/1.1) Baseline (HTTP/2) Protocol HTTP/1.1 plaintext HTTP/2 over TLS Connections 512 - 4,096 256 - 1,024 Requests per connection 1 at a time 100 concurrent streams Load generator gcannon h2load Port 8080 8443 HTTP/2 uses far fewer connections because each connection multiplexes many streams. The lower connection counts reflect real-world HTTP/2 usage patterns. Parameters Parameter Value Endpoint GET /baseline2?a=1&b=1 Connections 256, 1,024 Streams per connection 100 ( -m 100 ) Duration 5s Runs 3 (best taken) Load generator h2load"},{"u":"test-profiles/h2/baseline-h2/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/2 \\u203a Baseline (HTTP/2)","d":"The correctness checks validate.sh runs against the HTTP/2 TLS baseline benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the baseline-h2 test. The HTTPS port (8443) must be responding before checks begin. HTTP/2 protocol negotiation Sends a request to https://localhost:8443/baseline2?a=1&b=1 using --http2 and checks the negotiated protocol version. The server must respond with HTTP/2 (not HTTP/1.1 fallback). GET /baseline2 over HTTP/2 Sends GET /baseline2?a=13&b=42 over HTTPS with HTTP/2 and verifies the response body is 55 . Anti-cheat: randomized query parameters Generates random values for a and b (100-999), sends GET /baseline2?a={a}&b={b} over HTTP/2, and verifies the response matches the expected sum. This detects hardcoded responses. TLS checks This profile\'s TLS listener on port 8443 also goes through the shared TLS checks. The certificate must be the one the harness mounted, the connection must negotiate TLS 1.3 with an AEAD cipher, ALPN must not name a protocol the client did not offer, and the server must accept no obsolete protocol or weak cipher. They are documented once, under json-tls validation ."},{"u":"test-profiles/h2/baseline-h2c","t":"Baseline (H2c)","c":"Knowledge Base \\u203a Test Profiles \\u203a H/2","d":"HTTP/2 cleartext on port 8082 with prior-knowledge framing, modelling proxy-to-origin and service-to-service traffic.","x":"HTTP/2 cleartext on port 8082 - prior-knowledge framing, no TLS. Models the reverse-proxy-to-origin and service-to-service patterns where HTTP/2 runs inside the trust boundary without encryption. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h2/baseline-h2c/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/2 \\u203a Baseline (H2c)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the HTTP/2 cleartext baseline benchmark.","x":"Standard Tuned Engine Must use the framework standard HTTP/2 cleartext (h2c) configuration. No custom ALPN settings or TLS cipher tuning (TLS isn\'t used on this port). May tune HTTP/2 stream limits, window sizes, and connection parameters. No specific rules. Ranked separately from frameworks. Same /baseline2?a=\\u2026&b=\\u2026 sum endpoint as the HTTP/2-TLS baseline, served as HTTP/2 cleartext - no TLS, h2 framing from the first byte. This matches the deployment pattern behind TLS-terminating load balancers (ALB \\u2192 backend, nginx \\u2192 app server) and inside service meshes where mTLS is handled by sidecars. Port: 8082 Connections: 256, 1,024, 4,096 Concurrent streams per connection: 100 Negotiation: prior-knowledge ( h2load -p h2c ) Workload GET /baseline2?a=1&b=1 sent over HTTP/2 cleartext. h2load opens multiple connections, each multiplexing up to 100 concurrent streams. The first bytes on every connection are the h2 preface - there is no HTTP/1.1 Upgrade dance and no ALPN (no TLS). What it measures HTTP/2 framing + HPACK + multiplexing without TLS overhead Protocol implementation cost in isolation - the delta against baseline-h2 is roughly the TLS cost Dual-serving h1 on the same port is allowed Port 8082 may also answer HTTP/1.1. The benchmark cannot pick it up: h2load -p h2c writes the HTTP/2 connection preface as the first bytes of every connection and never sends an HTTP/1.1 request, so the h1 path is unreachable during a measured run. Validation asserts the same way, with curl --http2-prior-knowledge . This is what RFC 9113 expects of a cleartext listener - distinguish the h2 preface from an HTTP/1.1 request line and serve each accordingly - and what nginx (1.25.1+), Apache ( Protocols h2c http/1.1 ) and h2o all do. The harness itself relies on it: the gRPC profiles run cleartext HTTP/2 against port 8080, the same listener that serves the HTTP/1.1 baseline. Expected request/response GET /baseline2?a=1&b=1 HTTP/2 HTTP/2 200 OK Content-Type: text/plain 2 How it differs from baseline-h2 Baseline (h2) Baseline (h2c) Protocol HTTP/2 over TLS HTTP/2 cleartext Port 8443 8082 Negotiation ALPN ( h2 ) prior-knowledge TLS required not used Real-world match edge-facing servers backend / service-to-service Parameters Parameter Value Endpoint GET /baseline2?a=1&b=1 Connections 256, 1,024, 4,096 Streams per connection 100 ( -m 100 ) Duration 5s Runs 3 (best taken) Load generator h2load with -p h2c"},{"u":"test-profiles/h2/baseline-h2c/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/2 \\u203a Baseline (H2c)","d":"The correctness checks validate.sh runs against the HTTP/2 cleartext baseline benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the baseline-h2c test. Port 8082 must be responding to a prior-knowledge h2 connection before checks begin. HTTP/2 cleartext (prior-knowledge) Sends GET /baseline2?a=1&b=1 to http://localhost:8082 with curl --http2-prior-knowledge . The negotiated protocol ( %{http_version} ) must report HTTP/2 . A server answering HTTP/1.1 here fails this check. The listener may also serve HTTP/1.1 on the same port. Nothing in the benchmark reaches that path: h2load -p h2c sends the HTTP/2 connection preface as the first bytes of every connection and never issues an HTTP/1.1 request, so a dual-serving port is still measured as h2c. GET /baseline2 over h2c Sends GET /baseline2?a=13&b=42 with prior-knowledge h2c and verifies the response body is 55 . Anti-cheat: randomized query parameters Generates random values for a and b (100\\u2013999), sends the request over h2c, and verifies the response matches the expected sum. Detects hardcoded responses. Content-Type Response must include Content-Type: text/plain (charset suffix permitted)."},{"u":"test-profiles/h2/json-h2c","t":"JSON (H2c)","c":"Knowledge Base \\u203a Test Profiles \\u203a H/2","d":"The JSON processing workload served over HTTP/2 cleartext, measuring serialization throughput without TLS overhead.","x":"The JSON Processing workload transported over HTTP/2 cleartext on port 8082 - measures JSON serialization throughput under multiplexed h2c streams without TLS overhead. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h2/json-h2c/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/2 \\u203a JSON (H2c)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the JSON over HTTP/2 cleartext benchmark.","x":"Standard Tuned Engine Must use the framework standard JSON serialization and standard HTTP/2 cleartext configuration. No pre-serialized caches, no custom serializers, no bypassing the framework response pipeline. May use alternative JSON libraries (simd-json, sonic-json), tune HTTP/2 stream and window parameters, and apply framework-specific optimizations. The JSON body must still be serialized per request from live data - pre-computed / pre-serialized response caches or response-lookup tables are not allowed on either type; they short-circuit the serialization workload the profile exists to measure. No specific rules. Ranked separately from frameworks. Same JSON Processing workload (dataset slice + per-item derived field + JSON serialization) served over HTTP/2 cleartext. Exercises the JSON pipeline under multiplexed h2 streams without the TLS CPU tax the baseline-h2 profile carries. Port: 8082 Connections: 1,024, 4,096 Concurrent streams per connection: 32 Negotiation: prior-knowledge ( h2load -p h2c ) Workload The load generator rotates through the same seven (count, m) pairs as the H/1 JSON profile: (1, 3) , (5, 7) , (10, 2) , (15, 5) , (25, 4) , (40, 8) , (50, 6) count selects a slice of the shared /data/dataset.json (50 items); m is the per-request multiplier that feeds into each item\'s total = price \\u00d7 quantity \\u00d7 m . Different multipliers per request ensure naive caching by path returns wrong values. What it measures JSON serialization throughput over h2c (no TLS in the way) h2 multiplexing efficiency when response bodies vary in size (1 \\u2192 50 items) Expected request/response GET /json/5?m=3 HTTP/2 HTTP/2 200 OK Content-Type: application/json {\\"count\\":5,\\"items\\":[\\u20265 items, each with total = price*quantity*3\\u2026]} Parameters Parameter Value Endpoint GET /json/{count}?m={multiplier} Connections 1,024, 4,096 Streams per connection 32 ( -m 32 ) Duration 5s Runs 3 (best taken) Load generator h2load with -p h2c and URI rotation ( -i json-h2c-uris.txt )"},{"u":"test-profiles/h2/json-h2c/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/2 \\u203a JSON (H2c)","d":"The correctness checks validate.sh runs against the JSON over HTTP/2 cleartext benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the json-h2c test. Port 8082 must already be answering h2c prior-knowledge before these checks begin. /json HTTP/2 cleartext Sends GET /json/1?m=1 to http://localhost:8082 with curl --http2-prior-knowledge . The negotiated protocol must report HTTP/2 - verifies the /json path isn\'t routed through an HTTP/1.1 fallback handler while /baseline2 correctly speaks h2c. Content-Type Response must include Content-Type: application/json (charset suffix permitted). Correctness across four (count, m) pairs Sends four requests with (count, m) \\u2208 {(12, 3), (22, 7), (31, 2), (50, 5)} - deliberately distinct from the benchmark\'s seven rotation pairs so any caching-by-key strategy returns stale data. For each response: the count field must equal the requested count, items.length must equal the count, every item must carry the full schema ( id , name , category , price , quantity , active , tags array, rating object with score + count , and total ), and total == price * quantity * m . Partial payloads that omit fields are rejected."},{"u":"test-profiles/h2/static-h2","t":"Static Files (HTTP/2)","c":"Knowledge Base \\u203a Test Profiles \\u203a H/2","d":"Serves twenty static assets over HTTP/2 with TLS, approximating a browser page load across multiplexed streams.","x":"Serves 20 static files of various types and sizes over HTTP/2 with TLS, simulating a realistic browser page load with multiplexed streams. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h2/static-h2/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/2 \\u203a Static Files (HTTP/2)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the HTTP/2 static file benchmark.","x":"Standard Tuned Engine Infrastructure File contents may be served from memory, but the cache must be the framework\'s own - a documented static file handler with whatever caching it comes with. A cache assembled in the entry does not count: no reading the directory into a map at startup, no pre-loaded buffers, no mapping the files by hand. The cache must also follow the disk: replace a file and the next response must carry the new bytes. Compression must use the framework\'s standard middleware or built-in static file handler - no handmade compression code. Serving the pre-compressed .br / .gz variants that sit on disk next to the originals is allowed , by a documented framework API (e.g. ASP.NET MapStaticAssets , nginx gzip_static / brotli_static , Caddy precompressed , Hono serveStatic({ precompressed: true }) ) where the framework has one, and otherwise by selecting the variant in the entry off Accept-Encoding . Those bytes already exist, so choosing one is a file read rather than compression - which is why this is allowed while compressing by hand is not. The response must carry the original file\'s Content-Type , the matching Content-Encoding , and must still follow the disk. File contents may be served from memory, the same rule as Standard: the cache must be the framework\'s own rather than one assembled in the entry, and it must follow the disk - replace a file and the next response must carry the new bytes. Pre-rendered response headers stay allowed, as does serving pre-compressed .gz/.br variants from disk via any mechanism, with any compression approach. File contents may be served from memory, the same rule as Standard: the cache must be the framework\'s own rather than one assembled in the entry, and it must follow the disk - replace a file and the next response must carry the new bytes. No other restrictions. Ranked separately from frameworks. Configuration is free, including sendfile, open_file_cache, mmap and in-memory caching - serving files fast from a tuned cache is the job. Pre-compressed .br/.gz variants may be served through the server\'s own mechanism (nginx gzip_static / brotli_static, Caddy precompressed). Bodies must come from the mounted static directory; a handler that synthesizes them is not serving files. HTTP/2 must be the server\'s own implementation, with stream and window tuning allowed. Serves 20 static files of various types and sizes over HTTP/2 with TLS, simulating a realistic browser page load with multiplexed streams. Connections: 256, 1,024 Concurrent streams per connection: 32 Workload The load generator ( h2load ) requests 20 static files in a round-robin pattern across multiplexed streams. All requests include Accept-Encoding: br;q=1, gzip;q=0.8 . CSS (5 files, 8\\u201355 KB): reset.css , layout.css , theme.css , components.css , utilities.css JavaScript (5 files, 15\\u2013400 KB): analytics.js , helpers.js , app.js , vendor.js , router.js HTML (2 files, 5\\u20138 KB): header.html , footer.html Fonts (2 files, 20\\u201325 KB): regular.woff2 , bold.woff2 SVG (2 files, 12\\u201355 KB): logo.svg , icon-sprite.svg Images (3 files, 15\\u2013120 KB): hero.webp , thumb1.webp , thumb2.webp JSON (1 file, 3 KB): manifest.json Total payload: ~1.16 MB across 20 files (~966 KB compressible text + ~200 KB binary). Pre-compressed versions ( .gz , .br ) are available on disk. See the HTTP/1.1 static files compression section for full compression rules. What it measures Static file serving throughput over HTTP/2 HTTP/2 multiplexing efficiency with varied response sizes Content-Type handling for different file types File serving strategy efficiency - how well the framework moves bytes to the client (sendfile, readahead, buffer sizing, and its own caching where it has any) TLS overhead with realistic mixed payloads Compression efficiency (optional - reduces I/O at the cost of CPU) Expected request/response GET /static/reset.css HTTP/2 HTTP/2 200 OK Content-Type: text/css (file contents) GET /static/app.js HTTP/2 HTTP/2 200 OK Content-Type: application/javascript (file contents) How it differs from baseline-h2 Baseline (HTTP/2) Static Files (HTTP/2) Endpoint Single GET /baseline2 20 different /static/* URIs Response size ~2 bytes 3\\u2013400 KB (varied) Content types text/plain CSS, JS, HTML, fonts, SVG, WebP, JSON h2load mode Single URI Multi-URI ( -i flag, round-robin) Parameters Parameter Value Endpoint 20 URIs under /static/* Connections 256, 1,024 Streams per connection 32 ( -m 32 ) Duration 5s Runs 3 (best taken) Load generator h2load with -i (multi-URI)"},{"u":"test-profiles/h2/static-h2/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/2 \\u203a Static Files (HTTP/2)","d":"The correctness checks validate.sh runs against the HTTP/2 static file benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate.sh for every framework subscribed to the static-h2 test. The HTTPS port (8443) must be responding before checks begin. Content-Type headers Verifies correct Content-Type headers for representative file types over HTTPS with HTTP/2: GET /static/reset.css - expects Content-Type: text/css GET /static/app.js - expects Content-Type: application/javascript GET /static/manifest.json - expects Content-Type: application/json Note: text/javascript is accepted as equivalent to application/javascript per RFC 9239. Response size Requests GET /static/reset.css over HTTP/2 and verifies the response size is greater than 0 bytes. This confirms the server is actually serving file content, not empty responses. 404 for nonexistent file Sends GET /static/nonexistent.txt over HTTP/2 and verifies the server returns HTTP 404 . TLS checks This profile\'s TLS listener on port 8443 also goes through the shared TLS checks. The certificate must be the one the harness mounted, the connection must negotiate TLS 1.3 with an AEAD cipher, ALPN must not name a protocol the client did not offer, and the server must accept no obsolete protocol or weak cipher. They are documented once, under json-tls validation ."},{"u":"test-profiles/h3","t":"H/3","c":"Knowledge Base \\u203a Test Profiles","d":"HTTP/3 profiles measuring performance over QUIC. Only frameworks with native QUIC support take part.","x":"H/3 test profiles measure framework performance over QUIC, the UDP-based transport protocol. Only frameworks with native QUIC support participate. Baseline Raw throughput over QUIC, testing HTTP/3 transport performance on frameworks with native QUIC support. Static Files Serves 20 static files over HTTP/3 (QUIC), simulating browser asset loading over the newest protocol."},{"u":"test-profiles/h3/baseline-h3","t":"Baseline (HTTP/3)","c":"Knowledge Base \\u203a Test Profiles \\u203a H/3","d":"Measures raw request throughput over QUIC, the UDP-based transport behind HTTP/3.","x":"Tests raw throughput over QUIC, the UDP-based transport protocol that powers HTTP/3. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h3/baseline-h3/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/3 \\u203a Baseline (HTTP/3)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the HTTP/3 baseline benchmark.","x":"Standard Tuned Engine Infrastructure Must use the framework native QUIC/HTTP3 support with default configuration. May tune QUIC parameters, congestion control, and UDP buffer sizes. No specific rules. Ranked separately from frameworks. Configuration is free - worker counts, buffer sizes, event-loop and socket tuning are all allowed. QUIC parameters, congestion control and UDP buffer sizes may all be tuned. QUIC and HTTP/3 must come from the server\'s own implementation or a standard library it is built against (quiche, ngtcp2, quictls, msquic). The endpoint must be answered by a module or handler loaded into the server itself, not proxied to an application process behind it - an upstream app makes it a gateway entry, not an infrastructure one. The HTTP/3 Baseline profile tests raw throughput over QUIC, the UDP-based transport protocol that powers HTTP/3. Connections: 64 How it works The load generator ( h2load-h3 ) connects to the server over HTTP/3 (QUIC) on port 8443 Sends GET /baseline2?a=1&b=1 over 64 connections, each multiplexing 64 streams, across 64 worker threads The server parses query parameters and returns the sum What it measures QUIC transport performance - UDP-based connection handling HTTP/3 framing overhead - compared to HTTP/1.1 and HTTP/2 TLS 1.3 integration - QUIC mandates encryption Framework QUIC implementation quality - varies significantly across frameworks Expected request/response GET /baseline2?a=1&b=1 HTTP/3 HTTP/3 200 OK Content-Type: text/plain 2 Parameters Parameter Value Endpoint GET /baseline2?a=1&b=1 Connections 64 Streams per connection 64 ( -m 64 ) Threads 64 ( H3THREADS ) Duration 5s Runs 3 (best taken) Load generator h2load-h3 ( --alpn-list=h3 ) Port 8443 (TLS + QUIC) Notes HTTP/3 support is not universal - only frameworks with native QUIC support participate HTTP/3 throughput is typically 4\\u20136\\u00d7 lower than HTTP/2 on the same framework. This reflects the inherent CPU cost of QUIC (per-packet AEAD, no kernel TLS offload, userspace packet processing) - see the h2load-h3 load generator docs for details"},{"u":"test-profiles/h3/baseline-h3/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/3 \\u203a Baseline (HTTP/3)","d":"The correctness checks validate.sh runs against the HTTP/3 baseline benchmark before a framework\'s results are accepted.","x":"validate.sh sends four requests to /baseline2?a=13&b=42 over QUIC and requires all four to come back 2xx : PASS [baseline-h3 over QUIC] (4/4 2xx, ALPN h3) There is no HTTP/3 client in the validator\'s base image and OpenSSL cannot speak QUIC, so the check runs the same ngtcp2-built h2load the benchmark uses, with --alpn-list=h3 . If that image has not been built the check reports SKIP and is counted as skipped. It never passes silently. This profile previously had no checks of its own and leaned on the HTTP/2 baseline validation, which only covers an entry that also subscribes to an h2 profile. An entry subscribing to H3 alone ran no assertions at all and still exited 0. Entries that serve /baseline2 over both h2 and h3 on port 8443 are still covered by HTTP/2 Baseline validation as well."},{"u":"test-profiles/h3/static-h3","t":"Static Files (HTTP/3)","c":"Knowledge Base \\u203a Test Profiles \\u203a H/3","d":"Serves twenty static assets over QUIC, approximating a browser loading page resources over HTTP/3.","x":"Serves 20 static files of various types over QUIC, simulating a browser loading page assets over HTTP/3. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/h3/static-h3/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a H/3 \\u203a Static Files (HTTP/3)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the HTTP/3 static file benchmark.","x":"Standard Tuned Engine Infrastructure File contents may be served from memory, but the cache must be the framework\'s own - a documented static file handler with whatever caching it comes with. A cache assembled in the entry does not count: no reading the directory into a map at startup, no pre-loaded buffers, no mapping the files by hand. The cache must also follow the disk: replace a file and the next response must carry the new bytes. Compression must use the framework\'s standard middleware or built-in static file handler - no handmade compression code. Serving the pre-compressed .br / .gz variants that sit on disk next to the originals is allowed , by a documented framework API (e.g. ASP.NET MapStaticAssets , nginx gzip_static / brotli_static , Caddy precompressed , Hono serveStatic({ precompressed: true }) ) where the framework has one, and otherwise by selecting the variant in the entry off Accept-Encoding . Those bytes already exist, so choosing one is a file read rather than compression - which is why this is allowed while compressing by hand is not. The response must carry the original file\'s Content-Type , the matching Content-Encoding , and must still follow the disk. File contents may be served from memory, the same rule as Standard: the cache must be the framework\'s own rather than one assembled in the entry, and it must follow the disk - replace a file and the next response must carry the new bytes. Pre-rendered response headers stay allowed, as does serving pre-compressed .gz/.br variants from disk via any mechanism, with any compression approach. File contents may be served from memory, the same rule as Standard: the cache must be the framework\'s own rather than one assembled in the entry, and it must follow the disk - replace a file and the next response must carry the new bytes. No other restrictions. Ranked separately from frameworks. Configuration is free, including sendfile, open_file_cache, mmap and in-memory caching - serving files fast from a tuned cache is the job. Pre-compressed .br/.gz variants may be served through the server\'s own mechanism (nginx gzip_static / brotli_static, Caddy precompressed). Bodies must come from the mounted static directory; a handler that synthesizes them is not serving files. QUIC and HTTP/3 must come from the server\'s own implementation or a standard library it is built against, with QUIC parameter tuning allowed. The HTTP/3 Static Files profile serves 20 static files of various types over QUIC, simulating a browser loading page assets over HTTP/3. Connections: 64 How it works The load generator ( h2load-h3 ) connects over HTTP/3 (QUIC) on port 8443 Cycles through 20 URIs from requests/static-h2-uris.txt (same file set as the HTTP/2 static test) All requests include Accept-Encoding: br;q=1, gzip;q=0.8 Each request fetches a different static file - CSS, JavaScript, HTML, fonts, SVGs, WebP images, and JSON The server returns file contents with the correct Content-Type , optionally compressed What it measures HTTP/3 static asset serving - mixed content types and sizes over QUIC QUIC multiplexing - how well the framework handles varied concurrent requests Content-Type handling - correct MIME type mapping across file types Compression efficiency (optional) - reduces payload size at the cost of CPU Static files 20 files (~1.16 MB total, ~966 KB compressible text + ~200 KB binary): Type Files Examples CSS 5 reset.css , layout.css , theme.css JavaScript 5 app.js , vendor.js , router.js HTML 2 header.html , footer.html Fonts 2 regular.woff2 , bold.woff2 SVG 2 logo.svg , icon-sprite.svg WebP 3 hero.webp , thumb1.webp , thumb2.webp JSON 1 manifest.json Pre-compressed versions ( .gz , .br ) are available on disk. See the HTTP/1.1 static files compression section for full compression rules. Expected request/response GET /static/logo.svg HTTP/3 HTTP/3 200 OK Content-Type: image/svg+xml (file contents) Parameters Parameter Value Endpoint 20 URIs under /static/* Connections 64 Streams per connection 64 ( -m 64 ) Threads 64 ( H3THREADS ) Duration 5s Runs 3 (best taken) Load generator h2load-h3 ( --alpn-list=h3 -i \\u2026 ) Port 8443 (TLS + QUIC)"},{"u":"test-profiles/h3/static-h3/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a H/3 \\u203a Static Files (HTTP/3)","d":"The correctness checks validate.sh runs against the HTTP/3 static file benchmark before a framework\'s results are accepted.","x":"validate.sh requests /static/reset.css four times over QUIC and requires all four to come back 2xx : PASS [static-h3 over QUIC] (4/4 2xx, ALPN h3) There is no HTTP/3 client in the validator\'s base image and OpenSSL cannot speak QUIC, so the check runs the same ngtcp2-built h2load the benchmark uses, with --alpn-list=h3 . If that image has not been built the check reports SKIP and is counted as skipped. It never passes silently. This profile previously had no checks of its own and leaned on the HTTP/2 static validation, which only covers an entry that also subscribes to an h2 profile. An entry subscribing to H3 alone ran no assertions at all and still exited 0. Content-Type, response sizes and 404 handling are still asserted over h2 by HTTP/2 Static Files validation for entries that serve both on port 8443."},{"u":"test-profiles/ws","t":"WebSocket","c":"Knowledge Base \\u203a Test Profiles","d":"WebSocket profiles measuring real-time bidirectional messaging throughput after an HTTP/1.1 upgrade.","x":"WebSocket test profiles measure framework performance for real-time bidirectional communication. The server listens on port 8080 and accepts WebSocket upgrade requests. Echo WebSocket echo throughput - upgrade, send messages, receive echoes. Echo Pipelined (16x) WebSocket echo with 16 messages in flight per connection - measures frame batching and read-buffer draining. Echo Short-lived WebSocket echo with each connection closed after 10 messages - measures the cost of the upgrade handshake."},{"u":"test-profiles/ws/echo","t":"Echo (WebSocket)","c":"Knowledge Base \\u203a Test Profiles \\u203a WebSocket","d":"Measures WebSocket echo throughput: each connection upgrades over HTTP/1.1, then exchanges text messages one at a time.","x":"Measures WebSocket echo throughput. Each connection upgrades via HTTP/1.1, then sends text messages and receives echoes. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/ws/echo-limited","t":"Echo Short-lived (WebSocket)","c":"Knowledge Base \\u203a Test Profiles \\u203a WebSocket","d":"Measures WebSocket upgrade cost: each connection is closed after 10 echo messages, so the handshake is paid continuously rather than amortized.","x":"Measures the cost of the WebSocket upgrade itself. Each connection performs the HTTP/1.1 upgrade, exchanges 10 messages, then closes and is replaced by a new one, so the handshake is paid continuously instead of being amortized over a long-lived session. This is the WebSocket counterpart to the HTTP/1.1 Short-lived Connection profile. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/ws/echo-limited/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a WebSocket \\u203a Echo Short-lived (WebSocket)","d":"Endpoint contract and rules for the short-lived WebSocket profile, where each connection is retired after 10 echo messages and re-upgraded.","x":"Standard Tuned Engine Must use the framework standard WebSocket API with default buffer sizes. The upgrade handshake must be handled by the framework\'s own WebSocket support - pre-computing or caching the 101 response across connections is not allowed. May optimize the upgrade path, frame handling and buffer sizes, including custom handshake parsing, as long as every connection performs a real handshake. No specific rules. Ranked separately from frameworks. The endpoint is exactly the one used by Echo : /ws on port 8080, echoing each text frame back unchanged. The difference is entirely in the client: the load generator sends 10 messages per connection, then closes it and opens a replacement. Connections: 512, 4,096 Messages per connection: 10 Pipeline: 1 (one message in flight at a time) Workload Open a TCP connection to port 8080 Send an HTTP/1.1 upgrade request to /ws After 101 Switching Protocols , switch to WebSocket framing Send a text frame containing \\"hello\\" , read the echo, repeat 10 times Close the connection and return to step 1 Measure echoes received per second Throughput is counted the same way as the other WebSocket profiles: one echo received is one completed response. The upgrade itself is not counted as a response, so a framework with a slow handshake shows up as lower echo throughput rather than as inflated numbers. What it measures Cost of the WebSocket upgrade handshake: request parsing, Sec-WebSocket-Accept computation, and the 101 response Per-connection setup and teardown: allocation of the connection\'s buffers and WebSocket state, and how promptly they are released Accept-path throughput under continuous connection churn, rather than the steady-state frame loop the other WebSocket profiles measure A framework that performs well on Echo but poorly here is spending its time in connection setup, not in frame handling. Expected upgrade request/response GET /ws HTTP/1.1 Host: localhost:8080 Upgrade: websocket Connection: Upgrade Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ== Sec-WebSocket-Version: 13 HTTP/1.1 101 Switching Protocols Upgrade: websocket Connection: Upgrade Sec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo= Sec-WebSocket-Accept must be computed per connection from the client\'s Sec-WebSocket-Key as defined in RFC 6455 . Because this profile establishes a new connection every 10 messages, a server that returns a fixed, precomputed accept value would fail the handshake for every connection after the first. Notes No server-side changes are needed beyond the existing /ws endpoint. A framework already subscribed to echo-ws implements everything this profile requires. Closing is driven by the client. The server should honour the close handshake, but a framework that simply observes the TCP FIN is not penalized."},{"u":"test-profiles/ws/echo-limited/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a WebSocket \\u203a Echo Short-lived (WebSocket)","d":"The short-lived WebSocket profile is validated by the same validate-ws.py checks as the Echo profile; the message limit is a load-generator behaviour.","x":"The same validate-ws.py checks executed for the Echo profile apply here. Closing a connection after 10 messages is a load-generator behaviour, not a separate server contract: the endpoint, the framing and the expected echo are identical. scripts/validate.sh runs those checks for any framework subscribed to echo-ws , echo-ws-pipeline or echo-ws-limited , so subscribing to this profile alone is enough to have the WebSocket endpoint validated. The behaviour specific to this profile, a fresh handshake for every connection, is exercised during the benchmark run rather than by a dedicated check. A server that only answers the first handshake correctly, or that reuses a precomputed Sec-WebSocket-Accept , fails every subsequent upgrade and reports a throughput near zero."},{"u":"test-profiles/ws/echo-pipeline","t":"Echo Pipelined (WebSocket)","c":"Knowledge Base \\u203a Test Profiles \\u203a WebSocket","d":"Measures WebSocket echo throughput with sixteen messages sent back to back before the first reply is read.","x":"Measures WebSocket echo throughput with pipelining. Each connection upgrades via HTTP/1.1, then sends 16 messages back-to-back before waiting for the echoes. Implementation Guidelines Endpoint specification, expected request/response format, and type-specific rules. Validation All checks executed by the validation script for this test profile."},{"u":"test-profiles/ws/echo-pipeline/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a WebSocket \\u203a Echo Pipelined (WebSocket)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the pipelined WebSocket echo benchmark.","x":"Standard Tuned Engine Must use the framework standard WebSocket API with default buffer sizes. No custom batching or read-ahead optimizations. May optimize WebSocket frame handling, buffer sizes, and use custom frame parsers or batched read paths. No specific rules. Ranked separately from frameworks. Measures WebSocket echo throughput with pipelining. Each connection upgrades via HTTP/1.1, then sends 16 text messages back-to-back before waiting for the echoes. Each echo counts as one completed response. Connections: 512, 4,096, 16,384 Pipeline: 16 (16 messages in flight per connection - send batch, drain echoes, repeat) Workload Open TCP connection to port 8080 Send HTTP/1.1 upgrade request to /ws After receiving 101 Switching Protocols , switch to WebSocket framing Send 16 text frames containing \\"hello\\" back-to-back, then read 16 echo frames Measure messages per second What it measures WebSocket frame parsing efficiency under burst load Frameworks that drain multiple frames from a single read buffer gain a major advantage Frameworks processing one frame at a time per connection see minimal improvement over the non-pipelined echo Write coalescing and syscall reduction on the send path Expected upgrade request/response GET /ws HTTP/1.1 Host: localhost:8080 Upgrade: websocket Connection: Upgrade Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ== Sec-WebSocket-Version: 13 HTTP/1.1 101 Switching Protocols Upgrade: websocket Connection: Upgrade Sec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo= Parameters Parameter Value Endpoint /ws (WebSocket upgrade) Connections 512, 4,096, 16,384 Pipeline 16 (16 messages in flight per connection) Message \\"hello\\" (5 bytes, text frame) Duration 5s Runs 3 (best taken) Load generator gcannon --ws -p 16"},{"u":"test-profiles/ws/echo-pipeline/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a WebSocket \\u203a Echo Pipelined (WebSocket)","d":"The correctness checks validate.sh runs against the pipelined WebSocket echo benchmark before a framework\'s results are accepted.","x":"The same validate-ws.py checks executed for the Echo profile apply here - pipelining is a load-generator behavior, not a separate server contract. The endpoint and frame semantics are identical, so a server that passes the Echo validation passes for Echo Pipelined as well. See the Echo validation page for the full list of checks (handshake, accept header, text/binary echo, multi-message echo, clean close, non-upgrade rejection, post-validation health check)."},{"u":"test-profiles/ws/echo/implementation","t":"Implementation Guidelines","c":"Knowledge Base \\u203a Test Profiles \\u203a WebSocket \\u203a Echo (WebSocket)","d":"Endpoint contract, request and response shapes, and the anti-cheat constraints a framework must satisfy for the WebSocket echo benchmark.","x":"Standard Tuned Engine Must use the framework standard WebSocket API with default buffer sizes. May optimize WebSocket frame handling, buffer sizes, and use custom frame parsers. No specific rules. Ranked separately from frameworks. Measures WebSocket echo throughput. Each connection upgrades via HTTP/1.1, then sends text messages and receives echoes. Each echo counts as one completed response. Connections: 512, 4,096, 16,384 Pipeline: 1 (one message in flight per connection - send, await echo, repeat) Workload Open TCP connection to port 8080 Send HTTP/1.1 upgrade request to /ws After receiving 101 Switching Protocols , switch to WebSocket framing Send text frames containing \\"hello\\" , receive echo frames Measure messages per second What it measures WebSocket upgrade handshake performance WebSocket frame parsing and construction efficiency Echo round-trip latency under load Connection scalability for real-time workloads Expected upgrade request/response GET /ws HTTP/1.1 Host: localhost:8080 Upgrade: websocket Connection: Upgrade Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ== Sec-WebSocket-Version: 13 HTTP/1.1 101 Switching Protocols Upgrade: websocket Connection: Upgrade Sec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo= Parameters Parameter Value Endpoint /ws (WebSocket upgrade) Connections 512, 4,096, 16,384 Pipeline 1 (one message in flight per connection) Message \\"hello\\" (5 bytes, text frame) Duration 5s Runs 3 (best taken) Load generator gcannon --ws"},{"u":"test-profiles/ws/echo/validation","t":"Validation","c":"Knowledge Base \\u203a Test Profiles \\u203a WebSocket \\u203a Echo (WebSocket)","d":"The correctness checks validate.sh runs against the WebSocket echo benchmark before a framework\'s results are accepted.","x":"The following checks are executed by validate-ws.py for every framework subscribed to the echo-ws test. The script uses a raw socket WebSocket client with no external dependencies. Upgrade handshake (101 status) Sends an HTTP/1.1 WebSocket upgrade request to /ws with a random Sec-WebSocket-Key . Verifies the server responds with HTTP 101 Switching Protocols . Sec-WebSocket-Accept header Verifies the server returns a correct Sec-WebSocket-Accept value, computed as Base64(SHA-1(key + \\"258EAFA5-E914-47DA-95CA-5BAB11DC85B6\\")) per RFC 6455. Text echo Sends a text frame containing a random string ( HttpArena-validate-{random_hex} ) and verifies the server echoes back the exact same text in a text frame (opcode 0x1). Binary echo Sends a binary frame containing 256 random bytes and verifies the server echoes back the exact same bytes in a binary frame (opcode 0x2). Multi-message echo Sends 5 text frames rapidly in sequence, each with a unique random payload. Verifies all 5 are echoed back in order with correct content. This tests message ordering and buffering under burst conditions. Clean close Sends a close frame with code 1000 (normal closure) and reason \\"validate done\\" . Verifies the server responds with a close frame containing code 1000. Reject non-upgrade request Sends a regular GET /ws request (without WebSocket upgrade headers) and verifies the server returns a 4xx status code (400 or 426), not 101 or 5xx. The server must not crash on non-WebSocket requests to the WebSocket endpoint. Post-validation health check After all tests complete, opens a new WebSocket connection, sends a text frame \\"health\\" , verifies the echo, and performs a clean close. This confirms the server is still alive and functioning correctly after handling the full validation suite."}]');
