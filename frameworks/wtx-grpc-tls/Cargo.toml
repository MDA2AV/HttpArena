[package]
name = "httparena-wtx-grpc-tls"
version = "0.1.0"
edition = "2024"

[build-dependencies]
pb-rs = { default-features = false, version = "0.10" }

[dependencies]
quick-protobuf = { default-features = false, version = "0.8" }
tokio = { default-features = false, features = ["macros", "rt-multi-thread"], version = "1.0" }
wtx = { default-features = false, features = ["crypto-ring", "grpc-server", "optimizations-std", "quick-protobuf", "tokio", "getrandom"], git = "https://github.com/c410-f3r/wtx", branch = "misc" }

[profile.release]
codegen-units = 1
lto = "thin"
opt-level = 3
panic = "abort"
