[package]
name = "httparena-rocket"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = { version = "0.5", features = ["json"] }

[profile.release]
opt-level = 3
codegen-units = 1
lto = "thin"
