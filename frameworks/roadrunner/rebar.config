{erl_opts, [debug_info]}.

{minimum_otp_vsn, "29"}.

{deps, [
    %% Benchmark-only: pinned to the unreleased conn_spawn memory branch so
    %% CI exercises it. Swap back to a hex version ({roadrunner, "0.3.0"})
    %% before merge.
    {roadrunner, {git, "https://github.com/arizona-framework/roadrunner.git",
        {branch, "perf/conn-spawn-memory"}}},
    {epgsql, "4.8.0"},
    {pooler, "1.6.0"}
]}.

{relx, [
    {release, {roadrunner_httparena, "0.1.0"},
        %% `quic` is an optional roadrunner runtime dep (started on demand
        %% for HTTP/3, not in roadrunner's `applications`), so a release
        %% using http3 must include it explicitly or the h3 listener fails
        %% with `{quic, "no such file or directory"}` at boot.
        [roadrunner_httparena, roadrunner, quic]},
    {sys_config, "./config/sys.config"},
    {dev_mode, false},
    {include_erts, true},
    {extended_start_script, true}
]}.

{profiles, [
    {prod, [
        {relx, [{mode, prod}]}
    ]}
]}.
